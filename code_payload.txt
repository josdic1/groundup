🚀 GROUNDUP MONOREPO BUNDLE
================================================================================

PROJECT ROOT: /Users/joshdicker/Documents/Organized_Files/01_Projects/groundup
TOTAL FILES: 56
INCLUDES: frontend, server, packages/shared-types

--- BATCH 1 | package.json ---
STATUS: ✅ REVIEWABLE (16 lines)
FILE: package.json
```json
{
  "name": "groundup",
  "private": true,
  "workspaces": [
    "frontend",
    "server",
    "packages/*"
  ],
  "scripts": {
    "build:shared": "npm run build --workspace=@groundup/shared-types",
    "build:server": "npm run build:shared && npm run build --workspace=server",
    "start:server": "npm run start --workspace=server",
    "build:frontend": "npm run build:shared && npm run build --workspace=frontend"
  }
}
```

================================================================================

--- BATCH 2 | packages/shared-types/package.json ---
STATUS: ✅ REVIEWABLE (13 lines)
FILE: packages/shared-types/package.json
```json
{
  "name": "@groundup/shared-types",
  "version": "1.0.0",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
```

================================================================================

--- BATCH 3 | packages/shared-types/tsconfig.json ---
STATUS: ✅ REVIEWABLE (15 lines)
FILE: packages/shared-types/tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "declaration": true,
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true
  },
  "include": ["src"]
}
```

================================================================================

--- BATCH 4 | packages/shared-types/src/index.ts ---
STATUS: ✅ REVIEWABLE (77 lines)
FILE: packages/shared-types/src/index.ts
```ts
export type SoldBy = 'weight' | 'each';

export type MenuItem = {
  id: string;
  name: string;
  category: string;
  price: number;
  unit: string;
  soldBy: SoldBy;
  isActive: boolean;
};

export type Address = {
  street: string;
  city: string;
  state: string;
  zip: string;
};

export type Customer = {
  customerId: string;
  firstName: string;
  lastName: string;
  phone: string;
  email: string;
  loyaltyPoints: number;
  dietaryNotes: string;
  address: Address;
};

// Customer enriched with computed order stats, returned by GET /api/customers
export type CustomerWithStats = Customer & {
  totalSpent: number;
  orderCount: number;
  lastOrderAt: number | null;
};

export type OrderStatus = 'placed' | 'in_prep' | 'ready' | 'completed' | 'cancelled';
export type OrderSource = 'counter' | 'online';
export type FulfillmentType = 'in_store' | 'pickup' | 'delivery';

export type SizeTier = {
  label: string;
  weightLbs: number | null;
  price: number;
};

export type MenuItemTiers = {
  menuItemId: string;
  tiers: SizeTier[];
};

export type OrderLineItem = {
  menuItemId: string;
  name: string;
  quantity: number;
  unitPrice: number;
  lineTotal: number;
  notes?: string;
};

export type Order = {
  id: string;
  customerId: string | null;
  customerName: string;
  items: OrderLineItem[];
  total: number;
  status: OrderStatus;
  source: OrderSource;
  fulfillment: FulfillmentType;
  claimedBy: string | null;
  createdAt: number;
  notes?: string;
  deliveryAddress?: Address;
  scheduledFor?: number;
};
```

================================================================================

--- BATCH 5 | server/package.json ---
STATUS: ✅ REVIEWABLE (27 lines)
FILE: server/package.json
```json
{
  "name": "server",
  "version": "1.0.0",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@groundup/shared-types": "*",
    "cors": "^2.8.6",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/node": "^26.0.1",
    "tsx": "^4.22.4",
    "typescript": "^6.0.3"
  }
}
```

================================================================================

--- BATCH 6 | server/tsconfig.json ---
STATUS: ✅ REVIEWABLE (15 lines)
FILE: server/tsconfig.json
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "outDir": "dist",
    "rootDir": "src",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true
  },
  "include": ["src"]
}
```

================================================================================

--- BATCH 7 | server/src/index.ts ---
STATUS: ⚠️ LARGE (396 lines)
FILE: server/src/index.ts
```ts
import express from 'express';
import cors from 'cors';
import type { Order, OrderLineItem, OrderSource, FulfillmentType, Address, Customer, MenuItem } from '@groundup/shared-types';
import { CUSTOMERS } from './data/customers.js';
import { HISTORICAL_ORDERS } from './data/historicalOrders.js';
import { MENU } from './data/menu.js';

const app = express();
app.use(cors());
app.use(express.json());

let orders: Order[] = [];
let orderCounter = 1001;
let customers: Customer[] = [...CUSTOMERS];
let customerCounter = customers.length + 1;
let menu: MenuItem[] = [...MENU];

const menuCategoryById = new Map(MENU.map((m) => [m.id, m.category]));

function computeCustomerStats(customerId: string) {
  const customerOrders = orders.filter(
    (o) => o.customerId === customerId && o.status !== 'cancelled'
  );
  const totalSpent = Math.round(
    customerOrders.reduce((sum, o) => sum + o.total, 0) * 100
  ) / 100;
  const orderCount = customerOrders.length;
  const lastOrderAt = customerOrders.length > 0
    ? Math.max(...customerOrders.map((o) => o.createdAt))
    : null;
  return { totalSpent, orderCount, lastOrderAt };
}

// ----- Orders -----

app.get('/api/orders', (req, res) => {
  res.json(orders);
});

app.post('/api/orders', (req, res) => {
  const { customerName, customerId, items, source, fulfillment, notes, deliveryAddress } = req.body as {
    customerName: string;
    customerId?: string | null;
    items: OrderLineItem[];
    source?: OrderSource;
    fulfillment?: FulfillmentType;
    notes?: string;
    deliveryAddress?: Address;
  };

  if (!items || items.length === 0) {
    return res.status(400).json({ error: 'Order must have at least one item' });
  }

  const total = Math.round(
    items.reduce((sum, i) => sum + i.lineTotal, 0) * 100
  ) / 100;

  const newOrder: Order = {
    id: `ord-${orderCounter++}`,
    customerId: customerId ?? null,
    customerName: customerName || 'Walk-in',
    items,
    total,
    status: 'placed',
    source: source ?? 'counter',
    fulfillment: fulfillment ?? 'in_store',
    claimedBy: null,
    createdAt: Date.now(),
    notes,
    deliveryAddress,
  };

  orders = [newOrder, ...orders];
  res.status(201).json(newOrder);
});

app.patch('/api/orders/:id', (req, res) => {
  const { id } = req.params;
  const { status, claimedBy } = req.body as {
    status?: Order['status'];
    claimedBy?: string;
  };

  const order = orders.find((o) => o.id === id);
  if (!order) {
    return res.status(404).json({ error: 'Order not found' });
  }

  if (status) order.status = status;
  if (claimedBy !== undefined) order.claimedBy = claimedBy;

  res.json(order);
});

app.delete('/api/orders/:id', (req, res) => {
  const { id } = req.params;
  const order = orders.find((o) => o.id === id);
  if (!order) {
    return res.status(404).json({ error: 'Order not found' });
  }
  if (order.status !== 'placed') {
    return res.status(400).json({ error: 'Only unclaimed orders can be sent back to the register' });
  }
  orders = orders.filter((o) => o.id !== id);
  res.json({ ok: true });
});

// ----- Menu -----

app.get('/api/menu', (req, res) => {
  res.json(menu.filter((item) => item.isActive));
});

app.get('/api/menu/admin', (req, res) => {
  res.json(menu);
});

app.patch('/api/menu/:id', (req, res) => {
  const { id } = req.params;
  const item = menu.find((m) => m.id === id);
  if (!item) return res.status(404).json({ error: 'Menu item not found' });

  Object.assign(item, req.body, { id: item.id });
  res.json(item);
});

app.post('/api/menu', (req, res) => {
  const body = req.body as Partial<MenuItem>;

  if (!body.name || !body.category || body.price === undefined || !body.unit || !body.soldBy) {
    return res.status(400).json({ error: 'Missing required menu fields' });
  }

  const id = body.id || body.name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/(^-|-$)/g, '');

  if (menu.some((m) => m.id === id)) {
    return res.status(400).json({ error: 'Menu item already exists' });
  }

  const item: MenuItem = {
    id,
    name: body.name,
    category: body.category,
    price: Number(body.price),
    unit: body.unit,
    soldBy: body.soldBy,
    isActive: body.isActive ?? true,
  };

  menu = [item, ...menu];
  res.status(201).json(item);
});

// ----- Customers -----

// Returns every customer with computed order stats merged in, for the table view.
app.get('/api/customers', (req, res) => {
  const enriched = customers.map((c) => ({
    ...c,
    ...computeCustomerStats(c.customerId),
  }));
  res.json(enriched);
});

app.get('/api/customers/:id/orders', (req, res) => {
  const { id } = req.params;
  const customerOrders = orders
    .filter((o) => o.customerId === id)
    .sort((a, b) => b.createdAt - a.createdAt);
  res.json(customerOrders);
});

app.post('/api/customers', (req, res) => {
  const body = req.body as Omit<Customer, 'customerId'>;
  if (!body.firstName || !body.lastName) {
    return res.status(400).json({ error: 'First and last name are required' });
  }
  const newCustomer: Customer = {
    customerId: `cust-new-${customerCounter++}`,
    firstName: body.firstName,
    lastName: body.lastName,
    phone: body.phone ?? '',
    email: body.email ?? '',
    loyaltyPoints: body.loyaltyPoints ?? 0,
    dietaryNotes: body.dietaryNotes ?? '',
    address: body.address ?? { street: '', city: '', state: 'NJ', zip: '' },
  };
  customers = [newCustomer, ...customers];
  res.status(201).json({ ...newCustomer, ...computeCustomerStats(newCustomer.customerId) });
});

app.patch('/api/customers/:id', (req, res) => {
  const { id } = req.params;
  const customer = customers.find((c) => c.customerId === id);
  if (!customer) {
    return res.status(404).json({ error: 'Customer not found' });
  }
  const body = req.body as Partial<Customer>;
  Object.assign(customer, body, { customerId: customer.customerId });
  res.json({ ...customer, ...computeCustomerStats(customer.customerId) });
});

// Key insights — most loyal, highest spend, customers who haven't ordered in a while.
app.get('/api/customers/insights', (req, res) => {
  const now = Date.now();
  const sixtyDaysAgo = now - 60 * 24 * 60 * 60 * 1000;

  const withStats = customers.map((c) => ({
    ...c,
    ...computeCustomerStats(c.customerId),
  }));

  const mostLoyal = [...withStats]
    .sort((a, b) => b.loyaltyPoints - a.loyaltyPoints)
    .slice(0, 5);

  const highestSpend = [...withStats]
    .sort((a, b) => b.totalSpent - a.totalSpent)
    .slice(0, 5);

  const needReconnect = withStats
    .filter((c) => c.lastOrderAt === null || c.lastOrderAt < sixtyDaysAgo)
    .sort((a, b) => (a.lastOrderAt ?? 0) - (b.lastOrderAt ?? 0))
    .slice(0, 5);

  res.json({ mostLoyal, highestSpend, needReconnect });
});

// ----- Stats / Dashboard -----

app.get('/api/stats', (req, res) => {
  const now = Date.now();
  const startOfToday = new Date();
  startOfToday.setHours(0, 0, 0, 0);
  const thirtyDaysAgo = now - 30 * 24 * 60 * 60 * 1000;

  const revenueOrders = orders.filter((o) => o.status !== 'cancelled');
  const last30Days = revenueOrders.filter((o) => o.createdAt >= thirtyDaysAgo);
  const todayOrders = revenueOrders.filter((o) => o.createdAt >= startOfToday.getTime());

  // At-a-glance
  const todayRevenue = Math.round(todayOrders.reduce((s, o) => s + o.total, 0) * 100) / 100;
  const todayOrderCount = todayOrders.length;
  const avgTicket = todayOrderCount > 0 ? Math.round((todayRevenue / todayOrderCount) * 100) / 100 : 0;
  const activeOrders = orders.filter((o) => o.status === 'placed' || o.status === 'in_prep' || o.status === 'ready').length;

  // Top-selling items (last 30 days), by revenue and by quantity
  const itemStats = new Map<string, { name: string; revenue: number; unitsSold: number }>();
  for (const o of last30Days) {
    for (const item of o.items) {
      const existing = itemStats.get(item.menuItemId) ?? { name: item.name, revenue: 0, unitsSold: 0 };
      existing.revenue += item.lineTotal;
      existing.unitsSold += item.quantity;
      itemStats.set(item.menuItemId, existing);
    }
  }
  const topByRevenue = Array.from(itemStats.values())
    .sort((a, b) => b.revenue - a.revenue)
    .slice(0, 8)
    .map((i) => ({ ...i, revenue: Math.round(i.revenue * 100) / 100 }));
  const topByUnits = Array.from(itemStats.values())
    .sort((a, b) => b.unitsSold - a.unitsSold)
    .slice(0, 8)
    .map((i) => ({ ...i, unitsSold: Math.round(i.unitsSold * 10) / 10 }));

  // Revenue by day (last 30 days)
  const revenueByDayMap = new Map<string, number>();
  for (const o of last30Days) {
    const day = new Date(o.createdAt).toISOString().slice(0, 10);
    revenueByDayMap.set(day, (revenueByDayMap.get(day) ?? 0) + o.total);
  }
  const revenueByDay = Array.from(revenueByDayMap.entries())
    .map(([date, revenue]) => ({ date, revenue: Math.round(revenue * 100) / 100 }))
    .sort((a, b) => (a.date < b.date ? -1 : 1));

  // Revenue by category (last 30 days)
  const categoryMap = new Map<string, number>();
  for (const o of last30Days) {
    for (const item of o.items) {
      const category = menuCategoryById.get(item.menuItemId) ?? 'Other';
      categoryMap.set(category, (categoryMap.get(category) ?? 0) + item.lineTotal);
    }
  }
  const revenueByCategory = Array.from(categoryMap.entries())
    .map(([category, revenue]) => ({ category, revenue: Math.round(revenue * 100) / 100 }))
    .sort((a, b) => b.revenue - a.revenue);

  // Revenue by category, per day — for the category trend line chart.
  // Only the top categories by total revenue are tracked individually to keep the chart readable.
  const topCategoryNames = revenueByCategory.slice(0, 4).map((c) => c.category);
  const categoryByDayMap = new Map<string, Record<string, number>>();
  for (const o of last30Days) {
    const day = new Date(o.createdAt).toISOString().slice(0, 10);
    if (!categoryByDayMap.has(day)) categoryByDayMap.set(day, {});
    const dayBucket = categoryByDayMap.get(day)!;
    for (const item of o.items) {
      const category = menuCategoryById.get(item.menuItemId) ?? 'Other';
      if (!topCategoryNames.includes(category)) continue;
      dayBucket[category] = (dayBucket[category] ?? 0) + item.lineTotal;
    }
  }
  const categoryTrend = Array.from(categoryByDayMap.entries())
    .map(([date, categories]) => {
      const row: Record<string, number | string> = { date };
      for (const cat of topCategoryNames) {
        row[cat] = Math.round((categories[cat] ?? 0) * 100) / 100;
      }
      return row;
    })
    .sort((a, b) => ((a.date as string) < (b.date as string) ? -1 : 1));

  // Busiest hours (last 30 days), 0-23
  const hourCounts = new Array(24).fill(0);
  for (const o of last30Days) {
    const hour = new Date(o.createdAt).getHours();
    hourCounts[hour]++;
  }
  const busiestHours = hourCounts.map((count, hour) => ({ hour, count }));

  res.json({
    todayRevenue,
    todayOrderCount,
    avgTicket,
    activeOrders,
    topByRevenue,
    topByUnits,
    revenueByDay,
    revenueByCategory,
    categoryTrend,
    topCategoryNames,
    busiestHours,
  });
});

// ----- Demo controls -----

// Generates a handful of "active right now" orders, timed relative to
// whenever this is called — so they always look genuinely fresh, never stale.
function generateFreshLiveOrders(): Order[] {
  const customersWithData = customers.slice(0, 8);
  const sampleItems: OrderLineItem[] = [
    { menuItemId: 'beef-brisket-first-cut', name: 'First Cut Beef Brisket', quantity: 3.2, unitPrice: 15.49, lineTotal: 49.57 },
    { menuItemId: 'poultry-chicken-cutlets-family-pack', name: 'Chicken Cutlets (Family Pack)', quantity: 2.4, unitPrice: 7.99, lineTotal: 19.18 },
    { menuItemId: 'beef-pepper-steak-fajita', name: 'Premium Pepper Steak (Fajita Strips)', quantity: 1.8, unitPrice: 13.99, lineTotal: 25.18 },
    { menuItemId: 'deli-sweet-noodle-kugel-24oz', name: 'Sweet Noodle Kugel (24 oz)', quantity: 2, unitPrice: 10.99, lineTotal: 21.98 },
    { menuItemId: 'lamb-shoulder-chops', name: "Tevya's Ranch Shoulder Lamb Chops", quantity: 1.3, unitPrice: 17.99, lineTotal: 23.39 },
  ];

  const specs: { status: Order['status']; claimedBy: string | null; minutesAgo: number }[] = [
    { status: 'placed', claimedBy: null, minutesAgo: 2 },
    { status: 'placed', claimedBy: null, minutesAgo: 8 },
    { status: 'in_prep', claimedBy: 'Mike', minutesAgo: 12 },
    { status: 'in_prep', claimedBy: 'Dani', minutesAgo: 18 },
    { status: 'ready', claimedBy: 'Sam', minutesAgo: 25 },
  ];

  const now = Date.now();
  return specs.map((spec, i) => {
    const customer = Math.random() < 0.6 ? customersWithData[i % customersWithData.length] : null;
    const items = [sampleItems[i % sampleItems.length]];
    const total = Math.round(items.reduce((s, it) => s + it.lineTotal, 0) * 100) / 100;
    return {
      id: `live-${i + 1}`,
      customerId: customer?.customerId ?? null,
      customerName: customer ? `${customer.firstName} ${customer.lastName}` : 'Walk-in',
      items,
      total,
      status: spec.status,
      source: 'counter',
      fulfillment: 'in_store',
      claimedBy: spec.claimedBy,
      createdAt: now - spec.minutesAgo * 60 * 1000,
    };
  });
}

// Wipes all orders entirely. Menu and customers stay as-is.
app.post('/api/demo/empty', (req, res) => {
  orders = [];
  res.json({ ok: true, message: 'All orders cleared' });
});

// Restores the curated demo dataset — historical completed orders, plus a
// handful of freshly-timed live orders so the stream never looks stale.
app.post('/api/demo/seed', (req, res) => {
  orders = [...HISTORICAL_ORDERS, ...generateFreshLiveOrders()];
  res.json({ ok: true, message: 'Demo data loaded' });
});

const PORT = process.env.PORT ? parseInt(process.env.PORT, 10) : 3001;
app.listen(PORT, () => {
  console.log(`Server running on http://localhost:${PORT}`);
  console.log(`Starting empty. ${HISTORICAL_ORDERS.length} historical orders available via the Data menu.`);
});
```

================================================================================

--- BATCH 8 | frontend/package.json ---
STATUS: ✅ REVIEWABLE (35 lines)
FILE: frontend/package.json
```json
{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "lint": "eslint .",
    "preview": "vite preview"
  },
  "dependencies": {
    "@groundup/shared-types": "*",
    "lucide-react": "^1.21.0",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "react-router-dom": "^7.18.0",
    "recharts": "^3.9.0"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/node": "^24.13.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "^10.5.0",
    "eslint-plugin-react-hooks": "^7.1.1",
    "eslint-plugin-react-refresh": "^0.5.3",
    "globals": "^17.6.0",
    "typescript": "~6.0.2",
    "typescript-eslint": "^8.61.0",
    "vite": "^8.1.0"
  }
}
```

================================================================================

--- BATCH 9 | frontend/tsconfig.json ---
STATUS: ✅ REVIEWABLE (8 lines)
FILE: frontend/tsconfig.json
```json
{
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
```

================================================================================

--- BATCH 10 | frontend/tsconfig.app.json ---
STATUS: ✅ REVIEWABLE (26 lines)
FILE: frontend/tsconfig.app.json
```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "types": ["vite/client"],
    "skipLibCheck": true,

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

================================================================================

--- BATCH 11 | frontend/tsconfig.node.json ---
STATUS: ✅ REVIEWABLE (24 lines)
FILE: frontend/tsconfig.node.json
```json
{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.node.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023"],
    "types": ["node"],
    "skipLibCheck": true,

    /* Bundler mode */
    "module": "nodenext",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,

    /* Linting */
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["vite.config.ts"]
}
```

================================================================================

--- BATCH 12 | frontend/vite.config.ts ---
STATUS: ✅ REVIEWABLE (8 lines)
FILE: frontend/vite.config.ts
```ts
import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// https://vite.dev/config/
export default defineConfig({
  plugins: [react()],
})
```

================================================================================

--- BATCH 13 | frontend/index.html ---
STATUS: ✅ REVIEWABLE (14 lines)
FILE: frontend/index.html
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>groundup</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

================================================================================

--- BATCH 14 | frontend/src/main.tsx ---
STATUS: ✅ REVIEWABLE (11 lines)
FILE: frontend/src/main.tsx
```tsx
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import './index.css'
import App from './app/App.tsx'

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

================================================================================

--- BATCH 15 | frontend/src/app/App.tsx ---
STATUS: ✅ REVIEWABLE (46 lines)
FILE: frontend/src/app/App.tsx
```tsx
import { useState, useEffect } from 'react';
import { BrowserRouter, Routes, Route } from 'react-router-dom';
import TopNav from '../components/nav/TopNav';
import MainPage from '../pages/CounterPage';
import OnlineOrder from '../pages/OnlineOrderPage';
import Dashboard from '../pages/DashboardPage';
import CustomersPage from '../pages/CustomersPage';
import MenuPage from '../pages/MenuPage';
import Toast from '../components/ui/Toast';
import { MenuProvider } from '../providers/MenuProvider';
import './App.shell.css';

function App() {
  const [toastMessage, setToastMessage] = useState<string | null>(null);

  useEffect(() => {
    const pending = sessionStorage.getItem('mkb-toast');
    if (pending) {
      setToastMessage(pending);
      sessionStorage.removeItem('mkb-toast');
    }
  }, []);

  return (
    <BrowserRouter>
      <MenuProvider>
        <div className="app-shell">
          <TopNav />
          <div className="app-shell-body">
            <Routes>
              <Route path="/" element={<MainPage />} />
              <Route path="/online" element={<OnlineOrder />} />
              <Route path="/menu" element={<MenuPage />} />
              <Route path="/dashboard" element={<Dashboard />} />
              <Route path="/customers" element={<CustomersPage />} />
            </Routes>
          </div>
          {toastMessage && <Toast message={toastMessage} onDismiss={() => setToastMessage(null)} />}
        </div>
      </MenuProvider>
    </BrowserRouter>
  );
}

export default App;
```

================================================================================

--- BATCH 16 | frontend/src/config.ts ---
STATUS: ✅ REVIEWABLE (10 lines)
FILE: frontend/src/config.ts
```ts
export const STORE_CONFIG = {
  name: 'Maple Kosher Meats',
  staff: ['Mike', 'Dani', 'Sam'],
  apiBaseUrl: import.meta.env.VITE_API_BASE_URL || 'http://localhost:3001/api',
  orderStreamPollIntervalMs: 3000,
  saturdayClosed: true,
};

export type StaffName = (typeof STORE_CONFIG.staff)[number];
```

================================================================================

--- BATCH 17 | frontend/src/index.css ---
STATUS: ✅ REVIEWABLE (52 lines)
FILE: frontend/src/index.css
```css
@import url('https://fonts.googleapis.com/css2?family=Oswald:wght@400;500;600;700&family=Inter:wght@400;500;600;700;800&display=swap');

:root {
  --ink: #1c1410;
  --paper: #faf6ee;
  --paper-dim: #f0e9da;
  --rare: #a23b2e;
  --sage: #5c6b47;
  --ochre: #d4a24c;
  --line: rgba(28, 20, 16, 0.12);
  --shadow: rgba(28, 20, 16, 0.18);

  --font-display: 'Oswald', sans-serif;
  --font-body: 'Inter', sans-serif;

  font-family: var(--font-body);
  color: var(--ink);
}

* {
  box-sizing: border-box;
}

html, body, #root {
  margin: 0;
  padding: 0;
  height: 100%;
  background: var(--paper-dim);
}

body {
  -webkit-font-smoothing: antialiased;
}

button {
  font-family: inherit;
  cursor: pointer;
}

button:focus-visible,
[tabindex]:focus-visible {
  outline: 3px solid var(--rare);
  outline-offset: 2px;
}

@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.001ms !important;
    transition-duration: 0.001ms !important;
  }
}
```

================================================================================

--- BATCH 18 | frontend/eslint.config.js ---
STATUS: ✅ REVIEWABLE (23 lines)
FILE: frontend/eslint.config.js
```javascript
import js from '@eslint/js'
import globals from 'globals'
import reactHooks from 'eslint-plugin-react-hooks'
import reactRefresh from 'eslint-plugin-react-refresh'
import tseslint from 'typescript-eslint'
import { defineConfig, globalIgnores } from 'eslint/config'

export default defineConfig([
  globalIgnores(['dist']),
  {
    files: ['**/*.{ts,tsx}'],
    extends: [
      js.configs.recommended,
      tseslint.configs.recommended,
      reactHooks.configs.flat.recommended,
      reactRefresh.configs.vite,
    ],
    languageOptions: {
      globals: globals.browser,
    },
  },
])
```

================================================================================

--- BATCH 19 | frontend/src/App.layout.css ---
STATUS: ✅ REVIEWABLE (38 lines)
FILE: frontend/src/App.layout.css
```css
.mkb-app {
  display: flex;
  height: 100%;
  width: 100%;
  overflow: hidden;
  background: var(--paper-dim, #f0e9da);
}

.mkb-counter-pane {
  width: 42%;
  min-width: 380px;
  height: 100%;
  border-right: 2px solid var(--ink, #1c1410);
  background: var(--paper, #faf6ee);
}

.mkb-stream-pane {
  flex: 1;
  height: 100%;
  overflow: hidden;
}

@media (max-width: 900px) {
  .mkb-app {
    flex-direction: column;
  }
  .mkb-counter-pane {
    width: 100%;
    min-width: 0;
    height: 55%;
    border-right: none;
    border-bottom: 2px solid var(--ink, #1c1410);
  }
  .mkb-stream-pane {
    height: 45%;
  }
}
```

================================================================================

--- BATCH 20 | frontend/src/api/customers.ts ---
STATUS: ✅ REVIEWABLE (54 lines)
FILE: frontend/src/api/customers.ts
```ts
import type { CustomerWithStats, Customer, Order } from '@groundup/shared-types';
import { STORE_CONFIG } from '../config';

const BASE = STORE_CONFIG.apiBaseUrl;

export async function fetchCustomersWithStats(): Promise<CustomerWithStats[]> {
  const res = await fetch(`${BASE}/customers`);
  if (!res.ok) throw new Error('Failed to fetch customers');
  return res.json();
}

export async function fetchCustomerOrderHistory(customerId: string): Promise<Order[]> {
  const res = await fetch(`${BASE}/customers/${customerId}/orders`);
  if (!res.ok) throw new Error('Failed to fetch customer order history');
  return res.json();
}

export async function createCustomer(
  data: Omit<Customer, 'customerId'>
): Promise<CustomerWithStats> {
  const res = await fetch(`${BASE}/customers`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });
  if (!res.ok) throw new Error('Failed to create customer');
  return res.json();
}

export async function updateCustomer(
  customerId: string,
  data: Partial<Customer>
): Promise<CustomerWithStats> {
  const res = await fetch(`${BASE}/customers/${customerId}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });
  if (!res.ok) throw new Error('Failed to update customer');
  return res.json();
}

export type CustomerInsights = {
  mostLoyal: CustomerWithStats[];
  highestSpend: CustomerWithStats[];
  needReconnect: CustomerWithStats[];
};

export async function fetchCustomerInsights(): Promise<CustomerInsights> {
  const res = await fetch(`${BASE}/customers/insights`);
  if (!res.ok) throw new Error('Failed to fetch customer insights');
  return res.json();
}
```

================================================================================

--- BATCH 21 | frontend/src/api/demo.ts ---
STATUS: ✅ REVIEWABLE (12 lines)
FILE: frontend/src/api/demo.ts
```ts
const BASE = 'http://localhost:3001/api';

export async function resetToEmpty(): Promise<void> {
  const res = await fetch(`${BASE}/demo/empty`, { method: 'POST' });
  if (!res.ok) throw new Error('Failed to clear data');
}

export async function resetToDemoData(): Promise<void> {
  const res = await fetch(`${BASE}/demo/seed`, { method: 'POST' });
  if (!res.ok) throw new Error('Failed to load demo data');
}
```

================================================================================

--- BATCH 22 | frontend/src/api/menu.ts ---
STATUS: ✅ REVIEWABLE (42 lines)
FILE: frontend/src/api/menu.ts
```ts
import type { MenuItem } from '@groundup/shared-types';
import { STORE_CONFIG } from '../config';

const BASE = STORE_CONFIG.apiBaseUrl;

export type NewMenuItemInput = Omit<MenuItem, 'id'> & { id?: string };

export async function fetchMenu(): Promise<MenuItem[]> {
  const res = await fetch(`${BASE}/menu`);
  if (!res.ok) throw new Error('Failed to fetch menu');
  return res.json();
}

export async function fetchAdminMenu(): Promise<MenuItem[]> {
  const res = await fetch(`${BASE}/menu/admin`);
  if (!res.ok) throw new Error('Failed to fetch admin menu');
  return res.json();
}

export async function updateMenuItem(
  id: string,
  changes: Partial<MenuItem>
): Promise<MenuItem> {
  const res = await fetch(`${BASE}/menu/${id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(changes),
  });
  if (!res.ok) throw new Error('Failed to update menu item');
  return res.json();
}

export async function createMenuItem(data: NewMenuItemInput): Promise<MenuItem> {
  const res = await fetch(`${BASE}/menu`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(data),
  });
  if (!res.ok) throw new Error('Failed to create menu item');
  return res.json();
}
```

================================================================================

--- BATCH 23 | frontend/src/api/online-orders.ts ---
STATUS: ✅ REVIEWABLE (30 lines)
FILE: frontend/src/api/online-orders.ts
```ts
import type { Order, OrderLineItem, Address } from '@groundup/shared-types';
import { STORE_CONFIG } from '../config';

const BASE = STORE_CONFIG.apiBaseUrl;

export async function createOnlineOrder(params: {
  customerName: string;
  customerId: string | null;
  items: OrderLineItem[];
  fulfillment: 'pickup' | 'delivery';
  deliveryAddress?: Address;
  notes?: string;
}): Promise<Order> {
  const res = await fetch(`${BASE}/orders`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      customerName: params.customerName,
      customerId: params.customerId,
      items: params.items,
      source: 'online',
      fulfillment: params.fulfillment,
      deliveryAddress: params.deliveryAddress,
      notes: params.notes,
    }),
  });
  if (!res.ok) throw new Error('Failed to place order');
  return res.json();
}
```

================================================================================

--- BATCH 24 | frontend/src/api/orders.ts ---
STATUS: ✅ REVIEWABLE (67 lines)
FILE: frontend/src/api/orders.ts
```ts
import type { Order, OrderLineItem, Customer, FulfillmentType } from '@groundup/shared-types';
import { STORE_CONFIG } from '../config';

const BASE = STORE_CONFIG.apiBaseUrl;

export async function fetchOrders(): Promise<Order[]> {
  const res = await fetch(`${BASE}/orders`);
  if (!res.ok) throw new Error('Failed to fetch orders');
  return res.json();
}

export async function createOrder(
  customerName: string,
  items: OrderLineItem[],
  customerId?: string | null,
  fulfillment?: FulfillmentType,
  notes?: string
): Promise<Order> {
  const res = await fetch(`${BASE}/orders`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
      customerName,
      items,
      customerId: customerId ?? null,
      source: 'counter',
      fulfillment: fulfillment ?? 'in_store',
      notes,
    }),
  });
  if (!res.ok) throw new Error('Failed to create order');
  return res.json();
}

export async function updateOrder(
  id: string,
  changes: { status?: Order['status']; claimedBy?: string }
): Promise<Order> {
  const res = await fetch(`${BASE}/orders/${id}`, {
    method: 'PATCH',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(changes),
  });
  if (!res.ok) throw new Error('Failed to update order');
  return res.json();
}

export async function deleteOrder(id: string): Promise<void> {
  const res = await fetch(`${BASE}/orders/${id}`, { method: 'DELETE' });
  if (!res.ok) {
    const body = await res.json().catch(() => ({}));
    throw new Error(body.error || 'Failed to delete order');
  }
}

export async function fetchCustomers(): Promise<Customer[]> {
  const res = await fetch(`${BASE}/customers`);
  if (!res.ok) throw new Error('Failed to fetch customers');
  return res.json();
}

export async function fetchCustomerOrders(customerId: string): Promise<Order[]> {
  const res = await fetch(`${BASE}/customers/${customerId}/orders`);
  if (!res.ok) throw new Error('Failed to fetch customer order history');
  return res.json();
}
```

================================================================================

--- BATCH 25 | frontend/src/api/stats.ts ---
STATUS: ✅ REVIEWABLE (28 lines)
FILE: frontend/src/api/stats.ts
```ts
const BASE = 'http://localhost:3001/api';

export type ItemStat = { name: string; revenue: number; unitsSold: number };
export type DayRevenue = { date: string; revenue: number };
export type CategoryRevenue = { category: string; revenue: number };
export type HourCount = { hour: number; count: number };
export type CategoryTrendRow = { date: string } & Record<string, number | string>;

export type Stats = {
  todayRevenue: number;
  todayOrderCount: number;
  avgTicket: number;
  activeOrders: number;
  topByRevenue: ItemStat[];
  topByUnits: ItemStat[];
  revenueByDay: DayRevenue[];
  revenueByCategory: CategoryRevenue[];
  categoryTrend: CategoryTrendRow[];
  topCategoryNames: string[];
  busiestHours: HourCount[];
};

export async function fetchStats(): Promise<Stats> {
  const res = await fetch(`${BASE}/stats`);
  if (!res.ok) throw new Error('Failed to fetch stats');
  return res.json();
}
```

================================================================================

--- BATCH 26 | frontend/src/app/App.shell.css ---
STATUS: ✅ REVIEWABLE (13 lines)
FILE: frontend/src/app/App.shell.css
```css
.app-shell {
  display: flex;
  flex-direction: column;
  height: 100vh;
  width: 100vw;
  overflow: hidden;
}

.app-shell-body {
  flex: 1;
  overflow: hidden;
}
```

================================================================================

--- BATCH 27 | frontend/src/components/OnlineOrderPlaceholder.css ---
STATUS: ✅ REVIEWABLE (23 lines)
FILE: frontend/src/components/OnlineOrderPlaceholder.css
```css
.online-placeholder {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  color: rgba(28, 20, 16, 0.4);
}

.online-placeholder h1 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 22px;
  color: rgba(28, 20, 16, 0.6);
  margin: 0;
}

.online-placeholder p {
  font-size: 14px;
  margin: 0;
}
```

================================================================================

--- BATCH 28 | frontend/src/components/OnlineOrderPlaceholder.tsx ---
STATUS: ✅ REVIEWABLE (13 lines)
FILE: frontend/src/components/OnlineOrderPlaceholder.tsx
```tsx
import { Globe } from 'lucide-react';
import './OnlineOrderPlaceholder.css';

export default function OnlineOrderPlaceholder() {
  return (
    <div className="online-placeholder">
      <Globe size={40} strokeWidth={1.5} />
      <h1>Online Ordering</h1>
      <p>Customer-facing ordering isn't built yet — this is where it'll live.</p>
    </div>
  );
}
```

================================================================================

--- BATCH 29 | frontend/src/components/customers/CustomerEditModal.css ---
STATUS: ✅ REVIEWABLE (90 lines)
FILE: frontend/src/components/customers/CustomerEditModal.css
```css
.edit-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 70;
}

.edit-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 24px 28px 28px;
  width: 420px;
  box-shadow: 0 12px 32px var(--shadow);
  display: flex;
  flex-direction: column;
  gap: 10px;
}

.edit-modal-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 6px;
}

.edit-modal-head h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 20px;
  margin: 0;
}

.edit-modal input {
  width: 100%;
  font-size: 14px;
  padding: 10px 12px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  background: var(--paper);
  outline: none;
  font-family: var(--font-body);
}

.edit-modal input:focus {
  border-color: var(--rare);
}

.edit-modal-row {
  display: flex;
  gap: 10px;
}

.edit-modal-row input {
  flex: 1;
}

.edit-modal-actions {
  display: flex;
  gap: 10px;
  margin-top: 10px;
}

.edit-modal-actions .btn-secondary,
.edit-modal-actions .btn-primary {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  font-weight: 700;
  font-size: 13px;
}

.edit-modal-actions .btn-secondary {
  border: 1.5px solid var(--ink);
  background: transparent;
}

.edit-modal-actions .btn-primary {
  border: none;
  background: var(--ink);
  color: var(--paper);
}

.edit-modal-actions .btn-primary:disabled {
  background: var(--line);
  color: rgba(28, 20, 16, 0.4);
}
```

================================================================================

--- BATCH 30 | frontend/src/components/customers/CustomerEditModal.tsx ---
STATUS: ✅ REVIEWABLE (127 lines)
FILE: frontend/src/components/customers/CustomerEditModal.tsx
```tsx
import { useState } from 'react';
import { X } from 'lucide-react';
import type { CustomerWithStats, Address } from '@groundup/shared-types';
import './CustomerEditModal.css';

type Props = {
  customer: CustomerWithStats | null;
  onClose: () => void;
  onSave: (data: {
    firstName: string;
    lastName: string;
    phone: string;
    email: string;
    loyaltyPoints: number;
    dietaryNotes: string;
    address: Address;
  }) => Promise<void>;
};

export default function CustomerEditModal({ customer, onClose, onSave }: Props) {
  const [firstName, setFirstName] = useState(customer?.firstName ?? '');
  const [lastName, setLastName] = useState(customer?.lastName ?? '');
  const [phone, setPhone] = useState(customer?.phone ?? '');
  const [email, setEmail] = useState(customer?.email ?? '');
  const [loyaltyPoints, setLoyaltyPoints] = useState(String(customer?.loyaltyPoints ?? 0));
  const [dietaryNotes, setDietaryNotes] = useState(customer?.dietaryNotes ?? '');
  const [street, setStreet] = useState(customer?.address?.street ?? '');
  const [city, setCity] = useState(customer?.address?.city ?? '');
  const [zip, setZip] = useState(customer?.address?.zip ?? '');
  const [saving, setSaving] = useState(false);

  const isEdit = customer !== null;

  const handleSave = async () => {
    if (!firstName.trim() || !lastName.trim()) return;
    setSaving(true);
    try {
      await onSave({
        firstName: firstName.trim(),
        lastName: lastName.trim(),
        phone: phone.trim(),
        email: email.trim(),
        loyaltyPoints: parseInt(loyaltyPoints, 10) || 0,
        dietaryNotes: dietaryNotes.trim(),
        address: { street: street.trim(), city: city.trim(), state: 'NJ', zip: zip.trim() },
      });
    } finally {
      setSaving(false);
    }
  };

  return (
    <div className="edit-modal-backdrop" onClick={onClose}>
      <div className="edit-modal" onClick={(e) => e.stopPropagation()}>
        <div className="edit-modal-head">
          <h2>{isEdit ? 'Edit customer' : 'Add customer'}</h2>
          <button className="modal-close-btn" onClick={onClose}>
            <X size={16} strokeWidth={2} />
          </button>
        </div>

        <div className="edit-modal-row">
          <input
            placeholder="First name"
            value={firstName}
            onChange={(e) => setFirstName(e.target.value)}
          />
          <input
            placeholder="Last name"
            value={lastName}
            onChange={(e) => setLastName(e.target.value)}
          />
        </div>

        <input
          placeholder="Email"
          type="email"
          value={email}
          onChange={(e) => setEmail(e.target.value)}
        />
        <input
          placeholder="Phone"
          value={phone}
          onChange={(e) => setPhone(e.target.value)}
        />

        <div className="edit-modal-row">
          <input
            placeholder="Loyalty points"
            type="number"
            value={loyaltyPoints}
            onChange={(e) => setLoyaltyPoints(e.target.value)}
          />
          <input
            placeholder="Dietary notes"
            value={dietaryNotes}
            onChange={(e) => setDietaryNotes(e.target.value)}
          />
        </div>

        <input
          placeholder="Street address"
          value={street}
          onChange={(e) => setStreet(e.target.value)}
        />
        <div className="edit-modal-row">
          <input placeholder="City" value={city} onChange={(e) => setCity(e.target.value)} />
          <input placeholder="Zip" value={zip} onChange={(e) => setZip(e.target.value)} />
        </div>

        <div className="edit-modal-actions">
          <button className="btn-secondary" onClick={onClose}>
            Cancel
          </button>
          <button
            className="btn-primary"
            disabled={saving || !firstName.trim() || !lastName.trim()}
            onClick={handleSave}
          >
            {saving ? 'Saving…' : isEdit ? 'Save changes' : 'Add customer'}
          </button>
        </div>
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 31 | frontend/src/components/customers/CustomerInsightsModal.css ---
STATUS: ✅ REVIEWABLE (123 lines)
FILE: frontend/src/components/customers/CustomerInsightsModal.css
```css
.insights-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.5);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 70;
}

.insights-modal {
  background: var(--paper);
  border-radius: 16px;
  padding: 28px 32px 32px;
  width: 720px;
  max-width: 92vw;
  max-height: 80vh;
  overflow-y: auto;
  box-shadow: 0 20px 50px rgba(0, 0, 0, 0.35);
}

.insights-modal-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 4px;
}

.insights-modal-head h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 24px;
  letter-spacing: 0.02em;
  margin: 0;
}

.insights-loading {
  padding: 40px;
  text-align: center;
  color: rgba(28, 20, 16, 0.5);
  font-size: 13px;
}

.insights-panels {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
  margin-top: 18px;
}

.insights-panel {
  background: var(--paper-dim);
  border-radius: 12px;
  padding: 16px;
  border: 1px solid var(--line);
}

.insights-panel-title {
  display: flex;
  align-items: center;
  gap: 7px;
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 12px;
  letter-spacing: 0.04em;
  font-weight: 600;
  color: var(--rare);
  margin-bottom: 12px;
  padding-bottom: 10px;
  border-bottom: 1.5px solid var(--ink);
}

.insights-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 9px;
}

.insights-list li {
  display: flex;
  align-items: baseline;
  gap: 8px;
  font-size: 12px;
}

.insights-rank {
  width: 14px;
  flex-shrink: 0;
  font-weight: 700;
  color: rgba(28, 20, 16, 0.35);
  font-size: 11px;
}

.insights-name {
  flex: 1;
  font-weight: 600;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.insights-value {
  font-weight: 700;
  color: var(--ink);
  font-size: 11px;
  white-space: nowrap;
}

.insights-empty {
  font-size: 12px;
  font-style: italic;
  color: rgba(28, 20, 16, 0.45);
}

@media (max-width: 640px) {
  .insights-panels {
    grid-template-columns: 1fr;
  }
}
```

================================================================================

--- BATCH 32 | frontend/src/components/customers/CustomerInsightsModal.tsx ---
STATUS: ✅ REVIEWABLE (96 lines)
FILE: frontend/src/components/customers/CustomerInsightsModal.tsx
```tsx
import { useState, useEffect } from 'react';
import { X, Crown, TrendingUp, UserMinus } from 'lucide-react';
import { fetchCustomerInsights, type CustomerInsights } from '../../api/customers';
import './CustomerInsightsModal.css';

type Props = {
  onClose: () => void;
};

function formatDate(ts: number | null): string {
  if (!ts) return 'Never ordered';
  return new Date(ts).toLocaleDateString();
}

export default function CustomerInsightsModal({ onClose }: Props) {
  const [insights, setInsights] = useState<CustomerInsights | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchCustomerInsights()
      .then(setInsights)
      .catch(console.error)
      .finally(() => setLoading(false));
  }, []);

  return (
    <div className="insights-modal-backdrop" onClick={onClose}>
      <div className="insights-modal" onClick={(e) => e.stopPropagation()}>
        <div className="insights-modal-head">
          <h2>Customer insights</h2>
          <button className="modal-close-btn" onClick={onClose}>
            <X size={16} strokeWidth={2} />
          </button>
        </div>

        {loading && <p className="insights-loading">Loading…</p>}

        {insights && (
          <div className="insights-panels">
            <section className="insights-panel">
              <div className="insights-panel-title">
                <Crown size={15} strokeWidth={2} />
                Most loyal
              </div>
              <ol className="insights-list">
                {insights.mostLoyal.map((c, i) => (
                  <li key={c.customerId}>
                    <span className="insights-rank">{i + 1}</span>
                    <span className="insights-name">{c.firstName} {c.lastName}</span>
                    <span className="insights-value">{c.loyaltyPoints} pts</span>
                  </li>
                ))}
              </ol>
            </section>

            <section className="insights-panel">
              <div className="insights-panel-title">
                <TrendingUp size={15} strokeWidth={2} />
                Highest spend
              </div>
              <ol className="insights-list">
                {insights.highestSpend.map((c, i) => (
                  <li key={c.customerId}>
                    <span className="insights-rank">{i + 1}</span>
                    <span className="insights-name">{c.firstName} {c.lastName}</span>
                    <span className="insights-value">${c.totalSpent.toFixed(2)}</span>
                  </li>
                ))}
              </ol>
            </section>

            <section className="insights-panel">
              <div className="insights-panel-title">
                <UserMinus size={15} strokeWidth={2} />
                Need reconnect
              </div>
              <ol className="insights-list">
                {insights.needReconnect.length === 0 && (
                  <p className="insights-empty">Everyone's been in recently.</p>
                )}
                {insights.needReconnect.map((c, i) => (
                  <li key={c.customerId}>
                    <span className="insights-rank">{i + 1}</span>
                    <span className="insights-name">{c.firstName} {c.lastName}</span>
                    <span className="insights-value">{formatDate(c.lastOrderAt)}</span>
                  </li>
                ))}
              </ol>
            </section>
          </div>
        )}
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 33 | frontend/src/components/nav/TopNav.css ---
STATUS: ✅ REVIEWABLE (129 lines)
FILE: frontend/src/components/nav/TopNav.css
```css
.top-nav {
  display: flex;
  align-items: center;
  justify-content: space-between;
  height: 52px;
  padding: 0 20px;
  background: var(--ink);
  border-bottom: 2px solid var(--ink);
  flex-shrink: 0;
}

.top-nav-brand {
  font-family: var(--font-display);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  font-size: 15px;
  font-weight: 600;
  color: var(--paper);
}

.top-nav-links {
  display: flex;
  gap: 4px;
}

.top-nav-link {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  font-weight: 600;
  padding: 8px 14px;
  border-radius: 8px;
  color: rgba(250, 246, 238, 0.6);
  text-decoration: none;
  transition: background 0.15s, color 0.15s;
}

.top-nav-link:hover {
  color: var(--paper);
}

.top-nav-link.active {
  background: rgba(250, 246, 238, 0.12);
  color: var(--paper);
}

.admin-dropdown-wrap {
  position: relative;
}

.admin-trigger {
  cursor: pointer;
  background: transparent;
  border: 1.5px solid rgba(250, 246, 238, 0.25);
  color: rgba(250, 246, 238, 0.85);
}

.admin-trigger:hover {
  border-color: var(--rare);
  color: var(--paper);
}

.admin-dropdown-wrap .admin-trigger.active-open {
  border-color: var(--rare);
  background: rgba(162, 59, 46, 0.18);
  color: var(--paper);
}

.admin-dropdown {
  position: absolute;
  top: calc(100% + 6px);
  right: 0;
  background: var(--paper);
  border: 1.5px solid var(--line);
  border-radius: 10px;
  box-shadow: 0 10px 28px rgba(0, 0, 0, 0.25);
  overflow: hidden;
  min-width: 220px;
  z-index: 60;
}

.admin-dropdown-item {
  display: block;
  width: 100%;
  text-align: left;
  padding: 11px 14px;
  font-size: 13px;
  font-weight: 500;
  color: var(--ink);
  background: var(--paper);
  border: none;
  border-bottom: 1px solid var(--line);
}

.admin-dropdown-item:last-child {
  border-bottom: none;
}

.admin-dropdown-item:hover {
  background: var(--paper-dim);
}

.admin-dropdown-item:disabled {
  opacity: 0.5;
}

.admin-trigger.attention-ping {
  animation: admin-attention 1s ease-in-out 4;
}

@keyframes admin-attention {
  0%, 100% {
    border-color: rgba(250, 246, 238, 0.25);
    box-shadow: none;
  }
  50% {
    border-color: var(--rare);
    box-shadow: 0 0 0 3px rgba(162, 59, 46, 0.25);
  }
}

.top-nav-brand,
.top-nav-brand:hover,
.top-nav-brand:focus,
.top-nav-brand:visited {
  text-decoration: none;
}
```

================================================================================

--- BATCH 34 | frontend/src/components/nav/TopNav.tsx ---
STATUS: ✅ REVIEWABLE (67 lines)
FILE: frontend/src/components/nav/TopNav.tsx
```tsx
import { useState, useEffect } from 'react';
import { NavLink } from 'react-router-dom';
import { LayoutGrid, Globe, BarChart3, Users, Settings, ChevronDown, LayoutList } from 'lucide-react';
import { STORE_CONFIG } from '../../config';
import { resetToEmpty, resetToDemoData } from '../../api/demo';
import './TopNav.css';

export default function TopNav() {
  const [adminOpen, setAdminOpen] = useState(false);
  const [working, setWorking] = useState(false);
  const [attentionPing, setAttentionPing] = useState(true);

  useEffect(() => {
    const timer = setTimeout(() => setAttentionPing(false), 5000);
    return () => clearTimeout(timer);
  }, []);

  const handleEmpty = async () => {
    setWorking(true);
    try {
      await resetToEmpty();
      sessionStorage.setItem('mkb-toast', 'Started with no current orders');
      window.location.reload();
    } catch {
      alert('Could not clear data — is the server running?');
      setWorking(false);
    }
  };

  const handleDemoData = async () => {
    setWorking(true);
    try {
      await resetToDemoData();
      sessionStorage.setItem('mkb-toast', 'Loaded demo data');
      window.location.reload();
    } catch {
      alert('Could not load demo data — is the server running?');
      setWorking(false);
    }
  };

  return (
    <header className="top-nav">
      <NavLink to="/" className="top-nav-brand">{STORE_CONFIG.name}</NavLink>
      <nav className="top-nav-links">
        <NavLink to="/" className={({ isActive }) => `top-nav-link ${isActive ? 'active' : ''}`}><LayoutGrid size={15} />Counter / Stream</NavLink>
        <NavLink to="/online" className={({ isActive }) => `top-nav-link ${isActive ? 'active' : ''}`}><Globe size={15} />Online Order</NavLink>
        <NavLink to="/menu" className={({ isActive }) => `top-nav-link ${isActive ? 'active' : ''}`}><LayoutList size={15} />Menu</NavLink>
        <NavLink to="/dashboard" className={({ isActive }) => `top-nav-link ${isActive ? 'active' : ''}`}><BarChart3 size={15} />Dashboard</NavLink>
        <NavLink to="/customers" className={({ isActive }) => `top-nav-link ${isActive ? 'active' : ''}`}><Users size={15} />Customers</NavLink>

        <div className="admin-dropdown-wrap">
          <button className={`top-nav-link admin-trigger ${adminOpen ? 'active-open' : ''} ${attentionPing ? 'attention-ping' : ''}`} onClick={() => { setAdminOpen(v => !v); setAttentionPing(false); }} onBlur={() => setTimeout(() => setAdminOpen(false), 150)}>
            <Settings size={15} />Data<ChevronDown size={13} />
          </button>
          {adminOpen && (
            <div className="admin-dropdown">
              <button className="admin-dropdown-item" disabled={working} onMouseDown={(e) => { e.preventDefault(); handleEmpty(); }}>Start with no current orders</button>
              <button className="admin-dropdown-item" disabled={working} onMouseDown={(e) => { e.preventDefault(); handleDemoData(); }}>Load demo data</button>
            </div>
          )}
        </div>
      </nav>
    </header>
  );
}
```

================================================================================

--- BATCH 35 | frontend/src/components/orders/CounterScreen.css ---
STATUS: 🚨 VERY LARGE (502 lines)
FILE: frontend/src/components/orders/CounterScreen.css
```css
.counter-screen {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 28px 24px 0;
}

.counter-header {
  margin-bottom: 18px;
}

.counter-eyebrow {
  font-family: var(--font-body);
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--rare);
}

.counter-header h1 {
  font-family: var(--font-display);
  font-size: 32px;
  font-weight: 600;
  letter-spacing: 0.01em;
  margin: 2px 0 0;
  text-transform: uppercase;
}

.category-tabs {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 6px;
  margin-bottom: 16px;
}

.category-tab {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  font-family: var(--font-body);
  font-size: 12px;
  font-weight: 600;
  padding: 9px 8px;
  border-radius: 10px;
  border: 1.5px solid var(--line);
  background: var(--paper);
  color: var(--ink);
  transition: background 0.15s, color 0.15s, border-color 0.15s;
  white-space: nowrap;
}

.category-tab svg {
  flex-shrink: 0;
  opacity: 0.75;
}

.category-tab.active {
  background: var(--ink);
  color: var(--paper);
  border-color: var(--ink);
}

.category-tab.active svg {
  opacity: 1;
}

.item-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 10px;
  overflow-y: auto;
  flex: 1;
  padding-bottom: 16px;
}

.item-tile {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
  padding: 16px 14px;
  border: 1.5px solid var(--line);
  border-radius: 10px;
  background: var(--paper);
  text-align: left;
  transition: border-color 0.15s, transform 0.1s;
}

.item-tile:hover,
.item-tile:active {
  border-color: var(--rare);
  transform: translateY(-1px);
}

.item-name {
  font-family: var(--font-body);
  font-weight: 700;
  font-size: 15px;
}

.item-price {
  font-family: var(--font-body);
  font-size: 13px;
  color: var(--rare);
  font-weight: 600;
}

/* Amount entry modal (weight or quantity) */
.amount-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
}

.amount-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 28px;
  width: 320px;
  box-shadow: 0 12px 32px var(--shadow);
}

.amount-modal h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 22px;
  margin: 0 0 4px;
}

.amount-modal-price {
  color: var(--rare);
  font-weight: 600;
  margin: 0 0 16px;
}

.amount-modal input {
  width: 100%;
  font-size: 22px;
  font-weight: 600;
  padding: 12px 14px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  margin-bottom: 8px;
  font-family: var(--font-body);
  background: var(--paper);
  color: var(--ink);
  -moz-appearance: textfield;
  outline: none;
  transition: border-color 0.15s;
}

.amount-modal input:focus {
  border-color: var(--rare);
}

.amount-modal input::-webkit-outer-spin-button,
.amount-modal input::-webkit-inner-spin-button {
  -webkit-appearance: none;
  margin: 0;
}

.amount-modal-total {
  font-size: 18px;
  font-weight: 700;
  color: var(--sage);
  margin: 0 0 16px;
}

.amount-modal-actions {
  display: flex;
  gap: 10px;
}

.btn-secondary,
.btn-primary {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  font-weight: 700;
  font-size: 14px;
  border: 1.5px solid var(--ink);
}

.btn-secondary {
  background: transparent;
  color: var(--ink);
}

.btn-primary {
  background: var(--ink);
  color: var(--paper);
}

/* Cart panel */
.cart-panel {
  border-top: 2px solid var(--ink);
  padding: 16px 0 24px;
  background: var(--paper);
}

.customer-name-input {
  width: 100%;
  padding: 10px 12px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  font-family: var(--font-body);
  font-size: 14px;
  margin-bottom: 10px;
}

.cart-lines {
  max-height: 140px;
  overflow-y: auto;
  margin-bottom: 8px;
}

.cart-empty {
  color: rgba(28, 20, 16, 0.45);
  font-size: 13px;
  font-style: italic;
  margin: 6px 0;
}

.cart-line {
  display: flex;
  align-items: center;
  gap: 10px;
  padding: 8px 0;
  border-bottom: 1px solid var(--line);
}

.cart-line-info {
  display: flex;
  flex-direction: column;
  flex: 1;
}

.cart-line-name {
  font-weight: 600;
  font-size: 14px;
}

.cart-line-weight {
  font-size: 12px;
  color: rgba(28, 20, 16, 0.55);
}

.cart-line-total {
  font-weight: 700;
  font-size: 14px;
}

.cart-line-remove {
  background: transparent;
  border: none;
  font-size: 20px;
  color: var(--rare);
  line-height: 1;
  padding: 0 4px;
}

.cart-footer {
  display: flex;
  justify-content: space-between;
  align-items: baseline;
  padding: 10px 0;
}

.cart-total-label {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 14px;
  letter-spacing: 0.05em;
}

.cart-total-amount {
  font-size: 26px;
  font-weight: 700;
}

.send-order-btn {
  width: 100%;
  padding: 16px;
  border-radius: 10px;
  border: none;
  background: var(--rare);
  color: var(--paper);
  font-family: var(--font-display);
  text-transform: uppercase;
  letter-spacing: 0.05em;
  font-size: 16px;
  font-weight: 600;
}

.send-order-btn:disabled {
  background: var(--line);
  color: rgba(28, 20, 16, 0.4);
}

/* Customer autocomplete */
.customer-input-wrap {
  position: relative;
  margin-bottom: 10px;
}

.customer-input-wrap .customer-name-input {
  margin-bottom: 0;
  padding-right: 90px;
}

.history-pill {
  position: absolute;
  right: 6px;
  top: 6px;
  display: flex;
  align-items: center;
  gap: 4px;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
  padding: 6px 10px;
  border-radius: 999px;
  border: none;
  background: var(--ink);
  color: var(--paper);
}

.customer-suggestions {
  position: absolute;
  top: calc(100% + 4px);
  left: 0;
  right: 0;
  background: var(--paper);
  border: 1.5px solid var(--line);
  border-radius: 10px;
  box-shadow: 0 8px 24px var(--shadow);
  z-index: 40;
  overflow: hidden;
}

.customer-suggestion {
  display: block;
  width: 100%;
  text-align: left;
  padding: 10px 14px;
  font-size: 14px;
  font-weight: 500;
  background: var(--paper);
  border: none;
  border-bottom: 1px solid var(--line);
}

.customer-suggestion:last-child {
  border-bottom: none;
}

.customer-suggestion:hover {
  background: var(--paper-dim);
}

/* Customer history modal */
.history-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
}

.history-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 28px;
  width: 360px;
  max-height: 70vh;
  overflow-y: auto;
  box-shadow: 0 12px 32px var(--shadow);
}

.history-modal h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 22px;
  margin: 0 0 4px;
}

.history-modal-meta {
  color: var(--rare);
  font-weight: 600;
  font-size: 13px;
  margin: 0 0 16px;
}

.history-empty {
  color: rgba(28, 20, 16, 0.45);
  font-size: 13px;
  font-style: italic;
}

.history-order {
  padding: 10px 0;
  border-bottom: 1px solid var(--line);
}

.history-order:last-child {
  border-bottom: none;
}

.history-order-top {
  display: flex;
  justify-content: space-between;
  font-weight: 700;
  font-size: 13px;
  margin-bottom: 4px;
}

.history-order-items {
  font-size: 12px;
  color: rgba(28, 20, 16, 0.6);
  margin: 0;
}

/* Fulfillment toggle (pickup/delivery) */
.fulfillment-toggle {
  display: flex;
  gap: 6px;
  margin-bottom: 10px;
}

.fulfillment-option {
  flex: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  font-size: 12px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.02em;
  padding: 8px;
  border-radius: 8px;
  border: 1.5px solid var(--line);
  background: var(--paper);
  color: rgba(28, 20, 16, 0.55);
  transition: background 0.15s, color 0.15s, border-color 0.15s;
}

.fulfillment-option.active {
  background: var(--ink);
  color: var(--paper);
  border-color: var(--ink);
}

.amount-modal-note {
  width: 100%;
  font-size: 13px;
  padding: 10px 12px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  margin-bottom: 16px;
  font-family: var(--font-body);
  background: var(--paper);
  color: var(--ink);
  outline: none;
}

.amount-modal-note:focus {
  border-color: var(--rare);
}

.cart-line-note {
  font-size: 11px;
  font-style: italic;
  color: rgba(28, 20, 16, 0.5);
}

.order-note-input {
  width: 100%;
  font-size: 13px;
  padding: 9px 12px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  margin-bottom: 8px;
  font-family: var(--font-body);
  background: var(--paper);
  color: var(--ink);
  outline: none;
}

.order-note-input:focus {
  border-color: var(--rare);
}
```

================================================================================

--- BATCH 36 | frontend/src/components/orders/CounterScreen.tsx ---
STATUS: ⚠️ LARGE (383 lines)
FILE: frontend/src/components/orders/CounterScreen.tsx
```tsx
import { useState, useEffect, useMemo } from 'react';
import { Beef, Bird, Drumstick, Flame, Sandwich, CupSoda, History, Store, Truck } from 'lucide-react';
import type { MenuItem, OrderLineItem, Order, FulfillmentType, CustomerWithStats } from '@groundup/shared-types';
import { createOrder, fetchCustomerOrders } from '../../api/orders';
import { useMenu } from '../../hooks/useMenu';
import { useCustomers } from '../../hooks/useCustomers';
import './CounterScreen.css';

const CATEGORY_META: Record<string, { label: string; icon: typeof Beef }> = {
  'Glatt Kosher Beef (Fresh Cuts)': { label: 'Beef', icon: Beef },
  'Fresh Poultry (Chicken & Turkey)': { label: 'Poultry', icon: Bird },
  'Premium Lamb & Veal': { label: 'Lamb & Veal', icon: Drumstick },
  "Marinated & Oven-Ready Specials (Butcher's Prep)": { label: 'Prepared', icon: Flame },
  'Prepared Deli, Provisions & Shabbos Takeout': { label: 'Deli', icon: Sandwich },
  Beverages: { label: 'Drinks', icon: CupSoda },
};

let walkInCounter = 1;

type Props = {
  onOrderSent: () => void;
  recalledOrder: Order | null;
  onRecallConsumed: () => void;
};

export default function CounterScreen({ onOrderSent, recalledOrder, onRecallConsumed }: Props) {
  const { menu } = useMenu();
  const { customers } = useCustomers();
  const [activeCategory, setActiveCategory] = useState<string>('');
  const [cart, setCart] = useState<OrderLineItem[]>([]);
  const [pendingItem, setPendingItem] = useState<MenuItem | null>(null);
  const [amountInput, setAmountInput] = useState('');
  const [sending, setSending] = useState(false);

  const [nameInput, setNameInput] = useState('');
  const [selectedCustomer, setSelectedCustomer] = useState<CustomerWithStats | null>(null);
  const [showSuggestions, setShowSuggestions] = useState(false);

  const [historyOpen, setHistoryOpen] = useState(false);
  const [historyOrders, setHistoryOrders] = useState<Order[]>([]);
  const [historyLoading, setHistoryLoading] = useState(false);
  const [fulfillment, setFulfillment] = useState<FulfillmentType>('in_store');
  const [itemNoteInput, setItemNoteInput] = useState('');
  const [orderNote, setOrderNote] = useState('');

  const categories = useMemo(() => Array.from(new Set(menu.map((m) => m.category))), [menu]);

  useEffect(() => {
    if (menu.length > 0 && !activeCategory) {
      setActiveCategory(menu[0].category);
    }
  }, [menu, activeCategory]);

  // When an order is sent back from the stream, load it into the cart for editing.
  // Only consume the recall once customers have loaded, so we don't clear it
  // before we've had a real chance to match the customer by ID.
  useEffect(() => {
    if (!recalledOrder) return;
    if (recalledOrder.customerId && customers.length === 0) return;

    setCart(recalledOrder.items);
    setFulfillment(recalledOrder.fulfillment);
    setOrderNote(recalledOrder.notes ?? '');
    if (recalledOrder.customerId) {
      const match = customers.find((c) => c.customerId === recalledOrder.customerId);
      if (match) {
        setSelectedCustomer(match);
        setNameInput(`${match.firstName} ${match.lastName}`);
      } else {
        setNameInput(recalledOrder.customerName);
      }
    } else {
      setNameInput(recalledOrder.customerName);
    }
    onRecallConsumed();
  }, [recalledOrder, customers, onRecallConsumed]);

  const itemsInCategory = menu.filter((m) => m.category === activeCategory && m.isActive);

  const suggestions = useMemo(() => {
    const q = nameInput.trim().toLowerCase();
    if (!q || selectedCustomer) return [];
    return customers
      .filter((c) => `${c.firstName} ${c.lastName}`.toLowerCase().includes(q))
      .slice(0, 6);
  }, [nameInput, customers, selectedCustomer]);

  const startAdd = (item: MenuItem) => {
    setPendingItem(item);
    setAmountInput('');
    setItemNoteInput('');
  };

  const confirmAdd = () => {
    if (!pendingItem) return;
    const amount = parseFloat(amountInput);
    if (!amount || amount <= 0) return;

    const lineTotal = Math.round(pendingItem.price * amount * 100) / 100;
    setCart((prev) => [
      ...prev,
      {
        menuItemId: pendingItem.id,
        name: pendingItem.name,
        quantity: amount,
        unitPrice: pendingItem.price,
        lineTotal,
        notes: itemNoteInput.trim() || undefined,
      },
    ]);
    setPendingItem(null);
    setAmountInput('');
    setItemNoteInput('');
  };

  const removeLine = (index: number) => {
    setCart((prev) => prev.filter((_, i) => i !== index));
  };

  const cartTotal = Math.round(cart.reduce((sum, l) => sum + l.lineTotal, 0) * 100) / 100;

  const selectCustomer = (c: Customer) => {
    setSelectedCustomer(c);
    setNameInput(`${c.firstName} ${c.lastName}`);
    setShowSuggestions(false);
  };

  const clearCustomer = () => {
    setSelectedCustomer(null);
    setNameInput('');
  };

  const openHistory = async () => {
    if (!selectedCustomer) return;
    setHistoryOpen(true);
    setHistoryLoading(true);
    try {
      const orders = await fetchCustomerOrders(selectedCustomer.customerId);
      setHistoryOrders(orders);
    } catch (err) {
      console.error(err);
    } finally {
      setHistoryLoading(false);
    }
  };

  const sendOrder = async () => {
    if (cart.length === 0 || sending) return;
    setSending(true);
    try {
      const name =
        selectedCustomer
          ? `${selectedCustomer.firstName} ${selectedCustomer.lastName}`
          : nameInput.trim() || `Order #${walkInCounter++}`;
      await createOrder(
        name,
        cart,
        selectedCustomer?.customerId ?? null,
        fulfillment,
        orderNote.trim() || undefined
      );
      setCart([]);
      clearCustomer();
      setFulfillment('in_store');
      setOrderNote('');
      onOrderSent();
    } catch (err) {
      console.error(err);
      alert('Could not send order — is the server running?');
    } finally {
      setSending(false);
    }
  };

  const amountLabel = pendingItem?.soldBy === 'weight' ? 'Weight (lbs)' : 'Quantity';
  const amountValue = parseFloat(amountInput);
  const previewTotal =
    pendingItem && amountValue > 0 ? pendingItem.price * amountValue : null;

  return (
    <div className="counter-screen">
      <header className="counter-header">
        <span className="counter-eyebrow">Maple Kosher Meats</span>
        <h1>Counter</h1>
      </header>

      <div className="category-tabs">
        {categories.map((cat) => {
          const meta = CATEGORY_META[cat] ?? { label: cat, icon: Beef };
          const Icon = meta.icon;
          return (
            <button
              key={cat}
              className={`category-tab ${activeCategory === cat ? 'active' : ''}`}
              onClick={() => setActiveCategory(cat)}
              title={cat}
            >
              <Icon size={16} strokeWidth={2} />
              <span>{meta.label}</span>
            </button>
          );
        })}
      </div>

      <div className="item-grid">
        {itemsInCategory.map((item) => (
          <button key={item.id} className="item-tile" onClick={() => startAdd(item)}>
            <span className="item-name">{item.name}</span>
            <span className="item-price">
              ${item.price.toFixed(2)} {item.unit}
            </span>
          </button>
        ))}
      </div>

      {pendingItem && (
        <div className="amount-modal-backdrop" onClick={() => setPendingItem(null)}>
          <div className="amount-modal" onClick={(e) => e.stopPropagation()}>
            <h2>{pendingItem.name}</h2>
            <p className="amount-modal-price">
              ${pendingItem.price.toFixed(2)} {pendingItem.unit}
            </p>
            <input
              type="number"
              inputMode="decimal"
              autoFocus
              placeholder={amountLabel}
              value={amountInput}
              onChange={(e) => setAmountInput(e.target.value)}
              onKeyDown={(e) => e.key === 'Enter' && confirmAdd()}
            />
            {previewTotal !== null && (
              <p className="amount-modal-total">= ${previewTotal.toFixed(2)}</p>
            )}
            <input
              type="text"
              className="amount-modal-note"
              placeholder="Note (e.g. cut thin, extra fatty)"
              value={itemNoteInput}
              onChange={(e) => setItemNoteInput(e.target.value)}
            />
            <div className="amount-modal-actions">
              <button className="btn-secondary" onClick={() => setPendingItem(null)}>
                Cancel
              </button>
              <button className="btn-primary" onClick={confirmAdd}>
                Add to order
              </button>
            </div>
          </div>
        </div>
      )}

      {historyOpen && selectedCustomer && (
        <div className="history-modal-backdrop" onClick={() => setHistoryOpen(false)}>
          <div className="history-modal" onClick={(e) => e.stopPropagation()}>
            <h2>
              {selectedCustomer.firstName} {selectedCustomer.lastName}
            </h2>
            <p className="history-modal-meta">
              {selectedCustomer.loyaltyPoints} pts
              {selectedCustomer.dietaryNotes && ` · ${selectedCustomer.dietaryNotes}`}
            </p>
            <div className="history-modal-orders">
              {historyLoading && <p className="history-empty">Loading…</p>}
              {!historyLoading && historyOrders.length === 0 && (
                <p className="history-empty">No past orders yet.</p>
              )}
              {!historyLoading &&
                historyOrders.map((o) => (
                  <div className="history-order" key={o.id}>
                    <div className="history-order-top">
                      <span>{new Date(o.createdAt).toLocaleDateString()}</span>
                      <span>${o.total.toFixed(2)}</span>
                    </div>
                    <p className="history-order-items">
                      {o.items.map((i) => i.name).join(', ')}
                    </p>
                  </div>
                ))}
            </div>
          </div>
        </div>
      )}

      <div className="cart-panel">
        <div className="fulfillment-toggle">
          <button
            className={`fulfillment-option ${fulfillment === 'in_store' ? 'active' : ''}`}
            onClick={() => setFulfillment('in_store')}
          >
            In-Store
          </button>
          <button
            className={`fulfillment-option ${fulfillment === 'pickup' ? 'active' : ''}`}
            onClick={() => setFulfillment('pickup')}
          >
            <Store size={13} strokeWidth={2} />
            Pickup
          </button>
          <button
            className={`fulfillment-option ${fulfillment === 'delivery' ? 'active' : ''}`}
            onClick={() => setFulfillment('delivery')}
          >
            <Truck size={13} strokeWidth={2} />
            Delivery
          </button>
        </div>
        <div className="customer-input-wrap">
          <input
            className="customer-name-input"
            placeholder="Customer name"
            value={nameInput}
            onChange={(e) => {
              setNameInput(e.target.value);
              setSelectedCustomer(null);
              setShowSuggestions(true);
            }}
            onFocus={() => setShowSuggestions(true)}
            onBlur={() => setTimeout(() => setShowSuggestions(false), 200)}
          />
          {selectedCustomer && (
            <button className="history-pill" onClick={openHistory}>
              <History size={13} strokeWidth={2} />
              History
            </button>
          )}
          {showSuggestions && suggestions.length > 0 && (
            <div className="customer-suggestions">
              {suggestions.map((c) => (
                <button
                  key={c.customerId}
                  className="customer-suggestion"
                  onMouseDown={(e) => {
                    e.preventDefault();
                    selectCustomer(c);
                  }}
                >
                  {c.firstName} {c.lastName}
                </button>
              ))}
            </div>
          )}
        </div>
        <div className="cart-lines">
          {cart.length === 0 && <p className="cart-empty">No items yet — tap a product above.</p>}
          {cart.map((line, i) => (
            <div className="cart-line" key={i}>
              <div className="cart-line-info">
                <span className="cart-line-name">{line.name}</span>
                <span className="cart-line-weight">{line.quantity}</span>
                {line.notes && <span className="cart-line-note">"{line.notes}"</span>}
              </div>
              <span className="cart-line-total">${line.lineTotal.toFixed(2)}</span>
              <button className="cart-line-remove" onClick={() => removeLine(i)} aria-label="Remove">
                ×
              </button>
            </div>
          ))}
        </div>
        <input
          type="text"
          className="order-note-input"
          placeholder="Order note (e.g. call when ready)"
          value={orderNote}
          onChange={(e) => setOrderNote(e.target.value)}
        />
        <div className="cart-footer">
          <span className="cart-total-label">Total</span>
          <span className="cart-total-amount">${cartTotal.toFixed(2)}</span>
        </div>
        <button
          className="send-order-btn"
          disabled={cart.length === 0 || sending}
          onClick={sendOrder}
        >
          {sending ? 'Sending…' : 'Send order'}
        </button>
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 37 | frontend/src/components/orders/OrderStream.css ---
STATUS: ⚠️ LARGE (325 lines)
FILE: frontend/src/components/orders/OrderStream.css
```css
.order-stream {
  display: flex;
  flex-direction: column;
  height: 100%;
  padding: 28px 28px 0;
  background: var(--paper-dim);
}

.stream-header {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 18px;
}

.stream-header h1 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 28px;
  font-weight: 600;
  margin: 0;
}

.stream-count {
  font-size: 13px;
  font-weight: 600;
  color: var(--rare);
}

.stream-columns {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 18px;
  flex: 1;
  overflow: hidden;
  padding-bottom: 24px;
}

.stream-column {
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.stream-column-header {
  display: flex;
  justify-content: space-between;
  font-family: var(--font-body);
  font-weight: 700;
  font-size: 13px;
  text-transform: uppercase;
  letter-spacing: 0.05em;
  padding-bottom: 8px;
  border-bottom: 2px solid var(--ink);
  margin-bottom: 12px;
}

.stream-column-count {
  color: rgba(28, 20, 16, 0.5);
}

.stream-column-body {
  overflow-y: auto;
  flex: 1;
  display: flex;
  flex-direction: column;
  gap: 12px;
  padding-right: 4px;
}

.stream-empty {
  color: rgba(28, 20, 16, 0.4);
  font-size: 13px;
  font-style: italic;
}

/* Ticket card — signature element */
.order-ticket {
  position: relative;
  background: var(--paper);
  border-radius: 6px;
  padding: 14px 14px 12px;
  box-shadow: 0 2px 6px var(--shadow);
  border-left: 5px solid var(--line);
  transition: border-color 0.2s;
}

.ticket-placed {
  border-left-color: var(--rare);
}

.ticket-in_prep {
  border-left-color: var(--ochre);
}

.ticket-ready {
  border-left-color: var(--sage);
}

.ticket-notch {
  position: absolute;
  top: -6px;
  left: 16px;
  width: 12px;
  height: 12px;
  background: var(--paper-dim);
  border-radius: 50%;
  box-shadow: inset 0 1px 2px var(--shadow);
}

.ticket-top {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 6px;
  font-size: 11px;
  color: rgba(28, 20, 16, 0.5);
  font-weight: 600;
  margin-bottom: 4px;
  padding-right: 22px;
}

.fulfillment-pill {
  display: flex;
  align-items: center;
  gap: 3px;
  font-size: 9px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.03em;
  padding: 2px 7px;
  border-radius: 999px;
}

.fulfillment-pickup {
  background: rgba(92, 107, 71, 0.15);
  color: var(--sage);
}

.fulfillment-delivery {
  background: rgba(162, 59, 46, 0.12);
  color: var(--rare);
}

.ticket-customer {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 17px;
  margin: 0 0 8px;
  letter-spacing: 0.02em;
}

.ticket-items {
  list-style: none;
  padding: 0;
  margin: 0 0 10px;
  display: flex;
  flex-direction: column;
  gap: 3px;
}

.ticket-items li {
  font-size: 13px;
  font-weight: 500;
}

.ticket-item-weight {
  color: rgba(28, 20, 16, 0.55);
}

.ticket-footer {
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-top: 8px;
  border-top: 1px dashed var(--line);
  margin-bottom: 10px;
}

.ticket-total {
  font-weight: 700;
  font-size: 15px;
}

.ticket-claimed {
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  background: var(--ink);
  color: var(--paper);
  padding: 3px 8px;
  border-radius: 999px;
}

.ticket-actions {
  display: flex;
  flex-direction: column;
  gap: 6px;
}

.claim-btn {
  font-size: 12px;
  font-weight: 600;
  padding: 8px;
  border: 1.5px solid var(--ink);
  border-radius: 6px;
  background: transparent;
  color: var(--ink);
}

.claim-btn:hover {
  background: var(--ink);
  color: var(--paper);
}

.advance-btn {
  width: 100%;
  padding: 10px;
  border-radius: 6px;
  border: none;
  background: var(--ochre);
  color: var(--ink);
  font-weight: 700;
  font-size: 13px;
}

.advance-btn.complete {
  background: var(--sage);
  color: var(--paper);
}

/* Cancel button on each ticket */
.ticket-cancel {
  position: absolute;
  top: 8px;
  right: 8px;
  width: 20px;
  height: 20px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  border: none;
  background: var(--paper);
  color: rgba(28, 20, 16, 0.4);
  transition: background 0.15s, color 0.15s;
  z-index: 5;
}

.ticket-cancel:hover {
  background: var(--rare);
  color: var(--paper);
}

/* In-prep action row with unclaim + advance */
.ticket-actions-row {
  display: flex;
  gap: 6px;
}

.unclaim-btn {
  width: 36px;
  flex-shrink: 0;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  border: 1.5px solid var(--line);
  background: var(--paper);
  color: rgba(28, 20, 16, 0.6);
}

.unclaim-btn:hover {
  border-color: var(--ink);
  color: var(--ink);
}

.recall-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 5px;
  font-size: 11px;
  font-weight: 600;
  padding: 7px;
  border-radius: 6px;
  border: 1.5px dashed var(--line);
  background: transparent;
  color: rgba(28, 20, 16, 0.55);
  margin-top: 2px;
}

.recall-btn:hover {
  border-color: var(--ink);
  color: var(--ink);
}

.ticket-item-line {
  display: flex;
  justify-content: space-between;
}

.ticket-item-note {
  font-size: 11px;
  font-style: italic;
  color: rgba(28, 20, 16, 0.5);
  margin: 0 0 4px;
}

.ticket-order-note {
  font-size: 12px;
  background: rgba(212, 162, 76, 0.15);
  border-radius: 6px;
  padding: 6px 8px;
  margin: 0 0 10px;
  color: var(--ink);
}

.ticket-order-note::before {
  content: "NOTE — ";
  font-weight: 700;
  font-size: 10px;
  letter-spacing: 0.03em;
}
```

================================================================================

--- BATCH 38 | frontend/src/components/orders/OrderStream.tsx ---
STATUS: ✅ REVIEWABLE (189 lines)
FILE: frontend/src/components/orders/OrderStream.tsx
```tsx
import { useEffect, useState, useCallback } from 'react';
import { X, Undo2, Store, Truck, RotateCcw } from 'lucide-react';
import type { Order, OrderStatus } from '@groundup/shared-types';
import { fetchOrders, updateOrder } from '../../api/orders';
import { STORE_CONFIG } from '../../config';
import './OrderStream.css';

const COLUMNS: { status: OrderStatus; label: string }[] = [
  { status: 'placed', label: 'Placed' },
  { status: 'in_prep', label: 'In prep' },
  { status: 'ready', label: 'Ready' },
];

function timeAgo(ts: number): string {
  const mins = Math.floor((Date.now() - ts) / 60000);
  if (mins < 1) return 'just now';
  if (mins === 1) return '1 min ago';
  return `${mins} min ago`;
}

type Props = {
  refreshSignal: number;
  onRecall: (order: Order) => void;
};

export default function OrderStream({ refreshSignal, onRecall }: Props) {
  const [orders, setOrders] = useState<Order[]>([]);

  const load = useCallback(async () => {
    try {
      const data = await fetchOrders();
      setOrders(data);
    } catch (err) {
      console.error('Failed to load orders', err);
    }
  }, []);

  useEffect(() => {
    load();
    const interval = setInterval(load, STORE_CONFIG.orderStreamPollIntervalMs);
    return () => clearInterval(interval);
  }, [load, refreshSignal]);

  const handleClaim = async (orderId: string, staff: string) => {
    await updateOrder(orderId, { status: 'in_prep', claimedBy: staff });
    load();
  };

  const handleAdvance = async (order: Order) => {
    const next = order.status === 'in_prep' ? 'ready' : 'completed';
    await updateOrder(order.id, { status: next });
    load();
  };

  const handleUnclaim = async (order: Order) => {
    await updateOrder(order.id, { status: 'placed', claimedBy: '' });
    load();
  };

  const handleCancel = async (order: Order) => {
    if (!window.confirm(`Cancel order for ${order.customerName}?`)) return;
    await updateOrder(order.id, { status: 'cancelled' });
    load();
  };

  const visibleOrders = orders.filter(
    (o) => o.status !== 'completed' && o.status !== 'cancelled'
  );

  return (
    <div className="order-stream">
      <header className="stream-header">
        <h1>Order stream</h1>
        <span className="stream-count">{visibleOrders.length} active</span>
      </header>
      <div className="stream-columns">
        {COLUMNS.map((col) => {
          const colOrders = visibleOrders
            .filter((o) => o.status === col.status)
            .sort((a, b) => b.createdAt - a.createdAt);
          return (
            <div className="stream-column" key={col.status}>
              <div className="stream-column-header">
                <span>{col.label}</span>
                <span className="stream-column-count">{colOrders.length}</span>
              </div>
              <div className="stream-column-body">
                {colOrders.length === 0 && (
                  <p className="stream-empty">Nothing here right now.</p>
                )}
                {colOrders.map((order) => (
                  <div className={`order-ticket ticket-${order.status}`} key={order.id}>
                    <div className="ticket-notch" />
                    <button
                      className="ticket-cancel"
                      onClick={() => handleCancel(order)}
                      aria-label="Cancel order"
                      title="Cancel order"
                    >
                      <X size={14} strokeWidth={2.5} />
                    </button>
                    <div className="ticket-top">
                      <span className="ticket-id">#{order.id.replace('ord-', '').replace('hist-', '')}</span>
                      {order.fulfillment !== 'in_store' && (
                        <span className={`fulfillment-pill fulfillment-${order.fulfillment}`}>
                          {order.fulfillment === 'delivery' ? (
                            <Truck size={11} strokeWidth={2.5} />
                          ) : (
                            <Store size={11} strokeWidth={2.5} />
                          )}
                          {order.fulfillment}
                        </span>
                      )}
                      <span className="ticket-time">{timeAgo(order.createdAt)}</span>
                    </div>
                    <h3 className="ticket-customer">{order.customerName}</h3>
                    <ul className="ticket-items">
                      {order.items.map((item, i) => (
                        <li key={i}>
                          <div className="ticket-item-line">
                            <span>{item.name}</span>
                            <span className="ticket-item-weight">{item.quantity}</span>
                          </div>
                          {item.notes && <p className="ticket-item-note">"{item.notes}"</p>}
                        </li>
                      ))}
                    </ul>
                    {order.notes && <p className="ticket-order-note">{order.notes}</p>}
                    <div className="ticket-footer">
                      <span className="ticket-total">${order.total.toFixed(2)}</span>
                      {order.claimedBy && (
                        <span className="ticket-claimed">{order.claimedBy}</span>
                      )}
                    </div>

                    {order.status === 'placed' && (
                      <div className="ticket-actions">
                        {STORE_CONFIG.staff.map((staff) => (
                          <button
                            key={staff}
                            className="claim-btn"
                            onClick={() => handleClaim(order.id, staff)}
                          >
                            Claim — {staff}
                          </button>
                        ))}
                        <button
                          className="recall-btn"
                          onClick={() => onRecall(order)}
                          title="Send back to register to edit"
                        >
                          <RotateCcw size={13} strokeWidth={2} />
                          Send back to register
                        </button>
                      </div>
                    )}
                    {order.status === 'in_prep' && (
                      <div className="ticket-actions-row">
                        <button
                          className="unclaim-btn"
                          onClick={() => handleUnclaim(order)}
                          title="Send back to Placed"
                        >
                          <Undo2 size={14} strokeWidth={2} />
                        </button>
                        <button className="advance-btn" onClick={() => handleAdvance(order)}>
                          Mark ready
                        </button>
                      </div>
                    )}
                    {order.status === 'ready' && (
                      <button
                        className="advance-btn complete"
                        onClick={() => handleAdvance(order)}
                      >
                        Picked up
                      </button>
                    )}
                  </div>
                ))}
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 39 | frontend/src/components/ui/Toast.css ---
STATUS: ✅ REVIEWABLE (34 lines)
FILE: frontend/src/components/ui/Toast.css
```css
.toast {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 12px 20px;
  border-radius: 10px;
  background: var(--ink);
  color: var(--paper);
  font-size: 13px;
  font-weight: 600;
  box-shadow: 0 12px 28px rgba(0, 0, 0, 0.3);
  z-index: 100;
  animation: toast-in 0.2s ease-out;
}

.toast svg {
  color: var(--sage);
}

@keyframes toast-in {
  from {
    opacity: 0;
    transform: translate(-50%, 8px);
  }
  to {
    opacity: 1;
    transform: translate(-50%, 0);
  }
}
```

================================================================================

--- BATCH 40 | frontend/src/components/ui/Toast.tsx ---
STATUS: ✅ REVIEWABLE (23 lines)
FILE: frontend/src/components/ui/Toast.tsx
```tsx
import { useEffect } from 'react';
import { CheckCircle2 } from 'lucide-react';
import './Toast.css';

type Props = {
  message: string;
  onDismiss: () => void;
};

export default function Toast({ message, onDismiss }: Props) {
  useEffect(() => {
    const timer = setTimeout(onDismiss, 2500);
    return () => clearTimeout(timer);
  }, [onDismiss]);

  return (
    <div className="toast">
      <CheckCircle2 size={16} strokeWidth={2} />
      <span>{message}</span>
    </div>
  );
}
```

================================================================================

--- BATCH 41 | frontend/src/data/menuTiers.ts ---
STATUS: ⚠️ LARGE (359 lines)
FILE: frontend/src/data/menuTiers.ts
```ts
import type { MenuItemTiers } from '@groundup/shared-types';

export const MENU_TIERS: MenuItemTiers[] = [
  {
    menuItemId: "beef-ground-family-pack",
    tiers: [
      { label: "Small", weightLbs: 3.2, price: 27.17 },
      { label: "Medium", weightLbs: 3.6, price: 30.56 },
      { label: "Large", weightLbs: 4.0, price: 33.96 },
    ],
  },
  {
    menuItemId: "beef-ground-extra-lean",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 15.73 },
      { label: "Medium", weightLbs: 1.8, price: 18.88 },
      { label: "Large", weightLbs: 2.0, price: 20.98 },
    ],
  },
  {
    menuItemId: "beef-flank-steak",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 20.98 },
      { label: "Medium", weightLbs: 2.0, price: 27.98 },
      { label: "Large", weightLbs: 2.5, price: 34.98 },
    ],
  },
  {
    menuItemId: "beef-ribeye-boneless",
    tiers: [
      { label: "Small", weightLbs: 0.8, price: 19.19 },
      { label: "Medium", weightLbs: 1.0, price: 23.99 },
      { label: "Large", weightLbs: 1.2, price: 28.79 },
    ],
  },
  {
    menuItemId: "beef-skirt-steak-prime",
    tiers: [
      { label: "Small", weightLbs: 0.8, price: 38.39 },
      { label: "Medium", weightLbs: 1.1, price: 52.79 },
      { label: "Large", weightLbs: 1.5, price: 71.98 },
    ],
  },
  {
    menuItemId: "beef-chuck-eye-roast",
    tiers: [
      { label: "Small", weightLbs: 3.0, price: 38.97 },
      { label: "Medium", weightLbs: 3.8, price: 49.36 },
      { label: "Large", weightLbs: 4.5, price: 58.45 },
    ],
  },
  {
    menuItemId: "beef-pepper-steak-fajita",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 13.99 },
      { label: "Medium", weightLbs: 1.2, price: 16.79 },
      { label: "Large", weightLbs: 1.5, price: 20.98 },
    ],
  },
  {
    menuItemId: "beef-cholent-meat-mock-flanken",
    tiers: [
      { label: "Small", weightLbs: 1.2, price: 13.19 },
      { label: "Medium", weightLbs: 1.6, price: 17.58 },
      { label: "Large", weightLbs: 2.0, price: 21.98 },
    ],
  },
  {
    menuItemId: "beef-brisket-first-cut",
    tiers: [
      { label: "Small", weightLbs: 3.5, price: 54.22 },
      { label: "Medium", weightLbs: 4.2, price: 65.06 },
      { label: "Large", weightLbs: 5.0, price: 77.45 },
    ],
  },
  {
    menuItemId: "beef-flanken-boneless",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 34.48 },
      { label: "Medium", weightLbs: 2.0, price: 45.98 },
      { label: "Large", weightLbs: 2.5, price: 57.47 },
    ],
  },
  {
    menuItemId: "beef-marrow-bones-frozen",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 23.98 },
      { label: "Medium", weightLbs: 1.8, price: 28.78 },
      { label: "Large", weightLbs: 2.0, price: 31.98 },
    ],
  },
  {
    menuItemId: "poultry-whole-chicken",
    tiers: [
      { label: "Small", weightLbs: 3.5, price: 13.96 },
      { label: "Medium", weightLbs: 4.0, price: 15.96 },
      { label: "Large", weightLbs: 4.5, price: 17.96 },
    ],
  },
  {
    menuItemId: "poultry-chicken-cutlets-family-pack",
    tiers: [
      { label: "Small", weightLbs: 2.0, price: 15.98 },
      { label: "Medium", weightLbs: 2.5, price: 19.98 },
      { label: "Large", weightLbs: 3.0, price: 23.97 },
    ],
  },
  {
    menuItemId: "poultry-dark-cutlets-pargiot",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 10.48 },
      { label: "Medium", weightLbs: 2.0, price: 13.98 },
      { label: "Large", weightLbs: 2.5, price: 17.48 },
    ],
  },
  {
    menuItemId: "poultry-drumsticks-family-pack",
    tiers: [
      { label: "Small", weightLbs: 3.0, price: 9.87 },
      { label: "Medium", weightLbs: 3.5, price: 11.52 },
      { label: "Large", weightLbs: 4.0, price: 13.16 },
    ],
  },
  {
    menuItemId: "poultry-thighs-family-pack",
    tiers: [
      { label: "Small", weightLbs: 2.5, price: 11.23 },
      { label: "Medium", weightLbs: 3.0, price: 13.47 },
      { label: "Large", weightLbs: 3.5, price: 15.71 },
    ],
  },
  {
    menuItemId: "poultry-cutlets-extra-thin-schnitzel",
    tiers: [
      { label: "Small", weightLbs: 1.2, price: 13.19 },
      { label: "Medium", weightLbs: 1.5, price: 16.48 },
      { label: "Large", weightLbs: 1.8, price: 19.78 },
    ],
  },
  {
    menuItemId: "poultry-ground-chicken-white",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 13.48 },
      { label: "Medium", weightLbs: 1.8, price: 16.18 },
      { label: "Large", weightLbs: 2.0, price: 17.98 },
    ],
  },
  {
    menuItemId: "poultry-turkey-london-broil-skinless",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 15.73 },
      { label: "Medium", weightLbs: 1.9, price: 19.93 },
      { label: "Large", weightLbs: 2.2, price: 23.08 },
    ],
  },
  {
    menuItemId: "poultry-dark-turkey-roast",
    tiers: [
      { label: "Small", weightLbs: 2.0, price: 19.98 },
      { label: "Medium", weightLbs: 2.5, price: 24.98 },
      { label: "Large", weightLbs: 3.0, price: 29.97 },
    ],
  },
  {
    menuItemId: "poultry-whole-duck-frozen",
    tiers: [
      { label: "Small", weightLbs: 4.0, price: 59.96 },
      { label: "Medium", weightLbs: 4.8, price: 71.95 },
      { label: "Large", weightLbs: 5.5, price: 82.45 },
    ],
  },
  {
    menuItemId: "lamb-shoulder-chops",
    tiers: [
      { label: "Small", weightLbs: 1.2, price: 21.59 },
      { label: "Medium", weightLbs: 1.4, price: 25.19 },
      { label: "Large", weightLbs: 1.5, price: 26.98 },
    ],
  },
  {
    menuItemId: "lamb-baby-rib-chops",
    tiers: [
      { label: "Small", weightLbs: 0.8, price: 34.39 },
      { label: "Medium", weightLbs: 1.1, price: 47.29 },
      { label: "Large", weightLbs: 1.3, price: 55.89 },
    ],
  },
  {
    menuItemId: "lamb-ground-premium",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 24.99 },
      { label: "Medium", weightLbs: 1.2, price: 29.99 },
      { label: "Large", weightLbs: 1.5, price: 37.48 },
    ],
  },
  {
    menuItemId: "veal-chops-second-cut",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 29.99 },
      { label: "Medium", weightLbs: 1.2, price: 35.99 },
      { label: "Large", weightLbs: 1.5, price: 44.98 },
    ],
  },
  {
    menuItemId: "veal-ground",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 19.99 },
      { label: "Medium", weightLbs: 1.2, price: 23.99 },
      { label: "Large", weightLbs: 1.5, price: 29.98 },
    ],
  },
  {
    menuItemId: "veal-stew-meat",
    tiers: [
      { label: "Small", weightLbs: 1.2, price: 23.99 },
      { label: "Medium", weightLbs: 1.5, price: 29.98 },
      { label: "Large", weightLbs: 1.8, price: 35.98 },
    ],
  },
  {
    menuItemId: "prep-london-broil-seasoned",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 17.98 },
      { label: "Medium", weightLbs: 1.9, price: 22.78 },
      { label: "Large", weightLbs: 2.2, price: 26.38 },
    ],
  },
  {
    menuItemId: "prep-turkey-london-broil-seasoned",
    tiers: [
      { label: "Small", weightLbs: 1.2, price: 11.99 },
      { label: "Medium", weightLbs: 1.5, price: 14.98 },
      { label: "Large", weightLbs: 1.8, price: 17.98 },
    ],
  },
  {
    menuItemId: "prep-dark-turkey-shawarma",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 16.48 },
      { label: "Medium", weightLbs: 1.8, price: 19.78 },
      { label: "Large", weightLbs: 2.0, price: 21.98 },
    ],
  },
  {
    menuItemId: "prep-beef-pastrami-2nd-cut",
    tiers: [
      { label: "Small", weightLbs: 1.8, price: 25.18 },
      { label: "Medium", weightLbs: 2.1, price: 29.38 },
      { label: "Large", weightLbs: 2.5, price: 34.98 },
    ],
  },
  {
    menuItemId: "prep-french-roast-dry-rub",
    tiers: [
      { label: "Small", weightLbs: 2.5, price: 42.47 },
      { label: "Medium", weightLbs: 3.2, price: 54.37 },
      { label: "Large", weightLbs: 4.0, price: 67.96 },
    ],
  },
  {
    menuItemId: "prep-baby-chicken-pargiot-grill-ready",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 16.48 },
      { label: "Medium", weightLbs: 1.9, price: 20.88 },
      { label: "Large", weightLbs: 2.2, price: 24.18 },
    ],
  },
  {
    menuItemId: "prep-lamb-beef-kabobs",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 13.99 },
      { label: "Medium", weightLbs: 1.2, price: 16.79 },
      { label: "Large", weightLbs: 1.5, price: 20.98 },
    ],
  },
  {
    menuItemId: "prep-stuffed-chicken-capon",
    tiers: [
      { label: "Small", weightLbs: 1.5, price: 17.98 },
      { label: "Medium", weightLbs: 1.8, price: 21.58 },
      { label: "Large", weightLbs: 2.0, price: 23.98 },
    ],
  },
  {
    menuItemId: "deli-beef-salami-chub-16oz",
    tiers: [
      { label: "Standard", weightLbs: 1.0, price: 10.49 },
    ],
  },
  {
    menuItemId: "deli-sliced-beef-pastrami-6oz",
    tiers: [
      { label: "Standard", weightLbs: 0.375, price: 9.99 },
    ],
  },
  {
    menuItemId: "deli-sliced-smoked-turkey-breast-12oz",
    tiers: [
      { label: "Standard", weightLbs: 0.75, price: 12.99 },
    ],
  },
  {
    menuItemId: "deli-potato-kugel-family-24oz",
    tiers: [
      { label: "Standard", weightLbs: 1.5, price: 11.99 },
    ],
  },
  {
    menuItemId: "deli-sweet-noodle-kugel-24oz",
    tiers: [
      { label: "Standard", weightLbs: 1.5, price: 10.99 },
    ],
  },
  {
    menuItemId: "deli-broccoli-kugel-12oz",
    tiers: [
      { label: "Standard", weightLbs: 0.75, price: 8.99 },
    ],
  },
  {
    menuItemId: "deli-beef-cholent-thu-fri",
    tiers: [
      { label: "Standard", weightLbs: 2.0, price: 11.99 },
    ],
  },
  {
    menuItemId: "deli-chicken-schnitzel-prepared",
    tiers: [
      { label: "Small", weightLbs: 1.0, price: 15.99 },
      { label: "Medium", weightLbs: 1.2, price: 19.19 },
      { label: "Large", weightLbs: 1.5, price: 23.98 },
    ],
  },
  {
    menuItemId: "bev-dr-browns-cream-soda",
    tiers: [
      { label: "Standard", weightLbs: null, price: 2.49 },
    ],
  },
  {
    menuItemId: "bev-dr-browns-black-cherry",
    tiers: [
      { label: "Standard", weightLbs: null, price: 2.49 },
    ],
  },
  {
    menuItemId: "bev-dr-browns-cel-ray",
    tiers: [
      { label: "Standard", weightLbs: null, price: 2.49 },
    ],
  },
  {
    menuItemId: "bev-sparkling-water",
    tiers: [
      { label: "Standard", weightLbs: null, price: 1.99 },
    ],
  },
];
```

================================================================================

--- BATCH 42 | frontend/src/hooks/useCustomers.ts ---
STATUS: ✅ REVIEWABLE (40 lines)
FILE: frontend/src/hooks/useCustomers.ts
```ts
import { useState, useEffect } from 'react';
import type { CustomerWithStats } from '@groundup/shared-types';
import { fetchCustomersWithStats } from '../api/customers';

let cachedCustomers: CustomerWithStats[] | null = null;
let inFlight: Promise<CustomerWithStats[]> | null = null;

// Same pattern as useMenu — one fetch, shared cache, every screen reads
// from the same source instead of independently re-fetching.
export function useCustomers() {
  const [customers, setCustomers] = useState<CustomerWithStats[]>(cachedCustomers ?? []);
  const [loading, setLoading] = useState(cachedCustomers === null);

  useEffect(() => {
    if (cachedCustomers) {
      setCustomers(cachedCustomers);
      setLoading(false);
      return;
    }
    if (!inFlight) {
      inFlight = fetchCustomersWithStats();
    }
    inFlight
      .then((items) => {
        cachedCustomers = items;
        setCustomers(items);
      })
      .catch(console.error)
      .finally(() => setLoading(false));
  }, []);

  return { customers, loading };
}

// Call after any customer create/edit/order-placement so stats stay fresh.
export function invalidateCustomersCache() {
  cachedCustomers = null;
  inFlight = null;
}
```

================================================================================

--- BATCH 43 | frontend/src/hooks/useMenu.ts ---
STATUS: ✅ REVIEWABLE (11 lines)
FILE: frontend/src/hooks/useMenu.ts
```ts
import { useMenuContext } from '../providers/MenuProvider';

export function useMenu() {
  const { activeMenu, loading } = useMenuContext();
  return { menu: activeMenu, menuItems: activeMenu, loading };
}

export function invalidateMenuCache() {
  // MenuProvider owns menu state now.
}
```

================================================================================

--- BATCH 44 | frontend/src/pages/CounterPage.tsx ---
STATUS: ✅ REVIEWABLE (66 lines)
FILE: frontend/src/pages/CounterPage.tsx
```tsx
import { useState, useEffect } from 'react';
import { useLocation } from 'react-router-dom';
import type { Order } from '@groundup/shared-types';
import CounterScreen from '../components/orders/CounterScreen';
import OrderStream from '../components/orders/OrderStream';
import { deleteOrder } from '../api/orders';
import '../App.layout.css';

type StartOrderState = {
  startOrderForCustomerId?: string;
};

export default function MainPage() {
  const [refreshSignal, setRefreshSignal] = useState(0);
  const [recalledOrder, setRecalledOrder] = useState<Order | null>(null);
  const location = useLocation();

  // If the user navigated here from "Start order" on the Customers page,
  // pre-fill the cart's customer field via the same mechanism used for recalls.
  useEffect(() => {
    const state = location.state as StartOrderState | null;
    const customerId = state?.startOrderForCustomerId;
    if (customerId) {
      setRecalledOrder({
        id: '',
        customerId,
        customerName: '',
        items: [],
        total: 0,
        status: 'placed',
        source: 'counter',
        fulfillment: 'in_store',
        claimedBy: null,
        createdAt: Date.now(),
      });
      window.history.replaceState({}, document.title);
    }
  }, [location.state]);

  const handleRecall = async (order: Order) => {
    try {
      await deleteOrder(order.id);
      setRecalledOrder(order);
      setRefreshSignal((n) => n + 1);
    } catch (err) {
      console.error(err);
      alert('Could not send this order back — it may have already been claimed.');
    }
  };

  return (
    <div className="mkb-app">
      <div className="mkb-counter-pane">
        <CounterScreen
          onOrderSent={() => setRefreshSignal((n) => n + 1)}
          recalledOrder={recalledOrder}
          onRecallConsumed={() => setRecalledOrder(null)}
        />
      </div>
      <div className="mkb-stream-pane">
        <OrderStream refreshSignal={refreshSignal} onRecall={handleRecall} />
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 45 | frontend/src/pages/CustomersPage.css ---
STATUS: ⚠️ LARGE (301 lines)
FILE: frontend/src/pages/CustomersPage.css
```css
.customers-page {
  height: 100%;
  overflow-y: auto;
  padding: 28px 32px 48px;
  background: var(--paper-dim);
}

.customers-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 20px;
}

.customers-eyebrow {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--rare);
}

.customers-header h1 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 30px;
  font-weight: 600;
  margin: 2px 0 0;
}

.customers-header-actions {
  display: flex;
  gap: 10px;
}

.insights-btn,
.add-customer-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  font-weight: 600;
  padding: 10px 16px;
  border-radius: 8px;
  border: 1.5px solid var(--ink);
  background: var(--paper);
  color: var(--ink);
}

.insights-btn:hover,
.add-customer-btn:hover {
  background: var(--ink);
  color: var(--paper);
}

.add-customer-btn {
  background: var(--rare);
  border-color: var(--rare);
  color: var(--paper);
}

.add-customer-btn:hover {
  background: #8a3025;
}

.customers-search-wrap {
  position: relative;
  margin-bottom: 18px;
  max-width: 420px;
}

.search-icon {
  position: absolute;
  left: 12px;
  top: 50%;
  transform: translateY(-50%);
  color: rgba(28, 20, 16, 0.4);
  pointer-events: none;
}

.customers-search-input {
  width: 100%;
  font-size: 14px;
  padding: 10px 12px 10px 36px;
  border: 1.5px solid var(--line);
  border-radius: 10px;
  background: var(--paper);
  outline: none;
}

.customers-search-input:focus {
  border-color: var(--rare);
}

.customers-table-wrap {
  background: var(--paper);
  border: 1.5px solid var(--line);
  border-radius: 12px;
  overflow: hidden;
}

.customers-loading {
  padding: 32px;
  text-align: center;
  color: rgba(28, 20, 16, 0.5);
  font-size: 13px;
}

.customers-table {
  width: 100%;
  border-collapse: collapse;
}

.customers-table thead th {
  text-align: left;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  letter-spacing: 0.04em;
  color: rgba(28, 20, 16, 0.55);
  padding: 12px 16px;
  border-bottom: 2px solid var(--ink);
  cursor: pointer;
  white-space: nowrap;
  user-select: none;
}

.customers-table thead th:hover {
  color: var(--ink);
}

.customers-table thead th span {
  display: inline-flex;
  align-items: center;
  gap: 4px;
}

.sort-icon.idle {
  opacity: 0.35;
}

.sort-icon.active {
  color: var(--rare);
}

.customers-table tbody td {
  padding: 12px 16px;
  font-size: 13px;
  border-bottom: 1px solid var(--line);
}

.customers-table tbody tr:last-child td {
  border-bottom: none;
}

.customers-table tbody tr:hover {
  background: var(--paper-dim);
}

.customer-name-cell {
  font-weight: 600;
}

.dietary-tag {
  display: block;
  font-size: 11px;
  font-weight: 500;
  color: var(--sage);
  margin-top: 2px;
}

.contact-cell {
  display: flex;
  flex-direction: column;
  font-size: 12px;
}

.contact-phone {
  color: rgba(28, 20, 16, 0.5);
}

.actions-col {
  width: 1%;
}

.actions-cell {
  display: flex;
  gap: 4px;
  white-space: nowrap;
}

.row-action-btn {
  width: 30px;
  height: 30px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 7px;
  border: 1.5px solid var(--line);
  background: var(--paper);
  color: rgba(28, 20, 16, 0.6);
}

.row-action-btn:hover {
  border-color: var(--rare);
  color: var(--rare);
}

@media (max-width: 800px) {
  .hide-sm {
    display: none;
  }
}

/* Order history modal (reused style for this page) */
.history-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 60;
}

.history-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 24px 28px 28px;
  width: 380px;
  max-height: 70vh;
  overflow-y: auto;
  box-shadow: 0 12px 32px var(--shadow);
}

.history-modal-head {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
}

.history-modal-head h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 20px;
  margin: 0;
}

.modal-close-btn {
  width: 28px;
  height: 28px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  border: none;
  background: transparent;
  color: rgba(28, 20, 16, 0.5);
}

.modal-close-btn:hover {
  background: var(--paper-dim);
  color: var(--ink);
}

.history-modal-meta {
  color: var(--rare);
  font-weight: 600;
  font-size: 13px;
  margin: 4px 0 16px;
}

.history-empty {
  color: rgba(28, 20, 16, 0.45);
  font-size: 13px;
  font-style: italic;
}

.history-order {
  padding: 10px 0;
  border-bottom: 1px solid var(--line);
}

.history-order:last-child {
  border-bottom: none;
}

.history-order-top {
  display: flex;
  justify-content: space-between;
  font-weight: 700;
  font-size: 13px;
  margin-bottom: 4px;
}

.history-order-items {
  font-size: 12px;
  color: rgba(28, 20, 16, 0.6);
  margin: 0;
}
```

================================================================================

--- BATCH 46 | frontend/src/pages/CustomersPage.tsx ---
STATUS: ⚠️ LARGE (323 lines)
FILE: frontend/src/pages/CustomersPage.tsx
```tsx
import { useState, useEffect, useMemo } from 'react';
import { useNavigate } from 'react-router-dom';
import {
  Search,
  ArrowUpDown,
  ArrowUp,
  ArrowDown,
  History,
  ShoppingCart,
  Pencil,
  UserPlus,
  PieChart,
  X,
} from 'lucide-react';
import type { CustomerWithStats, Order } from '@groundup/shared-types';
import {
  fetchCustomersWithStats,
  fetchCustomerOrderHistory,
  createCustomer,
  updateCustomer,
} from '../api/customers';
import { invalidateCustomersCache } from '../hooks/useCustomers';
import CustomerEditModal from '../components/customers/CustomerEditModal';
import CustomerInsightsModal from '../components/customers/CustomerInsightsModal';
import './CustomersPage.css';

type SortKey = 'name' | 'loyaltyPoints' | 'totalSpent' | 'orderCount' | 'lastOrderAt';
type SortDir = 'asc' | 'desc';

function formatDate(ts: number | null): string {
  if (!ts) return 'Never';
  return new Date(ts).toLocaleDateString();
}

export default function CustomersPage() {
  const navigate = useNavigate();
  const [customers, setCustomers] = useState<CustomerWithStats[]>([]);
  const [loading, setLoading] = useState(true);
  const [search, setSearch] = useState('');
  const [sortKey, setSortKey] = useState<SortKey>('name');
  const [sortDir, setSortDir] = useState<SortDir>('asc');

  const [editTarget, setEditTarget] = useState<CustomerWithStats | null>(null);
  const [showAddModal, setShowAddModal] = useState(false);
  const [showInsights, setShowInsights] = useState(false);

  const [historyTarget, setHistoryTarget] = useState<CustomerWithStats | null>(null);
  const [historyOrders, setHistoryOrders] = useState<Order[]>([]);
  const [historyLoading, setHistoryLoading] = useState(false);

  const load = async () => {
    setLoading(true);
    try {
      const data = await fetchCustomersWithStats();
      setCustomers(data);
    } catch (err) {
      console.error(err);
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    load();
  }, []);

  const filtered = useMemo(() => {
    const q = search.trim().toLowerCase();
    let rows = customers;
    if (q) {
      rows = rows.filter((c) => {
        const fullName = `${c.firstName} ${c.lastName}`.toLowerCase();
        return (
          fullName.includes(q) ||
          c.email.toLowerCase().includes(q) ||
          c.phone.toLowerCase().includes(q)
        );
      });
    }
    const sorted = [...rows].sort((a, b) => {
      let cmp = 0;
      switch (sortKey) {
        case 'name':
          cmp = `${a.firstName} ${a.lastName}`.localeCompare(`${b.firstName} ${b.lastName}`);
          break;
        case 'loyaltyPoints':
          cmp = a.loyaltyPoints - b.loyaltyPoints;
          break;
        case 'totalSpent':
          cmp = a.totalSpent - b.totalSpent;
          break;
        case 'orderCount':
          cmp = a.orderCount - b.orderCount;
          break;
        case 'lastOrderAt':
          cmp = (a.lastOrderAt ?? 0) - (b.lastOrderAt ?? 0);
          break;
      }
      return sortDir === 'asc' ? cmp : -cmp;
    });
    return sorted;
  }, [customers, search, sortKey, sortDir]);

  const toggleSort = (key: SortKey) => {
    if (sortKey === key) {
      setSortDir((d) => (d === 'asc' ? 'desc' : 'asc'));
    } else {
      setSortKey(key);
      setSortDir('asc');
    }
  };

  const SortIcon = ({ active, dir }: { active: boolean; dir: SortDir }) => {
    if (!active) return <ArrowUpDown size={13} strokeWidth={2} className="sort-icon idle" />;
    return dir === 'asc' ? (
      <ArrowUp size={13} strokeWidth={2.5} className="sort-icon active" />
    ) : (
      <ArrowDown size={13} strokeWidth={2.5} className="sort-icon active" />
    );
  };

  const startOrder = (c: CustomerWithStats) => {
    navigate('/', { state: { startOrderForCustomerId: c.customerId } });
  };

  const openHistory = async (c: CustomerWithStats) => {
    setHistoryTarget(c);
    setHistoryLoading(true);
    try {
      const orders = await fetchCustomerOrderHistory(c.customerId);
      setHistoryOrders(orders);
    } catch (err) {
      console.error(err);
    } finally {
      setHistoryLoading(false);
    }
  };

  const handleSaveEdit = async (data: Partial<CustomerWithStats>) => {
    if (!editTarget) return;
    await updateCustomer(editTarget.customerId, data);
    invalidateCustomersCache();
    setEditTarget(null);
    load();
  };

  const handleSaveNew = async (data: Partial<CustomerWithStats>) => {
    await createCustomer({
      firstName: data.firstName ?? '',
      lastName: data.lastName ?? '',
      phone: data.phone ?? '',
      email: data.email ?? '',
      loyaltyPoints: data.loyaltyPoints ?? 0,
      dietaryNotes: data.dietaryNotes ?? '',
      address: data.address ?? { street: '', city: '', state: 'NJ', zip: '' },
    });
    invalidateCustomersCache();
    setShowAddModal(false);
    load();
  };

  return (
    <div className="customers-page">
      <header className="customers-header">
        <div>
          <span className="customers-eyebrow">Maple Kosher Meats</span>
          <h1>Customers</h1>
        </div>
        <div className="customers-header-actions">
          <button className="insights-btn" onClick={() => setShowInsights(true)}>
            <PieChart size={15} strokeWidth={2} />
            Insights
          </button>
          <button className="add-customer-btn" onClick={() => setShowAddModal(true)}>
            <UserPlus size={15} strokeWidth={2} />
            Add customer
          </button>
        </div>
      </header>

      <div className="customers-search-wrap">
        <Search size={16} strokeWidth={2} className="search-icon" />
        <input
          className="customers-search-input"
          placeholder="Search by name, email, or phone…"
          value={search}
          onChange={(e) => setSearch(e.target.value)}
        />
      </div>

      <div className="customers-table-wrap">
        {loading ? (
          <p className="customers-loading">Loading customers…</p>
        ) : filtered.length === 0 ? (
          <p className="customers-loading">No customers match your search.</p>
        ) : (
          <table className="customers-table">
            <thead>
              <tr>
                <th onClick={() => toggleSort('name')}>
                  <span>Name</span>
                  <SortIcon active={sortKey === 'name'} dir={sortDir} />
                </th>
                <th className="hide-sm">Contact</th>
                <th onClick={() => toggleSort('loyaltyPoints')}>
                  <span>Points</span>
                  <SortIcon active={sortKey === 'loyaltyPoints'} dir={sortDir} />
                </th>
                <th onClick={() => toggleSort('orderCount')}>
                  <span>Orders</span>
                  <SortIcon active={sortKey === 'orderCount'} dir={sortDir} />
                </th>
                <th onClick={() => toggleSort('totalSpent')}>
                  <span>Total spent</span>
                  <SortIcon active={sortKey === 'totalSpent'} dir={sortDir} />
                </th>
                <th onClick={() => toggleSort('lastOrderAt')} className="hide-sm">
                  <span>Last order</span>
                  <SortIcon active={sortKey === 'lastOrderAt'} dir={sortDir} />
                </th>
                <th className="actions-col"></th>
              </tr>
            </thead>
            <tbody>
              {filtered.map((c) => (
                <tr key={c.customerId}>
                  <td className="customer-name-cell">
                    {c.firstName} {c.lastName}
                    {c.dietaryNotes && <span className="dietary-tag">{c.dietaryNotes}</span>}
                  </td>
                  <td className="hide-sm contact-cell">
                    <span>{c.email}</span>
                    <span className="contact-phone">{c.phone}</span>
                  </td>
                  <td>{c.loyaltyPoints}</td>
                  <td>{c.orderCount ?? 0}</td>
                  <td>${(c.totalSpent ?? 0).toFixed(2)}</td>
                  <td className="hide-sm">{formatDate(c.lastOrderAt ?? null)}</td>
                  <td className="actions-cell">
                    <button
                      className="row-action-btn"
                      title="Start order"
                      onClick={() => startOrder(c)}
                    >
                      <ShoppingCart size={15} strokeWidth={2} />
                    </button>
                    <button
                      className="row-action-btn"
                      title="Order history"
                      onClick={() => openHistory(c)}
                    >
                      <History size={15} strokeWidth={2} />
                    </button>
                    <button
                      className="row-action-btn"
                      title="Edit"
                      onClick={() => setEditTarget(c)}
                    >
                      <Pencil size={15} strokeWidth={2} />
                    </button>
                  </td>
                </tr>
              ))}
            </tbody>
          </table>
        )}
      </div>

      {historyTarget && (
        <div className="history-modal-backdrop" onClick={() => setHistoryTarget(null)}>
          <div className="history-modal" onClick={(e) => e.stopPropagation()}>
            <div className="history-modal-head">
              <h2>{historyTarget.firstName} {historyTarget.lastName}</h2>
              <button className="modal-close-btn" onClick={() => setHistoryTarget(null)}>
                <X size={16} strokeWidth={2} />
              </button>
            </div>
            <p className="history-modal-meta">
              {historyTarget.loyaltyPoints} pts · ${historyTarget.totalSpent.toFixed(2)} lifetime
            </p>
            <div className="history-modal-orders">
              {historyLoading && <p className="history-empty">Loading…</p>}
              {!historyLoading && historyOrders.length === 0 && (
                <p className="history-empty">No past orders yet.</p>
              )}
              {!historyLoading &&
                historyOrders.map((o) => (
                  <div className="history-order" key={o.id}>
                    <div className="history-order-top">
                      <span>{new Date(o.createdAt).toLocaleDateString()}</span>
                      <span>${o.total.toFixed(2)}</span>
                    </div>
                    <p className="history-order-items">
                      {o.items.map((i) => i.name).join(', ')}
                    </p>
                  </div>
                ))}
            </div>
          </div>
        </div>
      )}

      {editTarget && (
        <CustomerEditModal
          customer={editTarget}
          onClose={() => setEditTarget(null)}
          onSave={handleSaveEdit}
        />
      )}

      {showAddModal && (
        <CustomerEditModal
          customer={null}
          onClose={() => setShowAddModal(false)}
          onSave={handleSaveNew}
        />
      )}

      {showInsights && <CustomerInsightsModal onClose={() => setShowInsights(false)} />}
    </div>
  );
}
```

================================================================================

--- BATCH 47 | frontend/src/pages/DashboardPage.css ---
STATUS: ✅ REVIEWABLE (200 lines)
FILE: frontend/src/pages/DashboardPage.css
```css
.dashboard {
  height: 100%;
  overflow-y: auto;
  padding: 28px 32px 48px;
  background: var(--paper-dim);
}

.dashboard-loading {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 16px;
  color: rgba(28, 20, 16, 0.5);
  font-size: 14px;
}

.dashboard-loading-logo {
  width: 120px;
  height: auto;
  opacity: 0.5;
  animation: logo-pulse 1.6s ease-in-out infinite;
}

@keyframes logo-pulse {
  0%, 100% {
    opacity: 0.35;
    transform: scale(0.97);
  }
  50% {
    opacity: 0.65;
    transform: scale(1.0);
  }
}

.dashboard-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 24px;
}

.dashboard-eyebrow {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--rare);
}

.dashboard-header h1 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 30px;
  font-weight: 600;
  margin: 2px 0 0;
}

.print-btn {
  display: flex;
  align-items: center;
  gap: 6px;
  font-size: 13px;
  font-weight: 600;
  padding: 10px 16px;
  border-radius: 8px;
  border: 1.5px solid var(--ink);
  background: var(--paper);
  color: var(--ink);
}

.print-btn:hover {
  background: var(--ink);
  color: var(--paper);
}

.glance-cards {
  display: grid;
  grid-template-columns: repeat(4, 1fr);
  gap: 14px;
  margin-bottom: 24px;
}

.glance-card {
  display: flex;
  flex-direction: column;
  gap: 4px;
  background: var(--paper);
  border: 1.5px solid var(--line);
  border-radius: 12px;
  padding: 16px;
}

.glance-icon {
  color: var(--rare);
  margin-bottom: 4px;
}

.glance-label {
  font-size: 12px;
  font-weight: 600;
  color: rgba(28, 20, 16, 0.55);
}

.glance-value {
  font-size: 26px;
  font-weight: 700;
  font-family: var(--font-display);
}

.dashboard-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 16px;
}

.panel-wide {
  grid-column: 1 / -1;
}

.dashboard-panel {
  background: var(--paper);
  border: 1.5px solid var(--line);
  border-radius: 12px;
  padding: 18px 20px;
}

.dashboard-panel h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 15px;
  font-weight: 600;
  letter-spacing: 0.02em;
  margin: 0 0 14px;
}

.dashboard-subnote {
  font-size: 12px;
  color: rgba(28, 20, 16, 0.55);
  margin: -6px 0 10px;
}

.ranked-list {
  list-style: none;
  margin: 0;
  padding: 0;
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.ranked-list li {
  display: flex;
  align-items: center;
  gap: 10px;
  font-size: 13px;
  padding-bottom: 8px;
  border-bottom: 1px solid var(--line);
}

.ranked-list li:last-child {
  border-bottom: none;
  padding-bottom: 0;
}

.rank-num {
  width: 18px;
  font-weight: 700;
  color: rgba(28, 20, 16, 0.4);
  flex-shrink: 0;
}

.rank-name {
  flex: 1;
  font-weight: 500;
}

.rank-value {
  font-weight: 700;
}

@media print {
  .top-nav,
  .print-btn {
    display: none !important;
  }
  .dashboard {
    overflow: visible;
    height: auto;
    padding: 0;
  }
  .dashboard-grid {
    grid-template-columns: 1fr;
  }
  .glance-cards {
    grid-template-columns: repeat(4, 1fr);
  }
}
```

================================================================================

--- BATCH 48 | frontend/src/pages/DashboardPage.tsx ---
STATUS: ✅ REVIEWABLE (235 lines)
FILE: frontend/src/pages/DashboardPage.tsx
```tsx
import { useEffect, useState } from 'react';
import { Printer, TrendingUp, ShoppingBag, Receipt, Activity } from 'lucide-react';
import {
  BarChart,
  Bar,
  LineChart,
  Line,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer,
} from 'recharts';
import { fetchStats, type Stats } from '../api/stats';
import './DashboardPage.css';

const SHORT_CATEGORY: Record<string, string> = {
  'Glatt Kosher Beef (Fresh Cuts)': 'Beef',
  'Fresh Poultry (Chicken & Turkey)': 'Poultry',
  'Premium Lamb & Veal': 'Lamb & Veal',
  "Marinated & Oven-Ready Specials (Butcher's Prep)": 'Prepared',
  'Prepared Deli, Provisions & Shabbos Takeout': 'Deli',
  Beverages: 'Drinks',
};

const CATEGORY_COLORS = ['#a23b2e', '#5c6b47', '#d4a24c', '#6b5a8e'];

function shortCategory(name: string): string {
  return SHORT_CATEGORY[name] ?? name;
}

function formatDayLabel(dateStr: string): string {
  const d = new Date(dateStr + 'T00:00:00');
  return d.toLocaleDateString(undefined, { month: 'short', day: 'numeric' });
}

function formatHourLabel(hour: number): string {
  if (hour === 0) return '12am';
  if (hour === 12) return '12pm';
  return hour < 12 ? `${hour}am` : `${hour - 12}pm`;
}

export default function Dashboard() {
  const [stats, setStats] = useState<Stats | null>(null);
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    fetchStats()
      .then(setStats)
      .catch(console.error)
      .finally(() => setLoading(false));
  }, []);

  const handlePrint = () => window.print();

  if (loading) {
    return (
      <div className="dashboard-loading">
        <img src="/maple-logo.png" alt="" className="dashboard-loading-logo" />
        Loading reports…
      </div>
    );
  }

  if (!stats) {
    return <div className="dashboard-loading">Could not load dashboard data.</div>;
  }

  const peakHour = stats.busiestHours.reduce(
    (best, h) => (h.count > best.count ? h : best),
    stats.busiestHours[0]
  );

  return (
    <div className="dashboard">
      <header className="dashboard-header">
        <div>
          <span className="dashboard-eyebrow">Maple Kosher Meats</span>
          <h1>Dashboard</h1>
        </div>
        <button className="print-btn" onClick={handlePrint}>
          <Printer size={15} strokeWidth={2} />
          Print report
        </button>
      </header>

      <div className="glance-cards">
        <div className="glance-card">
          <div className="glance-icon"><Receipt size={16} strokeWidth={2} /></div>
          <span className="glance-label">Today's revenue</span>
          <span className="glance-value">${stats.todayRevenue.toFixed(2)}</span>
        </div>
        <div className="glance-card">
          <div className="glance-icon"><ShoppingBag size={16} strokeWidth={2} /></div>
          <span className="glance-label">Orders today</span>
          <span className="glance-value">{stats.todayOrderCount}</span>
        </div>
        <div className="glance-card">
          <div className="glance-icon"><TrendingUp size={16} strokeWidth={2} /></div>
          <span className="glance-label">Avg ticket</span>
          <span className="glance-value">${stats.avgTicket.toFixed(2)}</span>
        </div>
        <div className="glance-card">
          <div className="glance-icon"><Activity size={16} strokeWidth={2} /></div>
          <span className="glance-label">Active orders</span>
          <span className="glance-value">{stats.activeOrders}</span>
        </div>
      </div>

      <div className="dashboard-grid">
        <section className="dashboard-panel panel-wide">
          <h2>Revenue — last 30 days</h2>
          <ResponsiveContainer width="100%" height={220}>
            <BarChart data={stats.revenueByDay}>
              <CartesianGrid strokeDasharray="3 3" stroke="rgba(28,20,16,0.08)" vertical={false} />
              <XAxis
                dataKey="date"
                tickFormatter={formatDayLabel}
                tick={{ fontSize: 11, fill: 'rgba(28,20,16,0.5)' }}
                interval={Math.ceil(stats.revenueByDay.length / 10)}
              />
              <YAxis tick={{ fontSize: 11, fill: 'rgba(28,20,16,0.5)' }} width={50} />
              <Tooltip
                formatter={(value: number) => [`$${value.toFixed(2)}`, 'Revenue']}
                labelFormatter={(label) => formatDayLabel(label as string)}
              />
              <Bar dataKey="revenue" fill="#a23b2e" radius={[3, 3, 0, 0]} />
            </BarChart>
          </ResponsiveContainer>
        </section>

        <section className="dashboard-panel panel-wide">
          <h2>Category trend — last 30 days</h2>
          <ResponsiveContainer width="100%" height={220}>
            <LineChart data={stats.categoryTrend}>
              <CartesianGrid strokeDasharray="3 3" stroke="rgba(28,20,16,0.08)" vertical={false} />
              <XAxis
                dataKey="date"
                tickFormatter={formatDayLabel}
                tick={{ fontSize: 11, fill: 'rgba(28,20,16,0.5)' }}
                interval={Math.ceil(stats.categoryTrend.length / 10)}
              />
              <YAxis tick={{ fontSize: 11, fill: 'rgba(28,20,16,0.5)' }} width={50} />
              <Tooltip
                formatter={(value: number) => `$${Number(value).toFixed(2)}`}
                labelFormatter={(label) => formatDayLabel(label as string)}
              />
              <Legend
                formatter={(value) => shortCategory(value as string)}
                wrapperStyle={{ fontSize: 12, paddingTop: 8 }}
                iconType="line"
                iconSize={14}
              />
              {stats.topCategoryNames.map((cat, i) => (
                <Line
                  key={cat}
                  type="monotone"
                  dataKey={cat}
                  name={cat}
                  stroke={CATEGORY_COLORS[i % CATEGORY_COLORS.length]}
                  strokeWidth={2}
                  dot={false}
                />
              ))}
            </LineChart>
          </ResponsiveContainer>
        </section>

        <section className="dashboard-panel">
          <h2>Top items — by revenue</h2>
          <ol className="ranked-list">
            {stats.topByRevenue.map((item, i) => (
              <li key={item.name}>
                <span className="rank-num">{i + 1}</span>
                <span className="rank-name">{item.name}</span>
                <span className="rank-value">${item.revenue.toFixed(2)}</span>
              </li>
            ))}
          </ol>
        </section>

        <section className="dashboard-panel">
          <h2>Top items — by units sold</h2>
          <ol className="ranked-list">
            {stats.topByUnits.map((item, i) => (
              <li key={item.name}>
                <span className="rank-num">{i + 1}</span>
                <span className="rank-name">{item.name}</span>
                <span className="rank-value">{item.unitsSold}</span>
              </li>
            ))}
          </ol>
        </section>

        <section className="dashboard-panel">
          <h2>Revenue by category</h2>
          <ol className="ranked-list">
            {stats.revenueByCategory.map((c, i) => (
              <li key={c.category}>
                <span className="rank-num">{i + 1}</span>
                <span className="rank-name">{c.category}</span>
                <span className="rank-value">${c.revenue.toFixed(2)}</span>
              </li>
            ))}
          </ol>
        </section>

        <section className="dashboard-panel">
          <h2>Busiest hours</h2>
          <p className="dashboard-subnote">
            Peak: {formatHourLabel(peakHour.hour)} ({peakHour.count} orders)
          </p>
          <ResponsiveContainer width="100%" height={160}>
            <BarChart data={stats.busiestHours}>
              <XAxis
                dataKey="hour"
                tickFormatter={formatHourLabel}
                tick={{ fontSize: 10, fill: 'rgba(28,20,16,0.5)' }}
                interval={2}
              />
              <YAxis tick={{ fontSize: 10, fill: 'rgba(28,20,16,0.5)' }} width={30} />
              <Tooltip
                formatter={(value: number) => [value, 'Orders']}
                labelFormatter={(label) => formatHourLabel(label as number)}
              />
              <Bar dataKey="count" fill="#5c6b47" radius={[3, 3, 0, 0]} />
            </BarChart>
          </ResponsiveContainer>
        </section>
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 49 | frontend/src/pages/MenuPage.css ---
STATUS: ✅ REVIEWABLE (98 lines)
FILE: frontend/src/pages/MenuPage.css
```css
.menu-page {
  padding: 34px 22px 60px;
  background: #f7f2ea;
  min-height: 100%;
  color: #1b120d;
}

.menu-page-header {
  margin-bottom: 24px;
}

.menu-eyebrow {
  margin: 0 0 10px;
  color: #a23b2e;
  font-size: 11px;
  font-weight: 800;
  letter-spacing: 0.18em;
  text-transform: uppercase;
}

.menu-page h1 {
  margin: 0;
  font-size: 42px;
  line-height: 1;
  text-transform: uppercase;
}

.menu-subtitle {
  margin: 10px 0 0;
  color: #6d625a;
}

.menu-add-card,
.menu-section {
  border: 1px solid #ded5c9;
  background: #fffaf2;
  border-radius: 14px;
  padding: 16px;
  margin-bottom: 18px;
}

.menu-add-card h2,
.menu-section h2 {
  margin: 0 0 14px;
  font-size: 16px;
}

.menu-add-grid,
.menu-row {
  display: grid;
  grid-template-columns: 2fr 1.2fr 100px 100px 100px 90px;
  gap: 8px;
}

.menu-table {
  display: grid;
  gap: 8px;
}

.menu-row {
  grid-template-columns: 2fr 100px 110px 100px 90px 80px;
  align-items: center;
}

.menu-row.inactive {
  opacity: 0.45;
}

.menu-page input,
.menu-page select,
.menu-page button {
  height: 38px;
  border: 1px solid #ded5c9;
  border-radius: 9px;
  background: #fff;
  padding: 0 10px;
  font: inherit;
}

.menu-page button {
  cursor: pointer;
  font-weight: 800;
  background: #1b120d;
  color: #fff;
}

.menu-page button:disabled {
  cursor: not-allowed;
  opacity: 0.35;
}

@media (max-width: 850px) {
  .menu-add-grid,
  .menu-row {
    grid-template-columns: 1fr;
  }
}
```

================================================================================

--- BATCH 50 | frontend/src/pages/MenuPage.tsx ---
STATUS: ✅ REVIEWABLE (109 lines)
FILE: frontend/src/pages/MenuPage.tsx
```tsx
import { useMemo, useState } from 'react';
import type { MenuItem, SoldBy } from '@groundup/shared-types';
import { useMenuContext } from '../providers/MenuProvider';
import './MenuPage.css';

const blankItem = {
  name: '',
  category: 'Specials',
  price: 0,
  unit: 'per lb',
  soldBy: 'weight' as SoldBy,
  isActive: true,
};

export default function MenuPage() {
  const { menu, categories, loading, error, saveMenuItem, addMenuItem } = useMenuContext();
  const [draft, setDraft] = useState<Record<string, MenuItem>>({});
  const [newItem, setNewItem] = useState(blankItem);

  const grouped = useMemo(() => {
    return categories.map((category) => ({
      category,
      items: menu.filter((item) => item.category === category),
    }));
  }, [categories, menu]);

  const getItem = (item: MenuItem) => draft[item.id] ?? item;

  const updateDraft = (item: MenuItem, changes: Partial<MenuItem>) => {
    setDraft((prev) => ({
      ...prev,
      [item.id]: { ...getItem(item), ...changes },
    }));
  };

  const save = async (item: MenuItem) => {
    await saveMenuItem(item.id, getItem(item));
    setDraft((prev) => {
      const next = { ...prev };
      delete next[item.id];
      return next;
    });
  };

  const addSpecial = async () => {
    if (!newItem.name.trim()) return;
    await addMenuItem(newItem);
    setNewItem(blankItem);
  };

  if (loading) return <main className="menu-page"><p>Loading menu…</p></main>;
  if (error) return <main className="menu-page"><p>{error}</p></main>;

  return (
    <main className="menu-page">
      <header className="menu-page-header">
        <div>
          <p className="menu-eyebrow">Maple Kosher Meats</p>
          <h1>Menu</h1>
          <p className="menu-subtitle">Edit prices, availability, units, and specials.</p>
        </div>
      </header>

      <section className="menu-add-card">
        <h2>Add Special</h2>
        <div className="menu-add-grid">
          <input placeholder="Item name" value={newItem.name} onChange={(e) => setNewItem({ ...newItem, name: e.target.value })} />
          <input placeholder="Category" value={newItem.category} onChange={(e) => setNewItem({ ...newItem, category: e.target.value })} />
          <input type="number" step="0.01" value={newItem.price} onChange={(e) => setNewItem({ ...newItem, price: Number(e.target.value) })} />
          <input value={newItem.unit} onChange={(e) => setNewItem({ ...newItem, unit: e.target.value })} />
          <select value={newItem.soldBy} onChange={(e) => setNewItem({ ...newItem, soldBy: e.target.value as SoldBy })}>
            <option value="weight">weight</option>
            <option value="each">each</option>
          </select>
          <button onClick={addSpecial}>Add</button>
        </div>
      </section>

      {grouped.map(({ category, items }) => (
        <section className="menu-section" key={category}>
          <h2>{category}</h2>
          <div className="menu-table">
            {items.map((item) => {
              const current = getItem(item);
              const changed = Boolean(draft[item.id]);

              return (
                <div className={`menu-row ${!current.isActive ? 'inactive' : ''}`} key={item.id}>
                  <input value={current.name} onChange={(e) => updateDraft(item, { name: e.target.value })} />
                  <input type="number" step="0.01" value={current.price} onChange={(e) => updateDraft(item, { price: Number(e.target.value) })} />
                  <input value={current.unit} onChange={(e) => updateDraft(item, { unit: e.target.value })} />
                  <select value={current.soldBy} onChange={(e) => updateDraft(item, { soldBy: e.target.value as SoldBy })}>
                    <option value="weight">weight</option>
                    <option value="each">each</option>
                  </select>
                  <button onClick={() => updateDraft(item, { isActive: !current.isActive })}>
                    {current.isActive ? 'Active' : 'Inactive'}
                  </button>
                  <button disabled={!changed} onClick={() => save(item)}>Save</button>
                </div>
              );
            })}
          </div>
        </section>
      ))}
    </main>
  );
}
```

================================================================================

--- BATCH 51 | frontend/src/pages/OnlineOrderPage.css ---
STATUS: 🚨 VERY LARGE (551 lines)
FILE: frontend/src/pages/OnlineOrderPage.css
```css
.online-order {
  display: flex;
  height: 100%;
  overflow: hidden;
  position: relative;
}

.online-order::before {
  content: '';
  position: absolute;
  inset: 0;
  background-image: url('/maple-logo.png');
  background-repeat: no-repeat;
  background-position: center 78%;
  background-size: 32% auto;
  opacity: 0.04;
  pointer-events: none;
  z-index: 0;
}

.online-order > * {
  position: relative;
  z-index: 1;
}

/* Identify / Checkout / Confirmed full-screen states */
.identify-screen,
.checkout-screen,
.confirm-screen {
  max-width: 440px;
  margin: 0 auto;
  padding: 60px 24px;
  width: 100%;
  display: flex;
  flex-direction: column;
  gap: 14px;
}

.identify-screen h1,
.checkout-screen h1,
.confirm-screen h1 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 26px;
  margin: 0 0 4px;
  text-align: center;
}

.identify-screen p {
  text-align: center;
  color: rgba(28, 20, 16, 0.6);
  font-size: 13px;
  margin: 0 0 8px;
}

.identify-screen input,
.checkout-note-input,
.address-fields input {
  width: 100%;
  font-size: 14px;
  padding: 12px 14px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  font-family: var(--font-body);
  outline: none;
}

.identify-screen input:focus,
.checkout-note-input:focus,
.address-fields input:focus {
  border-color: var(--rare);
}

.identify-btn,
.online-checkout-btn,
.confirm-new-order-btn {
  padding: 14px;
  border-radius: 10px;
  border: none;
  background: var(--rare);
  color: var(--paper);
  font-family: var(--font-display);
  text-transform: uppercase;
  letter-spacing: 0.04em;
  font-size: 14px;
  font-weight: 600;
}

.identify-btn:disabled,
.online-checkout-btn:disabled {
  background: var(--line);
  color: rgba(28, 20, 16, 0.4);
}

.checkout-greeting {
  text-align: center;
  font-size: 13px;
  color: var(--sage);
  font-weight: 600;
  margin: -6px 0 6px;
}

.address-fields {
  display: flex;
  flex-direction: column;
  gap: 8px;
}

.address-row {
  display: flex;
  gap: 8px;
}

.checkout-summary {
  border-top: 1.5px solid var(--line);
  border-bottom: 1.5px solid var(--line);
  padding: 12px 0;
  margin: 6px 0;
}

.checkout-line {
  display: flex;
  justify-content: space-between;
  font-size: 13px;
  padding: 4px 0;
}

.checkout-total-row {
  display: flex;
  justify-content: space-between;
  font-weight: 700;
  font-size: 16px;
  padding-top: 10px;
}

.checkout-actions {
  display: flex;
  gap: 10px;
}

.checkout-actions .btn-secondary,
.checkout-actions .btn-primary {
  flex: 1;
  padding: 14px;
  border-radius: 10px;
  font-weight: 700;
  font-size: 13px;
}

.checkout-actions .btn-secondary {
  border: 1.5px solid var(--ink);
  background: transparent;
}

.checkout-actions .btn-primary {
  border: none;
  background: var(--ink);
  color: var(--paper);
}

.checkout-actions .btn-primary:disabled {
  background: var(--line);
  color: rgba(28, 20, 16, 0.4);
}

.confirm-screen {
  align-items: center;
  text-align: center;
}

.confirm-icon {
  width: 56px;
  height: 56px;
  border-radius: 50%;
  background: rgba(92, 107, 71, 0.15);
  color: var(--sage);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-bottom: 6px;
}

.confirm-screen p {
  color: rgba(28, 20, 16, 0.6);
  font-size: 14px;
}

.confirm-new-order-btn {
  margin-top: 12px;
}

/* Browse + cart layout */
.online-browse-pane {
  flex: 1;
  overflow-y: auto;
  padding: 24px 28px;
}

.online-header {
  margin-bottom: 16px;
}

.online-eyebrow {
  font-size: 11px;
  font-weight: 700;
  letter-spacing: 0.12em;
  text-transform: uppercase;
  color: var(--rare);
}

.online-header h1 {
  font-family: var(--font-display);
  font-size: 28px;
  font-weight: 600;
  text-transform: uppercase;
  margin: 2px 0 0;
}

.online-item-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 12px;
}

.online-item-tile {
  display: flex;
  flex-direction: column;
  align-items: flex-start;
  gap: 4px;
  padding: 16px;
  border: 1.5px solid var(--line);
  border-radius: 10px;
  background: var(--paper);
  text-align: left;
}

.online-item-tile:hover {
  border-color: var(--rare);
}

.online-item-name {
  font-weight: 700;
  font-size: 14px;
}

.online-item-price {
  font-size: 13px;
  color: var(--rare);
  font-weight: 600;
}

/* Tier selection modal */
.tier-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
}

.tier-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 28px;
  width: 340px;
  box-shadow: 0 12px 32px var(--shadow);
}

.tier-modal h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 18px;
  margin: 0 0 16px;
}

.tier-options {
  display: flex;
  flex-direction: column;
  gap: 8px;
  margin-bottom: 16px;
}

.tier-option {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 8px;
  padding: 12px 14px;
  border-radius: 8px;
  border: 1.5px solid var(--line);
  background: var(--paper);
}

.tier-option:hover {
  border-color: var(--rare);
}

.tier-label {
  font-weight: 700;
  font-size: 13px;
  flex: 1;
  text-align: left;
}

.tier-weight {
  font-size: 11px;
  color: rgba(28, 20, 16, 0.5);
}

.tier-price {
  font-weight: 700;
  font-size: 13px;
  color: var(--rare);
}

/* Cart sidebar */
.online-cart-pane {
  width: 320px;
  flex-shrink: 0;
  background: var(--paper-dim);
  border-left: 2px solid var(--ink);
  padding: 24px 20px;
  display: flex;
  flex-direction: column;
}

.online-cart-pane h2 {
  display: flex;
  align-items: center;
  gap: 8px;
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 16px;
  margin: 0 0 16px;
}

.online-cart-lines {
  flex: 1;
  overflow-y: auto;
}

.online-cart-line {
  display: flex;
  align-items: center;
  gap: 8px;
  padding: 8px 0;
  border-bottom: 1px solid var(--line);
}

.online-cart-line-name {
  flex: 1;
  font-size: 13px;
  font-weight: 500;
}

.online-cart-line-total {
  font-size: 13px;
  font-weight: 700;
}

.online-cart-line button {
  background: transparent;
  border: none;
  font-size: 18px;
  color: var(--rare);
}

.online-cart-footer {
  display: flex;
  justify-content: space-between;
  font-weight: 700;
  font-size: 18px;
  padding: 14px 0;
  border-top: 1.5px solid var(--ink);
}

/* Header with customer badge */
.online-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  margin-bottom: 16px;
}

.customer-badge {
  display: flex;
  align-items: center;
  gap: 10px;
}

.customer-badge-info {
  display: flex;
  flex-direction: column;
  align-items: flex-end;
}

.customer-badge-name {
  font-size: 13px;
  font-weight: 700;
}

.customer-badge-points {
  font-size: 11px;
  color: var(--rare);
  font-weight: 600;
}

.history-pill {
  display: flex;
  align-items: center;
  gap: 5px;
  font-size: 11px;
  font-weight: 700;
  text-transform: uppercase;
  padding: 7px 12px;
  border-radius: 999px;
  border: none;
  background: var(--ink);
  color: var(--paper);
}

/* Tier modal note + action layout */
.tier-note-input {
  width: 100%;
  font-size: 13px;
  padding: 10px 12px;
  border: 1.5px solid var(--line);
  border-radius: 8px;
  margin-bottom: 12px;
  font-family: var(--font-body);
  outline: none;
}

.tier-note-input:focus {
  border-color: var(--rare);
}

.tier-option.selected {
  border-color: var(--rare);
  background: rgba(162, 59, 46, 0.06);
}

.tier-modal-actions {
  display: flex;
  gap: 10px;
}

.tier-modal-actions .btn-secondary,
.tier-modal-actions .btn-primary {
  flex: 1;
  padding: 12px;
  border-radius: 8px;
  font-weight: 700;
  font-size: 13px;
}

.tier-modal-actions .btn-secondary {
  border: 1.5px solid var(--ink);
  background: transparent;
}

.tier-modal-actions .btn-primary {
  border: none;
  background: var(--ink);
  color: var(--paper);
}

.tier-modal-actions .btn-primary:disabled {
  background: var(--line);
  color: rgba(28, 20, 16, 0.4);
}

/* Cart line note display */
.online-cart-line-info {
  flex: 1;
  display: flex;
  flex-direction: column;
}

.online-cart-line-note {
  font-size: 11px;
  font-style: italic;
  color: rgba(28, 20, 16, 0.5);
}

/* History modal */
.history-modal-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(28, 20, 16, 0.45);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 50;
}

.history-modal {
  background: var(--paper);
  border-radius: 14px;
  padding: 28px;
  width: 360px;
  max-height: 70vh;
  overflow-y: auto;
  box-shadow: 0 12px 32px var(--shadow);
}

.history-modal h2 {
  font-family: var(--font-display);
  text-transform: uppercase;
  font-size: 22px;
  margin: 0 0 4px;
}

.history-modal-meta {
  color: var(--rare);
  font-weight: 600;
  font-size: 13px;
  margin: 0 0 16px;
}

.history-empty {
  color: rgba(28, 20, 16, 0.45);
  font-size: 13px;
  font-style: italic;
}

.history-order {
  padding: 10px 0;
  border-bottom: 1px solid var(--line);
}

.history-order:last-child {
  border-bottom: none;
}

.history-order-top {
  display: flex;
  justify-content: space-between;
  font-weight: 700;
  font-size: 13px;
  margin-bottom: 4px;
}

.history-order-items {
  font-size: 12px;
  color: rgba(28, 20, 16, 0.6);
  margin: 0;
}
```

================================================================================

--- BATCH 52 | frontend/src/pages/OnlineOrderPage.tsx ---
STATUS: ⚠️ LARGE (417 lines)
FILE: frontend/src/pages/OnlineOrderPage.tsx
```tsx
import { useState, useMemo } from 'react';
import { Beef, Bird, Drumstick, Flame, Sandwich, CupSoda, Store, Truck, ShoppingCart, Check, History } from 'lucide-react';
import type { MenuItem, OrderLineItem, SizeTier, Address, Order, CustomerWithStats } from '@groundup/shared-types';
import { MENU_TIERS } from '../data/menuTiers';
import { createOnlineOrder } from '../api/online-orders';
import { fetchCustomerOrders } from '../api/orders';
import { useMenu } from '../hooks/useMenu';
import { useCustomers } from '../hooks/useCustomers';
import './OnlineOrderPage.css';

const CATEGORY_META: Record<string, { label: string; icon: typeof Beef }> = {
  'Glatt Kosher Beef (Fresh Cuts)': { label: 'Beef', icon: Beef },
  'Fresh Poultry (Chicken & Turkey)': { label: 'Poultry', icon: Bird },
  'Premium Lamb & Veal': { label: 'Lamb & Veal', icon: Drumstick },
  "Marinated & Oven-Ready Specials (Butcher's Prep)": { label: 'Prepared', icon: Flame },
  'Prepared Deli, Provisions & Shabbos Takeout': { label: 'Deli', icon: Sandwich },
  Beverages: { label: 'Drinks', icon: CupSoda },
};

const tiersByItemId = new Map(MENU_TIERS.map((t) => [t.menuItemId, t.tiers]));

type Step = 'browse' | 'checkout' | 'confirmed';

export default function OnlineOrder() {
  const { menu } = useMenu();
  const { customers } = useCustomers();
  const [step, setStep] = useState<Step>('browse');
  const [activeCategory, setActiveCategory] = useState<string>(menu[0]?.category ?? '');
  const [cart, setCart] = useState<OrderLineItem[]>([]);
  const [pendingItem, setPendingItem] = useState<MenuItem | null>(null);
  const [selectedTier, setSelectedTier] = useState<SizeTier | null>(null);
  const [itemNoteInput, setItemNoteInput] = useState('');

  const categories = useMemo(() => Array.from(new Set(menu.map((m) => m.category))), [menu]);

  // Set the initial active category once the menu has loaded, without
  // needing a separate fetch effect — the shared hook handles fetching.
  useMemo(() => {
    if (menu.length > 0 && !activeCategory) {
      setActiveCategory(menu[0].category);
    }
  }, [menu, activeCategory]);

  const [identified, setIdentified] = useState(false);
  const [emailInput, setEmailInput] = useState('');
  const [matchedCustomer, setMatchedCustomer] = useState<CustomerWithStats | null>(null);
  const [showHistory, setShowHistory] = useState(false);
  const [historyOrders, setHistoryOrders] = useState<Order[]>([]);
  const [historyLoading, setHistoryLoading] = useState(false);

  const openHistory = async () => {
    if (!matchedCustomer) return;
    setShowHistory(true);
    setHistoryLoading(true);
    try {
      const orders = await fetchCustomerOrders(matchedCustomer.customerId);
      setHistoryOrders(orders);
    } catch (err) {
      console.error(err);
    } finally {
      setHistoryLoading(false);
    }
  };

  const [fulfillment, setFulfillment] = useState<'pickup' | 'delivery'>('pickup');
  const [address, setAddress] = useState<Address>({ street: '', city: '', state: 'NJ', zip: '' });
  const [orderNote, setOrderNote] = useState('');
  const [placing, setPlacing] = useState(false);
  const [confirmedOrderId, setConfirmedOrderId] = useState('');

  const itemsInCategory = menu.filter((m) => m.category === activeCategory && m.isActive);
  const cartTotal = Math.round(cart.reduce((s, l) => s + l.lineTotal, 0) * 100) / 100;

  const pendingTiers: SizeTier[] = useMemo(() => {
    if (!pendingItem) return [];
    return tiersByItemId.get(pendingItem.id) ?? [];
  }, [pendingItem]);

  const handleIdentify = () => {
    const match = customers.find((c) => c.email.toLowerCase() === emailInput.trim().toLowerCase());
    setMatchedCustomer(match ?? null);
    setIdentified(true);
  };

  const openItem = (item: MenuItem) => {
    setPendingItem(item);
    setSelectedTier(null);
    setItemNoteInput('');
  };

  const confirmAddToCart = () => {
    if (!pendingItem || !selectedTier) return;
    setCart((prev) => [
      ...prev,
      {
        menuItemId: pendingItem.id,
        name: `${pendingItem.name} (${selectedTier.label})`,
        quantity: 1,
        unitPrice: selectedTier.price,
        lineTotal: selectedTier.price,
        notes: itemNoteInput.trim() || undefined,
      },
    ]);
    setPendingItem(null);
    setSelectedTier(null);
    setItemNoteInput('');
  };

  const removeLine = (index: number) => {
    setCart((prev) => prev.filter((_, i) => i !== index));
  };

  const placeOrder = async () => {
    setPlacing(true);
    try {
      const customerName = matchedCustomer
        ? `${matchedCustomer.firstName} ${matchedCustomer.lastName}`
        : emailInput.trim() || 'Online Guest';
      const order = await createOnlineOrder({
        customerName,
        customerId: matchedCustomer?.customerId ?? null,
        items: cart,
        fulfillment,
        deliveryAddress: fulfillment === 'delivery' ? address : undefined,
        notes: orderNote.trim() || undefined,
      });
      setConfirmedOrderId(order.id);
      setStep('confirmed');
    } catch (err) {
      console.error(err);
      alert('Could not place order — is the server running?');
    } finally {
      setPlacing(false);
    }
  };

  if (step === 'confirmed') {
    return (
      <div className="online-order">
        <div className="confirm-screen">
          <div className="confirm-icon"><Check size={32} strokeWidth={2.5} /></div>
          <h1>Order placed</h1>
          <p>Order #{confirmedOrderId.replace('ord-', '')} — we'll have it ready for {fulfillment}.</p>
          <button
            className="confirm-new-order-btn"
            onClick={() => {
              setStep('browse');
              setCart([]);
              setOrderNote('');
            }}
          >
            Place another order
          </button>
        </div>
      </div>
    );
  }

  if (!identified) {
    return (
      <div className="online-order">
        <div className="identify-screen">
          <h1>Maple Kosher Meats</h1>
          <p>Enter your email to continue, or check out as a guest.</p>
          <input
            type="email"
            placeholder="you@example.com"
            value={emailInput}
            onChange={(e) => setEmailInput(e.target.value)}
            onKeyDown={(e) => e.key === 'Enter' && handleIdentify()}
          />
          <button className="identify-btn" onClick={handleIdentify}>
            Continue
          </button>
        </div>
      </div>
    );
  }

  if (step === 'checkout') {
    return (
      <div className="online-order">
        <div className="checkout-screen">
          <h1>Checkout</h1>
          {matchedCustomer && (
            <p className="checkout-greeting">
              Welcome back, {matchedCustomer.firstName} — {matchedCustomer.loyaltyPoints} pts
            </p>
          )}

          <div className="fulfillment-toggle">
            <button
              className={`fulfillment-option ${fulfillment === 'pickup' ? 'active' : ''}`}
              onClick={() => setFulfillment('pickup')}
            >
              <Store size={14} strokeWidth={2} /> Pickup
            </button>
            <button
              className={`fulfillment-option ${fulfillment === 'delivery' ? 'active' : ''}`}
              onClick={() => setFulfillment('delivery')}
            >
              <Truck size={14} strokeWidth={2} /> Delivery
            </button>
          </div>

          {fulfillment === 'delivery' && (
            <div className="address-fields">
              <input
                placeholder="Street address"
                value={address.street}
                onChange={(e) => setAddress({ ...address, street: e.target.value })}
              />
              <div className="address-row">
                <input
                  placeholder="City"
                  value={address.city}
                  onChange={(e) => setAddress({ ...address, city: e.target.value })}
                />
                <input
                  placeholder="Zip"
                  value={address.zip}
                  onChange={(e) => setAddress({ ...address, zip: e.target.value })}
                />
              </div>
            </div>
          )}

          <input
            className="checkout-note-input"
            placeholder="Order note (optional)"
            value={orderNote}
            onChange={(e) => setOrderNote(e.target.value)}
          />

          <div className="checkout-summary">
            {cart.map((line, i) => (
              <div className="checkout-line" key={i}>
                <span>{line.name}</span>
                <span>${line.lineTotal.toFixed(2)}</span>
              </div>
            ))}
            <div className="checkout-total-row">
              <span>Total</span>
              <span>${cartTotal.toFixed(2)}</span>
            </div>
          </div>

          <div className="checkout-actions">
            <button className="btn-secondary" onClick={() => setStep('browse')}>
              Back to menu
            </button>
            <button
              className="btn-primary"
              disabled={placing || (fulfillment === 'delivery' && !address.street)}
              onClick={placeOrder}
            >
              {placing ? 'Placing order…' : `Place order — $${cartTotal.toFixed(2)}`}
            </button>
          </div>
        </div>
      </div>
    );
  }

  return (
    <div className="online-order">
      <div className="online-browse-pane">
        <header className="online-header">
          <div>
            <span className="online-eyebrow">Maple Kosher Meats</span>
            <h1>Order Online</h1>
          </div>
          {matchedCustomer && (
            <div className="customer-badge">
              <div className="customer-badge-info">
                <span className="customer-badge-name">{matchedCustomer.firstName} {matchedCustomer.lastName}</span>
                <span className="customer-badge-points">{matchedCustomer.loyaltyPoints} pts</span>
              </div>
              <button className="history-pill" onClick={openHistory}>
                <History size={13} strokeWidth={2} />
                Past orders
              </button>
            </div>
          )}
        </header>
        <div className="category-tabs">
          {categories.map((cat) => {
            const meta = CATEGORY_META[cat] ?? { label: cat, icon: Beef };
            const Icon = meta.icon;
            return (
              <button
                key={cat}
                className={`category-tab ${activeCategory === cat ? 'active' : ''}`}
                onClick={() => setActiveCategory(cat)}
                title={cat}
              >
                <Icon size={16} strokeWidth={2} />
                <span>{meta.label}</span>
              </button>
            );
          })}
        </div>
        <div className="online-item-grid">
          {itemsInCategory.map((item) => {
            const tiers = tiersByItemId.get(item.id) ?? [];
            const lowPrice = tiers.length > 0 ? Math.min(...tiers.map((t) => t.price)) : item.price;
            return (
              <button key={item.id} className="online-item-tile" onClick={() => openItem(item)}>
                <span className="online-item-name">{item.name}</span>
                <span className="online-item-price">From ${lowPrice.toFixed(2)}</span>
              </button>
            );
          })}
        </div>
      </div>

      {pendingItem && (
        <div className="tier-modal-backdrop" onClick={() => setPendingItem(null)}>
          <div className="tier-modal" onClick={(e) => e.stopPropagation()}>
            <h2>{pendingItem.name}</h2>
            <div className="tier-options">
              {pendingTiers.map((tier) => (
                <button
                  key={tier.label}
                  className={`tier-option ${selectedTier?.label === tier.label ? 'selected' : ''}`}
                  onClick={() => setSelectedTier(tier)}
                >
                  <span className="tier-label">{tier.label}</span>
                  {tier.weightLbs && <span className="tier-weight">~{tier.weightLbs} lb</span>}
                  <span className="tier-price">${tier.price.toFixed(2)}</span>
                </button>
              ))}
            </div>
            {selectedTier && (
              <input
                type="text"
                className="tier-note-input"
                placeholder="Note (e.g. cut thin, extra fatty)"
                value={itemNoteInput}
                onChange={(e) => setItemNoteInput(e.target.value)}
              />
            )}
            <div className="tier-modal-actions">
              <button className="btn-secondary" onClick={() => setPendingItem(null)}>
                Cancel
              </button>
              <button className="btn-primary" disabled={!selectedTier} onClick={confirmAddToCart}>
                Add to cart
              </button>
            </div>
          </div>
        </div>
      )}

      {showHistory && matchedCustomer && (
        <div className="history-modal-backdrop" onClick={() => setShowHistory(false)}>
          <div className="history-modal" onClick={(e) => e.stopPropagation()}>
            <h2>{matchedCustomer.firstName} {matchedCustomer.lastName}</h2>
            <p className="history-modal-meta">
              {matchedCustomer.loyaltyPoints} pts
              {matchedCustomer.dietaryNotes && ` · ${matchedCustomer.dietaryNotes}`}
            </p>
            <div className="history-modal-orders">
              {historyLoading && <p className="history-empty">Loading…</p>}
              {!historyLoading && historyOrders.length === 0 && (
                <p className="history-empty">No past orders yet.</p>
              )}
              {!historyLoading &&
                historyOrders.map((o) => (
                  <div className="history-order" key={o.id}>
                    <div className="history-order-top">
                      <span>{new Date(o.createdAt).toLocaleDateString()}</span>
                      <span>${o.total.toFixed(2)}</span>
                    </div>
                    <p className="history-order-items">
                      {o.items.map((i) => i.name).join(', ')}
                    </p>
                  </div>
                ))}
            </div>
          </div>
        </div>
      )}

      <div className="online-cart-pane">
        <h2>
          <ShoppingCart size={16} strokeWidth={2} /> Your order
        </h2>
        <div className="online-cart-lines">
          {cart.length === 0 && <p className="cart-empty">Nothing in your cart yet.</p>}
          {cart.map((line, i) => (
            <div className="online-cart-line" key={i}>
              <div className="online-cart-line-info">
                <span className="online-cart-line-name">{line.name}</span>
                {line.notes && <span className="online-cart-line-note">"{line.notes}"</span>}
              </div>
              <span className="online-cart-line-total">${line.lineTotal.toFixed(2)}</span>
              <button onClick={() => removeLine(i)} aria-label="Remove">×</button>
            </div>
          ))}
        </div>
        <div className="online-cart-footer">
          <span>Total</span>
          <span>${cartTotal.toFixed(2)}</span>
        </div>
        <button
          className="online-checkout-btn"
          disabled={cart.length === 0}
          onClick={() => setStep('checkout')}
        >
          Checkout
        </button>
      </div>
    </div>
  );
}
```

================================================================================

--- BATCH 53 | frontend/src/providers/MenuProvider.tsx ---
STATUS: ✅ REVIEWABLE (89 lines)
FILE: frontend/src/providers/MenuProvider.tsx
```tsx
import { createContext, useContext, useEffect, useMemo, useState } from 'react';
import type { MenuItem } from '@groundup/shared-types';
import {
  createMenuItem,
  fetchAdminMenu,
  updateMenuItem,
  type NewMenuItemInput,
} from '../api/menu';

type MenuContextValue = {
  menu: MenuItem[];
  activeMenu: MenuItem[];
  categories: string[];
  loading: boolean;
  error: string | null;
  refreshMenu: () => Promise<void>;
  saveMenuItem: (id: string, changes: Partial<MenuItem>) => Promise<void>;
  addMenuItem: (data: NewMenuItemInput) => Promise<void>;
};

const MenuContext = createContext<MenuContextValue | null>(null);

export function MenuProvider({ children }: { children: React.ReactNode }) {
  const [menu, setMenu] = useState<MenuItem[]>([]);
  const [loading, setLoading] = useState(true);
  const [error, setError] = useState<string | null>(null);

  const refreshMenu = async () => {
    setLoading(true);
    setError(null);
    try {
      const data = await fetchAdminMenu();
      setMenu(data);
    } catch (err) {
      console.error(err);
      setError('Could not load menu');
    } finally {
      setLoading(false);
    }
  };

  useEffect(() => {
    refreshMenu();
  }, []);

  const activeMenu = useMemo(
    () => menu.filter((item) => item.isActive),
    [menu]
  );

  const categories = useMemo(
    () => Array.from(new Set(menu.map((item) => item.category))).sort(),
    [menu]
  );

  const saveMenuItem = async (id: string, changes: Partial<MenuItem>) => {
    const updated = await updateMenuItem(id, changes);
    setMenu((items) => items.map((item) => (item.id === id ? updated : item)));
  };

  const addMenuItem = async (data: NewMenuItemInput) => {
    const created = await createMenuItem(data);
    setMenu((items) => [created, ...items]);
  };

  return (
    <MenuContext.Provider
      value={{
        menu,
        activeMenu,
        categories,
        loading,
        error,
        refreshMenu,
        saveMenuItem,
        addMenuItem,
      }}
    >
      {children}
    </MenuContext.Provider>
  );
}

export function useMenuContext() {
  const value = useContext(MenuContext);
  if (!value) throw new Error('useMenuContext must be used inside MenuProvider');
  return value;
}
```

================================================================================

--- BATCH 54 | server/src/data/customers.ts ---
STATUS: ✅ REVIEWABLE (185 lines)
FILE: server/src/data/customers.ts
```ts
import type { Customer } from '@groundup/shared-types';

export const CUSTOMERS: Customer[] = [
  {
    customerId: 'cust-001',
    firstName: 'Aaron',
    lastName: 'Goldstein',
    phone: '973-555-0198',
    email: 'agoldstein@example.com',
    loyaltyPoints: 450,
    dietaryNotes: 'No veal',
    address: { street: '123 Charlton Ave', city: 'South Orange', state: 'NJ', zip: '07079' },
  },
  {
    customerId: 'cust-002',
    firstName: 'Sarah',
    lastName: 'Levy',
    phone: '973-555-8822',
    email: 'slevy@example.com',
    loyaltyPoints: 120,
    dietaryNotes: '',
    address: { street: '45 Pleasant Valley Way', city: 'West Orange', state: 'NJ', zip: '07052' },
  },
  {
    customerId: 'cust-003',
    firstName: 'David',
    lastName: 'Cohen',
    phone: '973-555-3411',
    email: 'dcohen@example.com',
    loyaltyPoints: 890,
    dietaryNotes: 'Sesame allergy',
    address: { street: '78 Northfield Rd', city: 'Livingston', state: 'NJ', zip: '07039' },
  },
  {
    customerId: 'cust-004',
    firstName: 'Rivka',
    lastName: 'Friedman',
    phone: '973-555-2290',
    email: 'rfriedman@example.com',
    loyaltyPoints: 310,
    dietaryNotes: '',
    address: { street: '14 Vose Ave', city: 'South Orange', state: 'NJ', zip: '07079' },
  },
  {
    customerId: 'cust-005',
    firstName: 'Michael',
    lastName: 'Stern',
    phone: '973-555-7765',
    email: 'mstern@example.com',
    loyaltyPoints: 75,
    dietaryNotes: '',
    address: { street: '901 Eagle Rock Ave', city: 'West Orange', state: 'NJ', zip: '07052' },
  },
  {
    customerId: 'cust-006',
    firstName: 'Leah',
    lastName: 'Katz',
    phone: '973-555-4432',
    email: 'lkatz@example.com',
    loyaltyPoints: 540,
    dietaryNotes: 'No lamb',
    address: { street: '22 Wyoming Ave', city: 'Maplewood', state: 'NJ', zip: '07040' },
  },
  {
    customerId: 'cust-007',
    firstName: 'Joshua',
    lastName: 'Berkowitz',
    phone: '973-555-1183',
    email: 'jberkowitz@example.com',
    loyaltyPoints: 1020,
    dietaryNotes: '',
    address: { street: '6 Tillou Rd', city: 'South Orange', state: 'NJ', zip: '07079' },
  },
  {
    customerId: 'cust-008',
    firstName: 'Miriam',
    lastName: 'Adler',
    phone: '973-555-6654',
    email: 'madler@example.com',
    loyaltyPoints: 200,
    dietaryNotes: 'Dairy-free household',
    address: { street: '33 Mayhew Dr', city: 'Livingston', state: 'NJ', zip: '07039' },
  },
  {
    customerId: 'cust-009',
    firstName: 'Daniel',
    lastName: 'Rosen',
    phone: '973-555-9087',
    email: 'drosen@example.com',
    loyaltyPoints: 60,
    dietaryNotes: '',
    address: { street: '17 Ridgewood Rd', city: 'Maplewood', state: 'NJ', zip: '07040' },
  },
  {
    customerId: 'cust-010',
    firstName: 'Esther',
    lastName: 'Weiss',
    phone: '973-555-2298',
    email: 'eweiss@example.com',
    loyaltyPoints: 680,
    dietaryNotes: '',
    address: { street: '5 Crest Dr', city: 'West Orange', state: 'NJ', zip: '07052' },
  },
  {
    customerId: 'cust-011',
    firstName: 'Benjamin',
    lastName: 'Klein',
    phone: '973-555-3345',
    email: 'bklein@example.com',
    loyaltyPoints: 410,
    dietaryNotes: 'No organ meat',
    address: { street: '88 Wyoming Ave', city: 'Maplewood', state: 'NJ', zip: '07040' },
  },
  {
    customerId: 'cust-012',
    firstName: 'Tamar',
    lastName: 'Glick',
    phone: '973-555-7723',
    email: 'tglick@example.com',
    loyaltyPoints: 150,
    dietaryNotes: '',
    address: { street: '12 Forest Rd', city: 'South Orange', state: 'NJ', zip: '07079' },
  },
  {
    customerId: 'cust-013',
    firstName: 'Yosef',
    lastName: 'Mandel',
    phone: '973-555-5512',
    email: 'ymandel@example.com',
    loyaltyPoints: 920,
    dietaryNotes: '',
    address: { street: '40 Northfield Ave', city: 'West Orange', state: 'NJ', zip: '07052' },
  },
  {
    customerId: 'cust-014',
    firstName: 'Chana',
    lastName: 'Roth',
    phone: '973-555-8890',
    email: 'croth@example.com',
    loyaltyPoints: 25,
    dietaryNotes: 'New customer',
    address: { street: '3 Elmwood Ct', city: 'Livingston', state: 'NJ', zip: '07039' },
  },
  {
    customerId: 'cust-015',
    firstName: 'Ari',
    lastName: 'Schwartz',
    phone: '973-555-4421',
    email: 'aschwartz@example.com',
    loyaltyPoints: 330,
    dietaryNotes: '',
    address: { street: '27 Tillou Rd', city: 'South Orange', state: 'NJ', zip: '07079' },
  },
  {
    customerId: 'cust-016',
    firstName: 'Naomi',
    lastName: 'Brodsky',
    phone: '973-555-6678',
    email: 'nbrodsky@example.com',
    loyaltyPoints: 510,
    dietaryNotes: '',
    address: { street: '9 Hobart Ave', city: 'Maplewood', state: 'NJ', zip: '07040' },
  },
  {
    customerId: 'cust-017',
    firstName: 'Eli',
    lastName: 'Hoffman',
    phone: '973-555-2231',
    email: 'ehoffman@example.com',
    loyaltyPoints: 95,
    dietaryNotes: '',
    address: { street: '51 Pleasant Valley Way', city: 'West Orange', state: 'NJ', zip: '07052' },
  },
  {
    customerId: 'cust-018',
    firstName: 'Devorah',
    lastName: 'Lipman',
    phone: '973-555-9943',
    email: 'dlipman@example.com',
    loyaltyPoints: 720,
    dietaryNotes: 'Gluten-free',
    address: { street: '64 Northfield Rd', city: 'Livingston', state: 'NJ', zip: '07039' },
  },
];
```

================================================================================

--- BATCH 55 | server/src/data/historicalOrders.ts ---
STATUS: 🚨 VERY LARGE (1801 lines)
FILE: server/src/data/historicalOrders.ts
```ts
import type { Order } from '@groundup/shared-types';

export const HISTORICAL_ORDERS: Order[] = [
  {
    id: "hist-45",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "lamb-shoulder-chops",
        name: "Tevya's Ranch Shoulder Lamb Chops",
        quantity: 3.8,
        unitPrice: 17.99,
        lineTotal: 68.36,
      },
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 4.0,
        unitPrice: 19.99,
        lineTotal: 79.96,
      },
    ],
    total: 148.32,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1779934090700,
  },
  {
    id: "hist-23",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "beef-ribeye-boneless",
        name: "Boneless Rib-Eye Steak (Grain Finished)",
        quantity: 2.3,
        unitPrice: 23.99,
        lineTotal: 55.18,
      },
    ],
    total: 55.18,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1779984490700,
  },
  {
    id: "hist-26",
    customerId: "cust-011",
    customerName: "Benjamin Klein",
    items: [
      {
        menuItemId: "deli-beef-cholent-thu-fri",
        name: "Traditional Beef Cholent (Thursday/Friday Only)",
        quantity: 3,
        unitPrice: 11.99,
        lineTotal: 35.97,
      },
    ],
    total: 35.97,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780006090700,
  },
  {
    id: "hist-47",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 1.9,
        unitPrice: 10.99,
        lineTotal: 20.88,
      },
      {
        menuItemId: "lamb-baby-rib-chops",
        name: "Tevya's Ranch Baby Lamb Rib Chops",
        quantity: 3.7,
        unitPrice: 42.99,
        lineTotal: 159.06,
      },
    ],
    total: 179.94,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780013290700,
  },
  {
    id: "hist-1",
    customerId: "cust-001",
    customerName: "Aaron Goldstein",
    items: [
      {
        menuItemId: "beef-flank-steak",
        name: "Flank Steak (Deveined London Broil)",
        quantity: 2.8,
        unitPrice: 13.99,
        lineTotal: 39.17,
      },
      {
        menuItemId: "bev-sparkling-water",
        name: "Sparkling Water",
        quantity: 2,
        unitPrice: 1.99,
        lineTotal: 3.98,
      },
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 1.1,
        unitPrice: 10.99,
        lineTotal: 12.09,
      },
    ],
    total: 55.24,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780027690700,
  },
  {
    id: "hist-40",
    customerId: "cust-014",
    customerName: "Chana Roth",
    items: [
      {
        menuItemId: "poultry-cutlets-extra-thin-schnitzel",
        name: "Extra Thin Chicken Cutlets (Schnitzel Cut)",
        quantity: 2.4,
        unitPrice: 10.99,
        lineTotal: 26.38,
      },
      {
        menuItemId: "beef-ribeye-boneless",
        name: "Boneless Rib-Eye Steak (Grain Finished)",
        quantity: 1.8,
        unitPrice: 23.99,
        lineTotal: 43.18,
      },
      {
        menuItemId: "beef-chuck-eye-roast",
        name: "Chuck Eye Roast (Grain Finished)",
        quantity: 0.8,
        unitPrice: 12.99,
        lineTotal: 10.39,
      },
      {
        menuItemId: "deli-broccoli-kugel-12oz",
        name: "Broccoli Kugel (12 oz)",
        quantity: 1,
        unitPrice: 8.99,
        lineTotal: 8.99,
      },
    ],
    total: 88.94,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780038490700,
  },
  {
    id: "hist-10",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 3,
        unitPrice: 2.49,
        lineTotal: 7.47,
      },
      {
        menuItemId: "prep-london-broil-seasoned",
        name: "Meat Maven Seasoned London Broil",
        quantity: 1.0,
        unitPrice: 11.99,
        lineTotal: 11.99,
      },
    ],
    total: 19.46,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780056490700,
  },
  {
    id: "hist-17",
    customerId: "cust-009",
    customerName: "Daniel Rosen",
    items: [
      {
        menuItemId: "beef-brisket-first-cut",
        name: "First Cut Beef Brisket",
        quantity: 2.6,
        unitPrice: 15.49,
        lineTotal: 40.27,
      },
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 2.8,
        unitPrice: 15.99,
        lineTotal: 44.77,
      },
    ],
    total: 85.04,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780146490700,
  },
  {
    id: "hist-27",
    customerId: "cust-012",
    customerName: "Tamar Glick",
    items: [
      {
        menuItemId: "beef-marrow-bones-frozen",
        name: "Beef Marrow Bones (Freshly Frozen)",
        quantity: 2.6,
        unitPrice: 15.99,
        lineTotal: 41.57,
      },
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 1.6,
        unitPrice: 19.99,
        lineTotal: 31.98,
      },
    ],
    total: 73.55,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780153690700,
  },
  {
    id: "hist-46",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "bev-dr-browns-black-cherry",
        name: "Dr. Brown's Black Cherry",
        quantity: 2,
        unitPrice: 2.49,
        lineTotal: 4.98,
      },
      {
        menuItemId: "beef-pepper-steak-fajita",
        name: "Premium Pepper Steak (Fajita Strips)",
        quantity: 4.0,
        unitPrice: 13.99,
        lineTotal: 55.96,
      },
    ],
    total: 60.94,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780214890700,
  },
  {
    id: "hist-44",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 4.4,
        unitPrice: 4.49,
        lineTotal: 19.76,
      },
      {
        menuItemId: "beef-marrow-bones-frozen",
        name: "Beef Marrow Bones (Freshly Frozen)",
        quantity: 3.8,
        unitPrice: 15.99,
        lineTotal: 60.76,
      },
    ],
    total: 80.52,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780265290700,
  },
  {
    id: "hist-15",
    customerId: "cust-009",
    customerName: "Daniel Rosen",
    items: [
      {
        menuItemId: "prep-french-roast-dry-rub",
        name: "Oven-Ready Dry Rub French Roast",
        quantity: 4.2,
        unitPrice: 16.99,
        lineTotal: 71.36,
      },
      {
        menuItemId: "beef-ground-extra-lean",
        name: "Ground Beef Extra Lean",
        quantity: 4.1,
        unitPrice: 10.49,
        lineTotal: 43.01,
      },
      {
        menuItemId: "beef-cholent-meat-mock-flanken",
        name: "Tender Cholent Meat (Mock Flanken)",
        quantity: 3.9,
        unitPrice: 10.99,
        lineTotal: 42.86,
      },
    ],
    total: 157.23,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780268890700,
  },
  {
    id: "hist-3",
    customerId: "cust-001",
    customerName: "Aaron Goldstein",
    items: [
      {
        menuItemId: "beef-pepper-steak-fajita",
        name: "Premium Pepper Steak (Fajita Strips)",
        quantity: 1.8,
        unitPrice: 13.99,
        lineTotal: 25.18,
      },
      {
        menuItemId: "veal-chops-second-cut",
        name: "Veal Chops (Second Cut)",
        quantity: 3.2,
        unitPrice: 29.99,
        lineTotal: 95.97,
      },
    ],
    total: 121.15,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780294090700,
  },
  {
    id: "hist-12",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "beef-cholent-meat-mock-flanken",
        name: "Tender Cholent Meat (Mock Flanken)",
        quantity: 1.4,
        unitPrice: 10.99,
        lineTotal: 15.39,
      },
      {
        menuItemId: "poultry-turkey-london-broil-skinless",
        name: "Turkey London Broil (Skinless)",
        quantity: 0.8,
        unitPrice: 10.49,
        lineTotal: 8.39,
      },
      {
        menuItemId: "prep-london-broil-seasoned",
        name: "Meat Maven Seasoned London Broil",
        quantity: 3.5,
        unitPrice: 11.99,
        lineTotal: 41.97,
      },
    ],
    total: 65.75,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780297690700,
  },
  {
    id: "hist-49",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
      {
        menuItemId: "prep-stuffed-chicken-capon",
        name: "Meat Maven Stuffed Chicken Capon",
        quantity: 4.0,
        unitPrice: 11.99,
        lineTotal: 47.96,
      },
      {
        menuItemId: "lamb-shoulder-chops",
        name: "Tevya's Ranch Shoulder Lamb Chops",
        quantity: 1.8,
        unitPrice: 17.99,
        lineTotal: 32.38,
      },
      {
        menuItemId: "beef-ground-extra-lean",
        name: "Ground Beef Extra Lean",
        quantity: 2.9,
        unitPrice: 10.49,
        lineTotal: 30.42,
      },
    ],
    total: 113.25,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780297690700,
  },
  {
    id: "hist-22",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 1.1,
        unitPrice: 4.49,
        lineTotal: 4.94,
      },
      {
        menuItemId: "prep-beef-pastrami-2nd-cut",
        name: "Meat Maven Beef Pastrami (2nd Cut Brisket)",
        quantity: 3.2,
        unitPrice: 13.99,
        lineTotal: 44.77,
      },
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 2.1,
        unitPrice: 19.99,
        lineTotal: 41.98,
      },
      {
        menuItemId: "poultry-chicken-cutlets-family-pack",
        name: "Chicken Cutlets (Family Pack)",
        quantity: 2.3,
        unitPrice: 7.99,
        lineTotal: 18.38,
      },
    ],
    total: 110.07,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780348090700,
  },
  {
    id: "hist-30",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "beef-ribeye-boneless",
        name: "Boneless Rib-Eye Steak (Grain Finished)",
        quantity: 2.7,
        unitPrice: 23.99,
        lineTotal: 64.77,
      },
      {
        menuItemId: "deli-sliced-smoked-turkey-breast-12oz",
        name: "Sliced Smoked Turkey Breast (12 oz)",
        quantity: 3,
        unitPrice: 12.99,
        lineTotal: 38.97,
      },
      {
        menuItemId: "prep-beef-pastrami-2nd-cut",
        name: "Meat Maven Beef Pastrami (2nd Cut Brisket)",
        quantity: 1.4,
        unitPrice: 13.99,
        lineTotal: 19.59,
      },
    ],
    total: 123.33,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780351690700,
  },
  {
    id: "hist-13",
    customerId: "cust-006",
    customerName: "Leah Katz",
    items: [
      {
        menuItemId: "prep-baby-chicken-pargiot-grill-ready",
        name: "Seasoned Baby Chicken (Pargiot) - Grill Ready",
        quantity: 4.0,
        unitPrice: 10.99,
        lineTotal: 43.96,
      },
      {
        menuItemId: "beef-pepper-steak-fajita",
        name: "Premium Pepper Steak (Fajita Strips)",
        quantity: 1.9,
        unitPrice: 13.99,
        lineTotal: 26.58,
      },
    ],
    total: 70.54,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780384090700,
  },
  {
    id: "hist-29",
    customerId: "cust-012",
    customerName: "Tamar Glick",
    items: [
      {
        menuItemId: "poultry-chicken-cutlets-family-pack",
        name: "Chicken Cutlets (Family Pack)",
        quantity: 1.6,
        unitPrice: 7.99,
        lineTotal: 12.78,
      },
      {
        menuItemId: "poultry-turkey-london-broil-skinless",
        name: "Turkey London Broil (Skinless)",
        quantity: 1.0,
        unitPrice: 10.49,
        lineTotal: 10.49,
      },
    ],
    total: 23.27,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780506490700,
  },
  {
    id: "hist-35",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "poultry-whole-duck-frozen",
        name: "Whole Duck (Frozen)",
        quantity: 3.1,
        unitPrice: 14.99,
        lineTotal: 46.47,
      },
      {
        menuItemId: "beef-skirt-steak-prime",
        name: "Prime Beef Skirt Steak",
        quantity: 2.9,
        unitPrice: 47.99,
        lineTotal: 139.17,
      },
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 1.2,
        unitPrice: 8.49,
        lineTotal: 10.19,
      },
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 2.8,
        unitPrice: 10.99,
        lineTotal: 30.77,
      },
    ],
    total: 226.6,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780513690700,
  },
  {
    id: "hist-32",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "deli-sliced-beef-pastrami-6oz",
        name: "Sliced Beef Pastrami (6 oz)",
        quantity: 1,
        unitPrice: 9.99,
        lineTotal: 9.99,
      },
    ],
    total: 9.99,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780560490700,
  },
  {
    id: "hist-6",
    customerId: "cust-002",
    customerName: "Sarah Levy",
    items: [
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
      {
        menuItemId: "poultry-whole-duck-frozen",
        name: "Whole Duck (Frozen)",
        quantity: 1.6,
        unitPrice: 14.99,
        lineTotal: 23.98,
      },
    ],
    total: 26.47,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780592890700,
  },
  {
    id: "hist-14",
    customerId: "cust-008",
    customerName: "Miriam Adler",
    items: [
      {
        menuItemId: "beef-flanken-boneless",
        name: "Real Boneless Flanken",
        quantity: 3.6,
        unitPrice: 22.99,
        lineTotal: 82.76,
      },
      {
        menuItemId: "lamb-ground-premium",
        name: "Ground Lamb (Premium)",
        quantity: 2.8,
        unitPrice: 24.99,
        lineTotal: 69.97,
      },
    ],
    total: 152.73,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780614490700,
  },
  {
    id: "hist-41",
    customerId: "cust-015",
    customerName: "Ari Schwartz",
    items: [
      {
        menuItemId: "deli-beef-salami-chub-16oz",
        name: "Beef Salami Chub (16 oz)",
        quantity: 1,
        unitPrice: 10.49,
        lineTotal: 10.49,
      },
      {
        menuItemId: "bev-dr-browns-cel-ray",
        name: "Dr. Brown's Cel-Ray",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
      {
        menuItemId: "prep-london-broil-seasoned",
        name: "Meat Maven Seasoned London Broil",
        quantity: 4.3,
        unitPrice: 11.99,
        lineTotal: 51.56,
      },
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 3.4,
        unitPrice: 8.49,
        lineTotal: 28.87,
      },
    ],
    total: 93.41,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780643290700,
  },
  {
    id: "hist-5",
    customerId: "cust-002",
    customerName: "Sarah Levy",
    items: [
      {
        menuItemId: "beef-marrow-bones-frozen",
        name: "Beef Marrow Bones (Freshly Frozen)",
        quantity: 2.2,
        unitPrice: 15.99,
        lineTotal: 35.18,
      },
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 4.5,
        unitPrice: 10.99,
        lineTotal: 49.45,
      },
    ],
    total: 84.63,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1780654090700,
  },
  {
    id: "hist-33",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 3.2,
        unitPrice: 19.99,
        lineTotal: 63.97,
      },
    ],
    total: 63.97,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780776490700,
  },
  {
    id: "hist-34",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "poultry-dark-cutlets-pargiot",
        name: "Dark Chicken Cutlets (Pargiot)",
        quantity: 2.0,
        unitPrice: 6.99,
        lineTotal: 13.98,
      },
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 1.8,
        unitPrice: 15.99,
        lineTotal: 28.78,
      },
      {
        menuItemId: "bev-dr-browns-cel-ray",
        name: "Dr. Brown's Cel-Ray",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
    ],
    total: 45.25,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780787290700,
  },
  {
    id: "hist-9",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "beef-brisket-first-cut",
        name: "First Cut Beef Brisket",
        quantity: 2.6,
        unitPrice: 15.49,
        lineTotal: 40.27,
      },
      {
        menuItemId: "prep-baby-chicken-pargiot-grill-ready",
        name: "Seasoned Baby Chicken (Pargiot) - Grill Ready",
        quantity: 3.6,
        unitPrice: 10.99,
        lineTotal: 39.56,
      },
    ],
    total: 79.83,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1780801690700,
  },
  {
    id: "hist-36",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "beef-skirt-steak-prime",
        name: "Prime Beef Skirt Steak",
        quantity: 1.9,
        unitPrice: 47.99,
        lineTotal: 91.18,
      },
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 2.4,
        unitPrice: 4.49,
        lineTotal: 10.78,
      },
      {
        menuItemId: "lamb-ground-premium",
        name: "Ground Lamb (Premium)",
        quantity: 2.8,
        unitPrice: 24.99,
        lineTotal: 69.97,
      },
    ],
    total: 171.93,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1780988890700,
  },
  {
    id: "hist-31",
    customerId: "cust-013",
    customerName: "Yosef Mandel",
    items: [
      {
        menuItemId: "deli-potato-kugel-family-24oz",
        name: "Family Size Potato Kugel (24 oz)",
        quantity: 1,
        unitPrice: 11.99,
        lineTotal: 11.99,
      },
    ],
    total: 11.99,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781010490700,
  },
  {
    id: "hist-8",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "beef-flanken-boneless",
        name: "Real Boneless Flanken",
        quantity: 3.6,
        unitPrice: 22.99,
        lineTotal: 82.76,
      },
      {
        menuItemId: "poultry-cutlets-extra-thin-schnitzel",
        name: "Extra Thin Chicken Cutlets (Schnitzel Cut)",
        quantity: 2.8,
        unitPrice: 10.99,
        lineTotal: 30.77,
      },
      {
        menuItemId: "beef-brisket-first-cut",
        name: "First Cut Beef Brisket",
        quantity: 3.6,
        unitPrice: 15.49,
        lineTotal: 55.76,
      },
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 2.4,
        unitPrice: 4.49,
        lineTotal: 10.78,
      },
    ],
    total: 180.07,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1781060890700,
  },
  {
    id: "hist-11",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "deli-beef-salami-chub-16oz",
        name: "Beef Salami Chub (16 oz)",
        quantity: 3,
        unitPrice: 10.49,
        lineTotal: 31.47,
      },
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 1.2,
        unitPrice: 8.49,
        lineTotal: 10.19,
      },
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 3,
        unitPrice: 2.49,
        lineTotal: 7.47,
      },
    ],
    total: 49.13,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1781294890700,
  },
  {
    id: "hist-28",
    customerId: "cust-012",
    customerName: "Tamar Glick",
    items: [
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 1.9,
        unitPrice: 8.49,
        lineTotal: 16.13,
      },
      {
        menuItemId: "veal-chops-second-cut",
        name: "Veal Chops (Second Cut)",
        quantity: 3.4,
        unitPrice: 29.99,
        lineTotal: 101.97,
      },
      {
        menuItemId: "poultry-cutlets-extra-thin-schnitzel",
        name: "Extra Thin Chicken Cutlets (Schnitzel Cut)",
        quantity: 3.5,
        unitPrice: 10.99,
        lineTotal: 38.47,
      },
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 3.7,
        unitPrice: 10.99,
        lineTotal: 40.66,
      },
    ],
    total: 197.23,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781359690700,
  },
  {
    id: "hist-19",
    customerId: "cust-009",
    customerName: "Daniel Rosen",
    items: [
      {
        menuItemId: "beef-cholent-meat-mock-flanken",
        name: "Tender Cholent Meat (Mock Flanken)",
        quantity: 2.1,
        unitPrice: 10.99,
        lineTotal: 23.08,
      },
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 3.0,
        unitPrice: 4.49,
        lineTotal: 13.47,
      },
      {
        menuItemId: "poultry-drumsticks-family-pack",
        name: "Chicken Drumsticks (Family Pack)",
        quantity: 1.7,
        unitPrice: 3.29,
        lineTotal: 5.59,
      },
      {
        menuItemId: "beef-skirt-steak-prime",
        name: "Prime Beef Skirt Steak",
        quantity: 1.6,
        unitPrice: 47.99,
        lineTotal: 76.78,
      },
    ],
    total: 118.92,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1781399290700,
  },
  {
    id: "hist-4",
    customerId: "cust-002",
    customerName: "Sarah Levy",
    items: [
      {
        menuItemId: "poultry-dark-cutlets-pargiot",
        name: "Dark Chicken Cutlets (Pargiot)",
        quantity: 3.4,
        unitPrice: 6.99,
        lineTotal: 23.77,
      },
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 3.3,
        unitPrice: 15.99,
        lineTotal: 52.77,
      },
      {
        menuItemId: "poultry-ground-chicken-white",
        name: "Ground Chicken (White)",
        quantity: 1.1,
        unitPrice: 8.99,
        lineTotal: 9.89,
      },
    ],
    total: 86.43,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1781431690700,
  },
  {
    id: "hist-43",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 3,
        unitPrice: 2.49,
        lineTotal: 7.47,
      },
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 1.2,
        unitPrice: 4.49,
        lineTotal: 5.39,
      },
    ],
    total: 12.86,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1781446090700,
  },
  {
    id: "hist-24",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "poultry-chicken-cutlets-family-pack",
        name: "Chicken Cutlets (Family Pack)",
        quantity: 2.5,
        unitPrice: 7.99,
        lineTotal: 19.98,
      },
      {
        menuItemId: "prep-stuffed-chicken-capon",
        name: "Meat Maven Stuffed Chicken Capon",
        quantity: 2.4,
        unitPrice: 11.99,
        lineTotal: 28.78,
      },
    ],
    total: 48.76,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781525290700,
  },
  {
    id: "hist-38",
    customerId: "cust-014",
    customerName: "Chana Roth",
    items: [
      {
        menuItemId: "poultry-cutlets-extra-thin-schnitzel",
        name: "Extra Thin Chicken Cutlets (Schnitzel Cut)",
        quantity: 4.1,
        unitPrice: 10.99,
        lineTotal: 45.06,
      },
      {
        menuItemId: "beef-cholent-meat-mock-flanken",
        name: "Tender Cholent Meat (Mock Flanken)",
        quantity: 3.5,
        unitPrice: 10.99,
        lineTotal: 38.47,
      },
    ],
    total: 83.53,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781546890700,
  },
  {
    id: "hist-2",
    customerId: "cust-001",
    customerName: "Aaron Goldstein",
    items: [
      {
        menuItemId: "deli-sliced-beef-pastrami-6oz",
        name: "Sliced Beef Pastrami (6 oz)",
        quantity: 1,
        unitPrice: 9.99,
        lineTotal: 9.99,
      },
      {
        menuItemId: "poultry-chicken-cutlets-family-pack",
        name: "Chicken Cutlets (Family Pack)",
        quantity: 1.0,
        unitPrice: 7.99,
        lineTotal: 7.99,
      },
      {
        menuItemId: "bev-dr-browns-cel-ray",
        name: "Dr. Brown's Cel-Ray",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
    ],
    total: 20.47,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1781550490700,
  },
  {
    id: "hist-37",
    customerId: "cust-014",
    customerName: "Chana Roth",
    items: [
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 3.8,
        unitPrice: 15.99,
        lineTotal: 60.76,
      },
    ],
    total: 60.76,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1781554090700,
  },
  {
    id: "hist-16",
    customerId: "cust-009",
    customerName: "Daniel Rosen",
    items: [
      {
        menuItemId: "deli-sliced-beef-pastrami-6oz",
        name: "Sliced Beef Pastrami (6 oz)",
        quantity: 1,
        unitPrice: 9.99,
        lineTotal: 9.99,
      },
      {
        menuItemId: "beef-chuck-eye-roast",
        name: "Chuck Eye Roast (Grain Finished)",
        quantity: 3.5,
        unitPrice: 12.99,
        lineTotal: 45.47,
      },
    ],
    total: 55.46,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781597290700,
  },
  {
    id: "hist-18",
    customerId: "cust-009",
    customerName: "Daniel Rosen",
    items: [
      {
        menuItemId: "poultry-dark-cutlets-pargiot",
        name: "Dark Chicken Cutlets (Pargiot)",
        quantity: 1.5,
        unitPrice: 6.99,
        lineTotal: 10.48,
      },
      {
        menuItemId: "prep-stuffed-chicken-capon",
        name: "Meat Maven Stuffed Chicken Capon",
        quantity: 2.0,
        unitPrice: 11.99,
        lineTotal: 23.98,
      },
      {
        menuItemId: "bev-sparkling-water",
        name: "Sparkling Water",
        quantity: 3,
        unitPrice: 1.99,
        lineTotal: 5.97,
      },
      {
        menuItemId: "bev-dr-browns-black-cherry",
        name: "Dr. Brown's Black Cherry",
        quantity: 3,
        unitPrice: 2.49,
        lineTotal: 7.47,
      },
    ],
    total: 47.9,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1781651290700,
  },
  {
    id: "hist-25",
    customerId: "cust-011",
    customerName: "Benjamin Klein",
    items: [
      {
        menuItemId: "prep-turkey-london-broil-seasoned",
        name: "Meat Maven Seasoned Turkey London Broil",
        quantity: 3.8,
        unitPrice: 9.99,
        lineTotal: 37.96,
      },
    ],
    total: 37.96,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781658490700,
  },
  {
    id: "hist-50",
    customerId: "cust-018",
    customerName: "Devorah Lipman",
    items: [
      {
        menuItemId: "lamb-baby-rib-chops",
        name: "Tevya's Ranch Baby Lamb Rib Chops",
        quantity: 3.0,
        unitPrice: 42.99,
        lineTotal: 128.97,
      },
      {
        menuItemId: "bev-sparkling-water",
        name: "Sparkling Water",
        quantity: 3,
        unitPrice: 1.99,
        lineTotal: 5.97,
      },
      {
        menuItemId: "poultry-whole-duck-frozen",
        name: "Whole Duck (Frozen)",
        quantity: 1.2,
        unitPrice: 14.99,
        lineTotal: 17.99,
      },
      {
        menuItemId: "prep-london-broil-seasoned",
        name: "Meat Maven Seasoned London Broil",
        quantity: 4.1,
        unitPrice: 11.99,
        lineTotal: 49.16,
      },
    ],
    total: 202.09,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781705290700,
  },
  {
    id: "hist-48",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "veal-ground",
        name: "Ground Veal",
        quantity: 1.8,
        unitPrice: 19.99,
        lineTotal: 35.98,
      },
      {
        menuItemId: "lamb-shoulder-chops",
        name: "Tevya's Ranch Shoulder Lamb Chops",
        quantity: 1.1,
        unitPrice: 17.99,
        lineTotal: 19.79,
      },
    ],
    total: 55.77,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781770090700,
  },
  {
    id: "hist-51",
    customerId: "cust-018",
    customerName: "Devorah Lipman",
    items: [
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 3.5,
        unitPrice: 8.49,
        lineTotal: 29.71,
      },
      {
        menuItemId: "prep-lamb-beef-kabobs",
        name: "Meat Maven Lamb & Beef Kabobs",
        quantity: 3.5,
        unitPrice: 13.99,
        lineTotal: 48.97,
      },
      {
        menuItemId: "prep-stuffed-chicken-capon",
        name: "Meat Maven Stuffed Chicken Capon",
        quantity: 3.3,
        unitPrice: 11.99,
        lineTotal: 39.57,
      },
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 2,
        unitPrice: 2.49,
        lineTotal: 4.98,
      },
    ],
    total: 123.23,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781852890700,
  },
  {
    id: "hist-42",
    customerId: "cust-016",
    customerName: "Naomi Brodsky",
    items: [
      {
        menuItemId: "deli-sliced-smoked-turkey-breast-12oz",
        name: "Sliced Smoked Turkey Breast (12 oz)",
        quantity: 2,
        unitPrice: 12.99,
        lineTotal: 25.98,
      },
      {
        menuItemId: "deli-beef-salami-chub-16oz",
        name: "Beef Salami Chub (16 oz)",
        quantity: 1,
        unitPrice: 10.49,
        lineTotal: 10.49,
      },
      {
        menuItemId: "beef-flanken-boneless",
        name: "Real Boneless Flanken",
        quantity: 1.0,
        unitPrice: 22.99,
        lineTotal: 22.99,
      },
    ],
    total: 59.46,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1781993290700,
  },
  {
    id: "hist-39",
    customerId: "cust-014",
    customerName: "Chana Roth",
    items: [
      {
        menuItemId: "bev-dr-browns-cel-ray",
        name: "Dr. Brown's Cel-Ray",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
      {
        menuItemId: "lamb-shoulder-chops",
        name: "Tevya's Ranch Shoulder Lamb Chops",
        quantity: 3.3,
        unitPrice: 17.99,
        lineTotal: 59.37,
      },
    ],
    total: 61.86,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1782025690700,
  },
  {
    id: "hist-20",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "poultry-drumsticks-family-pack",
        name: "Chicken Drumsticks (Family Pack)",
        quantity: 1.0,
        unitPrice: 3.29,
        lineTotal: 3.29,
      },
    ],
    total: 3.29,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1782148090700,
  },
  {
    id: "hist-7",
    customerId: "cust-003",
    customerName: "David Cohen",
    items: [
      {
        menuItemId: "beef-skirt-steak-prime",
        name: "Prime Beef Skirt Steak",
        quantity: 4.0,
        unitPrice: 47.99,
        lineTotal: 191.96,
      },
      {
        menuItemId: "poultry-dark-cutlets-pargiot",
        name: "Dark Chicken Cutlets (Pargiot)",
        quantity: 2.0,
        unitPrice: 6.99,
        lineTotal: 13.98,
      },
      {
        menuItemId: "deli-sliced-beef-pastrami-6oz",
        name: "Sliced Beef Pastrami (6 oz)",
        quantity: 3,
        unitPrice: 9.99,
        lineTotal: 29.97,
      },
    ],
    total: 235.91,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1782277690700,
  },
  {
    id: "hist-21",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "poultry-ground-chicken-white",
        name: "Ground Chicken (White)",
        quantity: 2.6,
        unitPrice: 8.99,
        lineTotal: 23.37,
      },
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 2.8,
        unitPrice: 15.99,
        lineTotal: 44.77,
      },
    ],
    total: 68.14,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1782277690700,
  },
  {
    id: "hist-54",
    customerId: "cust-012",
    customerName: "Tamar Glick",
    items: [
      {
        menuItemId: "deli-beef-salami-chub-16oz",
        name: "Beef Salami Chub (16 oz)",
        quantity: 1,
        unitPrice: 10.49,
        lineTotal: 10.49,
      },
      {
        menuItemId: "beef-ground-family-pack",
        name: "Ground Beef Family Pack (Grain Finished)",
        quantity: 2.4,
        unitPrice: 8.49,
        lineTotal: 20.38,
      },
      {
        menuItemId: "poultry-dark-turkey-roast",
        name: "Dark Turkey Roast",
        quantity: 2.9,
        unitPrice: 9.99,
        lineTotal: 28.97,
      },
      {
        menuItemId: "poultry-turkey-london-broil-skinless",
        name: "Turkey London Broil (Skinless)",
        quantity: 3.2,
        unitPrice: 10.49,
        lineTotal: 33.57,
      },
    ],
    total: 93.41,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1782463130994,
  },
  {
    id: "hist-53",
    customerId: "cust-006",
    customerName: "Leah Katz",
    items: [
      {
        menuItemId: "poultry-chicken-cutlets-family-pack",
        name: "Chicken Cutlets (Family Pack)",
        quantity: 3.3,
        unitPrice: 7.99,
        lineTotal: 26.37,
      },
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 2.2,
        unitPrice: 19.99,
        lineTotal: 43.98,
      },
    ],
    total: 70.35,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1782470469413,
  },
  {
    id: "hist-52",
    customerId: "cust-008",
    customerName: "Miriam Adler",
    items: [
      {
        menuItemId: "deli-chicken-schnitzel-prepared",
        name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
        quantity: 4.1,
        unitPrice: 15.99,
        lineTotal: 65.56,
      },
      {
        menuItemId: "beef-cholent-meat-mock-flanken",
        name: "Tender Cholent Meat (Mock Flanken)",
        quantity: 2.7,
        unitPrice: 10.99,
        lineTotal: 29.67,
      },
      {
        menuItemId: "bev-sparkling-water",
        name: "Sparkling Water",
        quantity: 3,
        unitPrice: 1.99,
        lineTotal: 5.97,
      },
    ],
    total: 101.2,
    status: "completed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1782477779266,
  },
  {
    id: "hist-59",
    customerId: "cust-003",
    customerName: "David Cohen",
    items: [
      {
        menuItemId: "beef-pepper-steak-fajita",
        name: "Premium Pepper Steak (Fajita Strips)",
        quantity: 3.7,
        unitPrice: 13.99,
        lineTotal: 51.76,
      },
    ],
    total: 51.76,
    status: "ready",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Sam",
    createdAt: 1782484990700,
  },
  {
    id: "hist-58",
    customerId: "cust-004",
    customerName: "Rivka Friedman",
    items: [
      {
        menuItemId: "deli-sweet-noodle-kugel-24oz",
        name: "Sweet Noodle Kugel (24 oz)",
        quantity: 3,
        unitPrice: 10.99,
        lineTotal: 32.97,
      },
      {
        menuItemId: "beef-skirt-steak-prime",
        name: "Prime Beef Skirt Steak",
        quantity: 2.9,
        unitPrice: 47.99,
        lineTotal: 139.17,
      },
      {
        menuItemId: "prep-dark-turkey-shawarma",
        name: "Meat Maven Dark Turkey Shawarma",
        quantity: 3.5,
        unitPrice: 10.99,
        lineTotal: 38.47,
      },
      {
        menuItemId: "veal-stew-meat",
        name: "Veal Stew Meat",
        quantity: 2.2,
        unitPrice: 19.99,
        lineTotal: 43.98,
      },
    ],
    total: 254.59,
    status: "in_prep",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Dani",
    createdAt: 1782485410700,
  },
  {
    id: "hist-57",
    customerId: null,
    customerName: "Walk-in",
    items: [
      {
        menuItemId: "beef-chuck-eye-roast",
        name: "Chuck Eye Roast (Grain Finished)",
        quantity: 1.9,
        unitPrice: 12.99,
        lineTotal: 24.68,
      },
      {
        menuItemId: "poultry-thighs-family-pack",
        name: "Chicken Thighs (Family Pack)",
        quantity: 3.8,
        unitPrice: 4.49,
        lineTotal: 17.06,
      },
      {
        menuItemId: "bev-dr-browns-cream-soda",
        name: "Dr. Brown's Cream Soda",
        quantity: 1,
        unitPrice: 2.49,
        lineTotal: 2.49,
      },
    ],
    total: 44.23,
    status: "in_prep",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: "Mike",
    createdAt: 1782485770700,
  },
  {
    id: "hist-56",
    customerId: null,
    customerName: "Walk-in",
    items: [
      {
        menuItemId: "poultry-turkey-london-broil-skinless",
        name: "Turkey London Broil (Skinless)",
        quantity: 2.7,
        unitPrice: 10.49,
        lineTotal: 28.32,
      },
    ],
    total: 28.32,
    status: "placed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: null,
    createdAt: 1782486010700,
  },
  {
    id: "hist-55",
    customerId: "cust-010",
    customerName: "Esther Weiss",
    items: [
      {
        menuItemId: "prep-baby-chicken-pargiot-grill-ready",
        name: "Seasoned Baby Chicken (Pargiot) - Grill Ready",
        quantity: 3.7,
        unitPrice: 10.99,
        lineTotal: 40.66,
      },
      {
        menuItemId: "prep-beef-pastrami-2nd-cut",
        name: "Meat Maven Beef Pastrami (2nd Cut Brisket)",
        quantity: 4.4,
        unitPrice: 13.99,
        lineTotal: 61.56,
      },
    ],
    total: 102.22,
    status: "placed",
    source: 'counter',
    fulfillment: 'in_store',
    claimedBy: null,
    createdAt: 1782486370700,
  },
];
```

================================================================================

--- BATCH 56 | server/src/data/menu.ts ---
STATUS: ⚠️ LARGE (428 lines)
FILE: server/src/data/menu.ts
```ts
import type { MenuItem } from '@groundup/shared-types';

export const MENU: MenuItem[] = [
  {
    id: "beef-ground-family-pack",
    name: "Ground Beef Family Pack (Grain Finished)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 8.49,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-ground-extra-lean",
    name: "Ground Beef Extra Lean",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 10.49,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-flank-steak",
    name: "Flank Steak (Deveined London Broil)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 13.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-ribeye-boneless",
    name: "Boneless Rib-Eye Steak (Grain Finished)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 23.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-skirt-steak-prime",
    name: "Prime Beef Skirt Steak",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 47.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-chuck-eye-roast",
    name: "Chuck Eye Roast (Grain Finished)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 12.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-pepper-steak-fajita",
    name: "Premium Pepper Steak (Fajita Strips)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 13.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-cholent-meat-mock-flanken",
    name: "Tender Cholent Meat (Mock Flanken)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 10.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-brisket-first-cut",
    name: "First Cut Beef Brisket",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 15.49,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-flanken-boneless",
    name: "Real Boneless Flanken",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 22.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "beef-marrow-bones-frozen",
    name: "Beef Marrow Bones (Freshly Frozen)",
    category: "Glatt Kosher Beef (Fresh Cuts)",
    price: 15.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-whole-chicken",
    name: "Whole Chicken (Super Clean)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 3.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-chicken-cutlets-family-pack",
    name: "Chicken Cutlets (Family Pack)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 7.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-dark-cutlets-pargiot",
    name: "Dark Chicken Cutlets (Pargiot)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 6.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-drumsticks-family-pack",
    name: "Chicken Drumsticks (Family Pack)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 3.29,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-thighs-family-pack",
    name: "Chicken Thighs (Family Pack)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 4.49,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-cutlets-extra-thin-schnitzel",
    name: "Extra Thin Chicken Cutlets (Schnitzel Cut)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 10.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-ground-chicken-white",
    name: "Ground Chicken (White)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 8.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-turkey-london-broil-skinless",
    name: "Turkey London Broil (Skinless)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 10.49,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-dark-turkey-roast",
    name: "Dark Turkey Roast",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 9.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "poultry-whole-duck-frozen",
    name: "Whole Duck (Frozen)",
    category: "Fresh Poultry (Chicken & Turkey)",
    price: 14.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "lamb-shoulder-chops",
    name: "Tevya's Ranch Shoulder Lamb Chops",
    category: "Premium Lamb & Veal",
    price: 17.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "lamb-baby-rib-chops",
    name: "Tevya's Ranch Baby Lamb Rib Chops",
    category: "Premium Lamb & Veal",
    price: 42.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "lamb-ground-premium",
    name: "Ground Lamb (Premium)",
    category: "Premium Lamb & Veal",
    price: 24.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "veal-chops-second-cut",
    name: "Veal Chops (Second Cut)",
    category: "Premium Lamb & Veal",
    price: 29.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "veal-ground",
    name: "Ground Veal",
    category: "Premium Lamb & Veal",
    price: 19.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "veal-stew-meat",
    name: "Veal Stew Meat",
    category: "Premium Lamb & Veal",
    price: 19.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-london-broil-seasoned",
    name: "Meat Maven Seasoned London Broil",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 11.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-turkey-london-broil-seasoned",
    name: "Meat Maven Seasoned Turkey London Broil",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 9.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-dark-turkey-shawarma",
    name: "Meat Maven Dark Turkey Shawarma",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 10.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-beef-pastrami-2nd-cut",
    name: "Meat Maven Beef Pastrami (2nd Cut Brisket)",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 13.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-french-roast-dry-rub",
    name: "Oven-Ready Dry Rub French Roast",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 16.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-baby-chicken-pargiot-grill-ready",
    name: "Seasoned Baby Chicken (Pargiot) - Grill Ready",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 10.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-lamb-beef-kabobs",
    name: "Meat Maven Lamb & Beef Kabobs",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 13.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "prep-stuffed-chicken-capon",
    name: "Meat Maven Stuffed Chicken Capon",
    category: "Marinated & Oven-Ready Specials (Butcher's Prep)",
    price: 11.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "deli-beef-salami-chub-16oz",
    name: "Beef Salami Chub (16 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 10.49,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-sliced-beef-pastrami-6oz",
    name: "Sliced Beef Pastrami (6 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 9.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-sliced-smoked-turkey-breast-12oz",
    name: "Sliced Smoked Turkey Breast (12 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 12.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-potato-kugel-family-24oz",
    name: "Family Size Potato Kugel (24 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 11.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-sweet-noodle-kugel-24oz",
    name: "Sweet Noodle Kugel (24 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 10.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-broccoli-kugel-12oz",
    name: "Broccoli Kugel (12 oz)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 8.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-beef-cholent-thu-fri",
    name: "Traditional Beef Cholent (Thursday/Friday Only)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 11.99,
    unit: "per container",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "deli-chicken-schnitzel-prepared",
    name: "Prepared Breaded Chicken Schnitzel (Ready to Eat)",
    category: "Prepared Deli, Provisions & Shabbos Takeout",
    price: 15.99,
    unit: "per lb",
    soldBy: "weight",
    isActive: true,
  },
  {
    id: "bev-dr-browns-cream-soda",
    name: "Dr. Brown's Cream Soda",
    category: "Beverages",
    price: 2.49,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "bev-dr-browns-black-cherry",
    name: "Dr. Brown's Black Cherry",
    category: "Beverages",
    price: 2.49,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "bev-dr-browns-cel-ray",
    name: "Dr. Brown's Cel-Ray",
    category: "Beverages",
    price: 2.49,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
  {
    id: "bev-sparkling-water",
    name: "Sparkling Water",
    category: "Beverages",
    price: 1.99,
    unit: "per each",
    soldBy: "each",
    isActive: true,
  },
];
```

================================================================================

